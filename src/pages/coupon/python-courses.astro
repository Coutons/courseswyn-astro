---
import Layout from '../../layouts/Layout.astro';
import DealCard from '../../components/coupon/DealCard.astro';
import coupons from '../../data/coupons.json';

const CATEGORY = 'Programming';
const ITEMS_PER_PAGE = 12;

export async function getStaticPaths() {
  return [{ params: {} }];
}

const programmingCoupons = coupons.filter(c => 
  c.category === 'Programming' || c.category?.includes('Python')
);
const totalCoupons = programmingCoupons.length;
const totalPages = Math.ceil(totalCoupons / ITEMS_PER_PAGE);
const currentMonthYear = new Date().toLocaleDateString('en-US', { month: 'long', year: 'numeric' });
const couponsLast24h = programmingCoupons.filter(c => {
  const now = new Date();
  const oneDayAgo = new Date(now.getTime() - 24 * 60 * 60 * 1000);
  return new Date(c.addedDate) >= oneDayAgo && new Date(c.addedDate) <= now;
}).length;

const couponsJSON = JSON.stringify(programmingCoupons);
---

<script define:vars={{ couponsJSON, ITEMS_PER_PAGE }}>
  const couponsData = JSON.parse(couponsJSON);
  let filteredCoupons = [...couponsData];
  let currentPage = 1;

  window.goToPage = function(page) {
    currentPage = Math.max(1, Math.min(page, Math.ceil(filteredCoupons.length / ITEMS_PER_PAGE)));
    updateCouponsDisplay();
    updatePagination();
    window.scrollTo({ top: 0, behavior: 'smooth' });
  };

  function updateCouponsDisplay() {
    const grid = document.getElementById('coupons-grid');
    if (!grid) return;
    const startIdx = (currentPage - 1) * ITEMS_PER_PAGE;
    const endIdx = startIdx + ITEMS_PER_PAGE;
    const pageItems = filteredCoupons.slice(startIdx, endIdx);
    const cards = grid.querySelectorAll('[data-coupon-slug]');
    cards.forEach(card => card.style.display = 'none');
    pageItems.forEach(coupon => {
      const card = grid.querySelector(`[data-coupon-slug="${coupon.slug}"]`);
      if (card) card.style.display = '';
    });
  }

  function updatePagination() {
    const totalPages = Math.ceil(filteredCoupons.length / ITEMS_PER_PAGE);
    const paginationControls = document.getElementById('pagination-controls');
    if (paginationControls) {
      paginationControls.innerHTML = '';
      if (totalPages <= 1) return;
      for (let i = 1; i <= totalPages; i++) {
        const pageBtn = document.createElement('button');
        pageBtn.textContent = String(i);
        pageBtn.className = `px-3 py-2 rounded-lg transition ${
          i === currentPage 
            ? 'bg-blue-500/20 border border-blue-500/50 text-white' 
            : 'border border-white/10 hover:bg-white/5 text-white/70 hover:text-white'
        }`;
        pageBtn.onclick = () => window.goToPage(i);
        paginationControls.appendChild(pageBtn);
      }
    }
  }

  document.addEventListener('DOMContentLoaded', () => {
    updateCouponsDisplay();
    updatePagination();
  });
</script>

<Layout 
  title={`Free Python & Web Development Courses - 100% Off Udemy Coupon Codes ${currentMonthYear}`}
  description={`Browse ${totalCoupons}+ free Python & programming courses with 100% off Udemy coupon codes and discount deals. Learn web development, Django, Flask, APIs, databases with verified daily updated coupon codes. ${couponsLast24h}+ new courses added daily.`}
  structuredData={{
    "@context": "https://schema.org",
    "@type": "CollectionPage",
    "name": `Free Python & Programming Udemy Coupons ${currentMonthYear}`,
    "description": `${totalCoupons}+ verified 100% off Udemy programming courses`,
    "url": `https://courseswyn.com/coupon/python-courses/`,
    "mainEntity": {
      "@type": "AggregateOffer",
      "name": `Free Python & Programming Courses with 100% Off Coupons`,
      "price": "0",
      "priceCurrency": "USD",
      "offerCount": totalCoupons
    }
  }}
>
  <section class="mb-16 relative">
    <div class="absolute inset-0 -z-10">
      <div class="absolute top-0 left-1/4 w-72 h-72 bg-blue-500/20 rounded-full blur-3xl animate-pulse"></div>
      <div class="absolute bottom-0 right-1/4 w-72 h-72 bg-cyan-500/20 rounded-full blur-3xl animate-pulse" style="animation-delay: 1s;"></div>
    </div>

    <div class="relative">
      <div class="inline-flex items-center gap-3 mb-6">
        <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-gradient-to-r from-blue-500/20 to-cyan-500/20 border border-blue-500/40">
          <span class="w-2 h-2 rounded-full bg-gradient-to-r from-blue-400 to-cyan-400 animate-pulse"></span>
          <span class="text-xs font-semibold text-blue-300">✓ Verified Daily</span>
        </div>
        <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-gradient-to-r from-cyan-500/20 to-blue-500/20 border border-cyan-500/40">
          <span class="text-xs font-semibold text-cyan-300">{couponsLast24h}+ New in 24h</span>
        </div>
      </div>

      <h1 class="text-5xl md:text-6xl lg:text-7xl font-black text-white mb-2 leading-tight">
        Best <strong>Python & Programming</strong> Courses<br />
        <span class="bg-gradient-to-r from-blue-400 via-cyan-400 to-teal-400 bg-clip-text text-transparent">100% Off Udemy Coupons</span>
      </h1>
      <p class="text-lg md:text-xl text-blue-300 font-semibold mb-6">Master Backend, Web Development, APIs, Databases & More - Free & Verified Daily</p>

      <p class="text-lg text-white/80 leading-relaxed mb-6 max-w-2xl">
        Learn <strong>Python and programming</strong> with {totalCoupons}+ verified 100% off Udemy coupons. Get instant access to premium courses covering Python, web development, JavaScript, databases, and backend technologies with lifetime access and {couponsLast24h}+ new courses daily.
      </p>

      <div class="flex gap-3 flex-wrap">
        <a href="/coupon/" class="px-6 py-3 rounded-lg bg-gradient-to-r from-blue-500 to-cyan-500 hover:shadow-lg hover:shadow-blue-500/50 text-white font-semibold transition">
          Browse All {coupons.length}+ Coupons
        </a>
        <a href="#coupons-grid" class="px-6 py-3 rounded-lg border border-white/20 hover:border-blue-400/50 hover:bg-blue-500/10 text-white font-semibold transition">
          View Programming Courses ↓
        </a>
      </div>
    </div>
  </section>

  <section class="mb-16 py-12 rounded-2xl bg-gradient-to-r from-blue-500/20 via-cyan-500/15 to-teal-500/10 border border-blue-500/30 backdrop-blur-sm p-8">
    <h2 class="text-3xl md:text-4xl font-black text-white mb-6">
      Why Learn <strong>Python & Programming</strong> in {currentMonthYear}?
    </h2>
    <div class="grid md:grid-cols-3 gap-6">
      <div class="flex gap-4">
        <div class="text-3xl">💻</div>
        <div>
          <h3 class="font-bold text-white mb-2">High-Demand Skills</h3>
          <p class="text-white/70 text-sm">Python developers earn $120K-$180K+ annually. Master web development, backend coding, and data science with industry-standard technologies from our free courses.</p>
        </div>
      </div>
      <div class="flex gap-4">
        <div class="text-3xl">🚀</div>
        <div>
          <h3 class="font-bold text-white mb-2">Build Real Projects</h3>
          <p class="text-white/70 text-sm">Create web apps, APIs, databases, and full-stack applications. Build a portfolio that impresses employers and lands you your dream developer role.</p>
        </div>
      </div>
      <div class="flex gap-4">
        <div class="text-3xl">🎓</div>
        <div>
          <h3 class="font-bold text-white mb-2">100% Free Learning</h3>
          <p class="text-white/70 text-sm">Access {totalCoupons}+ premium programming courses completely free. Lifetime access + certificates. Save $5,000+ in learning investments.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="mb-16">
    <div class="mb-8">
      <h2 class="text-3xl md:text-4xl font-black text-white mb-3">
        {totalCoupons}+ Free Python & Programming Courses
      </h2>
      <p class="text-white/60 text-base max-w-2xl">
        Verified 100% off Udemy programming coupons. Each coupon works instantly with one click. No code entry needed. Learn Python, JavaScript, web development, backend systems, and more.
      </p>
    </div>

    <section class="deals-grid mb-12" id="coupons-grid">
      {programmingCoupons.map((c) => (
        <div data-coupon-slug={c.slug}>
          <DealCard
            title={c.title}
            description={c.description}
            image={c.image}
            price={c.price}
            originalPrice={c.originalPrice}
            discount={c.discount}
            url={c.url}
            provider={c.provider}
            category={c.category}
            rating={c.rating}
            students={c.students}
            verified={c.verified}
            expiryDate={c.expiryDate}
            slug={c.slug}
          />
        </div>
      ))}
    </section>

    {totalPages > 1 && (
      <nav class="flex justify-center gap-2 mb-12 flex-wrap" id="pagination-controls"></nav>
    )}
  </section>

  <section class="mb-16 border-t border-white/10 pt-12">
    <h2 class="text-3xl font-black text-white mb-8">
      Programming Learning Path - Free Udemy Courses
    </h2>
    <div class="space-y-4">
      <div class="flex gap-4 p-6 rounded-xl bg-gradient-to-br from-blue-500/10 to-cyan-500/10 border border-blue-500/20">
        <div class="w-10 h-10 rounded-full bg-gradient-to-br from-blue-500 to-cyan-500 flex items-center justify-center font-bold text-white flex-shrink-0">1</div>
        <div>
          <h3 class="font-bold text-white mb-2">Python Fundamentals</h3>
          <p class="text-white/70 text-sm">Master Python syntax, data structures, and core programming concepts. Build a solid foundation for advanced development.</p>
        </div>
      </div>
      <div class="flex gap-4 p-6 rounded-xl bg-gradient-to-br from-cyan-500/10 to-teal-500/10 border border-cyan-500/20">
        <div class="w-10 h-10 rounded-full bg-gradient-to-br from-cyan-500 to-teal-500 flex items-center justify-center font-bold text-white flex-shrink-0">2</div>
        <div>
          <h3 class="font-bold text-white mb-2">Web Development</h3>
          <p class="text-white/70 text-sm">Learn Django, Flask, HTML, CSS, JavaScript. Build dynamic web applications and RESTful APIs.</p>
        </div>
      </div>
      <div class="flex gap-4 p-6 rounded-xl bg-gradient-to-br from-teal-500/10 to-blue-500/10 border border-teal-500/20">
        <div class="w-10 h-10 rounded-full bg-gradient-to-br from-teal-500 to-blue-500 flex items-center justify-center font-bold text-white flex-shrink-0">3</div>
        <div>
          <h3 class="font-bold text-white mb-2">Databases & Backend</h3>
          <p class="text-white/70 text-sm">Master SQL, NoSQL, PostgreSQL, MongoDB. Design scalable backends and optimize database performance.</p>
        </div>
      </div>
      <div class="flex gap-4 p-6 rounded-xl bg-gradient-to-br from-blue-500/10 to-purple-500/10 border border-blue-500/20">
        <div class="w-10 h-10 rounded-full bg-gradient-to-br from-blue-500 to-purple-500 flex items-center justify-center font-bold text-white flex-shrink-0">4</div>
        <div>
          <h3 class="font-bold text-white mb-2">Advanced & Career Growth</h3>
          <p class="text-white/70 text-sm">Build full-stack applications, deploy to production, and land senior developer roles with $150K+ salary potential.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="mb-16 bg-gradient-to-r from-blue-500/20 to-cyan-500/20 rounded-2xl p-8 border border-blue-500/30 backdrop-blur-sm">
    <h2 class="text-3xl font-black text-white mb-4">
      Start Your Free Programming Career Today
    </h2>
    <p class="text-white/80 mb-6">
      Learn Python, web development, and backend programming with {totalCoupons}+ verified 100% off Udemy coupons. No credit card. Lifetime access. Professional certificates. Join thousands mastering programming for free.
    </p>
    <a href="#coupons-grid" class="inline-block px-8 py-4 rounded-lg bg-gradient-to-r from-blue-500 to-cyan-500 hover:shadow-lg hover:shadow-blue-500/50 text-white font-bold transition">
      Browse {totalCoupons}+ Free Programming Courses Now →
    </a>
  </section>

  <!-- Related Categories Navigation - Internal Linking -->
  <section class="mb-16 border-t border-white/10 pt-12">
    <h2 class="text-3xl font-black text-white mb-8">Explore More Free Udemy Coupon Categories</h2>
    <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-4">
      <a href="/coupon/ai-courses/" class="group relative overflow-hidden rounded-xl p-6 bg-gradient-to-br from-violet-500/20 to-indigo-500/20 border border-violet-500/30 hover:border-violet-500/50 transition-all hover:shadow-lg hover:shadow-violet-500/20">
        <div class="relative z-10">
          <h3 class="text-lg font-black text-white mb-2 group-hover:text-indigo-300 transition">AI & Machine Learning</h3>
          <p class="text-sm text-white/70 mb-4">Free AI courses with 100% off coupon codes. Learn ChatGPT, LLM, deep learning, NLP.</p>
          <span class="inline-block text-violet-400 font-bold group-hover:translate-x-2 transition">Browse Free Courses →</span>
        </div>
      </a>
      <a href="/coupon/data-science/" class="group relative overflow-hidden rounded-xl p-6 bg-gradient-to-br from-green-500/20 to-emerald-500/20 border border-green-500/30 hover:border-green-500/50 transition-all hover:shadow-lg hover:shadow-green-500/20">
        <div class="relative z-10">
          <h3 class="text-lg font-black text-white mb-2 group-hover:text-emerald-300 transition">Data Science & ML</h3>
          <p class="text-sm text-white/70 mb-4">Free data science courses with verified coupon codes. Learn Python, pandas, statistics.</p>
          <span class="inline-block text-green-400 font-bold group-hover:translate-x-2 transition">Browse Free Courses →</span>
        </div>
      </a>
      <a href="/coupon/business-courses/" class="group relative overflow-hidden rounded-xl p-6 bg-gradient-to-br from-yellow-500/20 to-amber-500/20 border border-yellow-500/30 hover:border-yellow-500/50 transition-all hover:shadow-lg hover:shadow-yellow-500/20">
        <div class="relative z-10">
          <h3 class="text-lg font-black text-white mb-2 group-hover:text-amber-300 transition">Business & Leadership</h3>
          <p class="text-sm text-white/70 mb-4">Free business courses with 100% off promo codes. Learn marketing, finance, sales.</p>
          <span class="inline-block text-yellow-400 font-bold group-hover:translate-x-2 transition">Browse Free Courses →</span>
        </div>
      </a>
      <a href="/coupon/design-courses/" class="group relative overflow-hidden rounded-xl p-6 bg-gradient-to-br from-pink-500/20 to-rose-500/20 border border-pink-500/30 hover:border-pink-500/50 transition-all hover:shadow-lg hover:shadow-pink-500/20">
        <div class="relative z-10">
          <h3 class="text-lg font-black text-white mb-2 group-hover:text-rose-300 transition">Design & UI/UX</h3>
          <p class="text-sm text-white/70 mb-4">Free design courses with verified coupon deals. Learn Figma, Photoshop, UI/UX.</p>
          <span class="inline-block text-pink-400 font-bold group-hover:translate-x-2 transition">Browse Free Courses →</span>
        </div>
      </a>
    </div>
    <div class="mt-8 text-center">
      <a href="/coupon/" class="inline-block px-8 py-4 rounded-lg bg-gradient-to-r from-emerald-500 to-teal-500 hover:shadow-lg hover:shadow-emerald-500/50 text-white font-bold transition">
        View All 100% Off Udemy Coupons & Free Courses →
      </a>
    </div>
  </section>
</Layout>
