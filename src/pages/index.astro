---
import Layout from "../layouts/Layout.astro";
import LedgerRow from "../components/LedgerRow.astro";
import { readDeals } from "../lib/store";
import { toISODateWIB } from "../lib/date";
import { normName, slugifyCategory } from "../lib/utils";
import { parseInstructors, createInstructorSlug } from "../lib/instructors";
import { getInstructorImage } from "../lib/instructorProfiles";

const allDeals = await readDeals();
const now = new Date();
const currentYear = now.getFullYear();
const currentMonth = now.toLocaleDateString("en-US", { month: "long" });
const activeDeals = allDeals.filter((d) => !d.expiresAt || new Date(d.expiresAt) > now);
const sortedDeals = [...activeDeals].sort((a, b) => new Date(b.updatedAt ?? b.createdAt ?? 0).getTime() - new Date(a.updatedAt ?? a.createdAt ?? 0).getTime());

const last24h = allDeals.filter((d) => { const t = new Date(d.updatedAt ?? d.createdAt ?? 0).getTime(); return now.getTime() - t < 24 * 3600000; }).length;

function catBlurb(name: string) {
  const c = name.toLowerCase();
  if (c.includes("it") || c.includes("software") || c.includes("inform")) return "Networks, security, certs and the tooling teams actually run on.";
  if (c === "development" || c.includes("program") || c.includes("desarrollo")) return "Code, apps and shipping software — from first script to deploy.";
  if (c.includes("business") || c.includes("negocio")) return "Lead teams, read numbers and grow something that lasts.";
  if (c.includes("design") || c.includes("dis")) return "Interfaces, visuals and the craft behind good-looking products.";
  if (c.includes("market")) return "Traffic, funnels and campaigns that pay for themselves.";
  if (c.includes("data") || c.includes("office") || c.includes("productiv") || c.includes("excel")) return "Sheets, dashboards and data skills used in real jobs.";
  if (c.includes("teach") || c.includes("academ")) return "Languages, exams and classroom-tested study paths.";
  if (c.includes("finance") || c.includes("account")) return "Money skills: accounting basics through investing logic.";
  if (c.includes("photo") || c.includes("video") || c.includes("music")) return "Creative production — shoot, edit and publish with confidence.";
  if (c.includes("personal")) return "Habits, focus and soft skills that compound over a career.";
  return "Hand-picked courses in this field at a fraction of list price.";
}

const catCounts = allDeals.reduce<Record<string, number>>((acc, d) => { const c = normName(d.category); if (c) acc[c] = (acc[c] || 0) + 1; return acc; }, {});
const topCats = Object.entries(catCounts).sort((a, b) => b[1] - a[1]).slice(0, 8).map(([name, count]) => ({ name, count, slug: slugifyCategory(name), blurb: catBlurb(name) }));
const topCats5 = topCats.slice(0, 5);

const poolPrimary = new Set<string>();
sortedDeals.slice(0, 8).forEach((d) => poolPrimary.add(String(d.slug)));
const poolSet = new Set<string>(poolPrimary);
for (const c of topCats5) {
  sortedDeals.filter((d) => normName(d.category) === c.name).slice(0, 6).forEach((d) => poolSet.add(String(d.slug)));
}
const poolDeals = sortedDeals.filter((d) => poolSet.has(String(d.slug)));
const latestSix = sortedDeals.slice(0, 6);

// Trending skills from real catalog depth (subcategory frequency)
const subcatCounts = new Map<string, number>();
for (const d of allDeals) {
  const s = (d.subcategory || "").trim();
  if (!s) continue;
  subcatCounts.set(s, (subcatCounts.get(s) ?? 0) + 1);
}
const trendingSkills = [...subcatCounts.entries()].sort((a, b) => b[1] - a[1]).slice(0, 12).map(([name, count]) => ({ name, count }));

// Learner favorites: highest rated with real enrollment weight
const popularPicks = [...activeDeals].filter((d) => (d.rating ?? 0) >= 4.5).sort((a, b) => ((b.rating ?? 0) - (a.rating ?? 0)) || ((b.students ?? 0) - (a.students ?? 0))).slice(0, 6);

// Deepest cuts: top-rated courses at 90%+ off — anti-duplikat rel latest.
// Selalu 6 kartu: prioritas 90%+, backfill dari favorit 4.5★ lalu diskon terbesar.
const latestSlugs = new Set(latestSix.map((d) => String(d.slug)));
const deepCuts: typeof popularPicks = [];
const pickedSlugs = new Set<string>();
for (const d of popularPicks) {
  if (deepCuts.length >= 6) break;
  const s = String(d.slug);
  if (pctOf(d) >= 90 && !latestSlugs.has(s) && !pickedSlugs.has(s)) { deepCuts.push(d); pickedSlugs.add(s); }
}
for (const d of popularPicks) {
  if (deepCuts.length >= 6) break;
  const s = String(d.slug);
  if (!latestSlugs.has(s) && !pickedSlugs.has(s)) { deepCuts.push(d); pickedSlugs.add(s); }
}
if (deepCuts.length < 6) {
  const byPct = [...sortedDeals].sort((a, b) => pctOf(b) - pctOf(a));
  for (const d of byPct) {
    if (deepCuts.length >= 6) break;
    const s = String(d.slug);
    if (!latestSlugs.has(s) && !pickedSlugs.has(s)) { deepCuts.push(d); pickedSlugs.add(s); }
  }
}
const deepCutsCount = sortedDeals.filter((d) => pctOf(d) >= 90).length;

// Instructors with the deepest bench in this catalog
const instructorCounts = new Map<string, { name: string; count: number; slug: string }>();
for (const d of allDeals) {
  if (!d.instructor) continue;
  for (const n of parseInstructors(d.instructor)) {
    const slug = createInstructorSlug(n);
    if (!slug) continue;
    const e = instructorCounts.get(slug);
    if (e) e.count++;
    else instructorCounts.set(slug, { name: n, count: 1, slug });
  }
}
const topInstructors = [...instructorCounts.values()].sort((a, b) => b.count - a.count).slice(0, 8);
const instructorImages = new Map<string, string>();
for (const ins of topInstructors) {
  const img = await getInstructorImage(ins.slug);
  if (img) instructorImages.set(ins.slug, img);
}

const discountPcts = allDeals
  .filter((d): d is typeof d & { price: number; originalPrice: number } => typeof d.price === "number" && typeof d.originalPrice === "number" && d.originalPrice > d.price && d.price > 0)
  .map((d) => Math.round(100 - (d.price / d.originalPrice) * 100));
const maxDiscountPct = discountPcts.length ? Math.max(...discountPcts) : 95;
const avgDiscount = discountPcts.length ? Math.round(discountPcts.reduce((a, b) => a + b, 0) / discountPcts.length) : 0;
const liveCount = activeDeals.length;
const totalCount = allDeals.length;
const uniqueInstructors = new Set(allDeals.map((d) => (d.instructor || "").trim()).filter(Boolean)).size;

const spotlight = [...sortedDeals].sort((a, b) => (b.rating ?? 0) - (a.rating ?? 0))[0];
function clean(s: string) { return String(s || "").replace(/&amp;/g, "&").replace(/&quot;/g, '"').replace(/&apos;|&#8217;/g, "'").replace(/\s{2,}/g, " ").trim(); }
function pctOf(d: any) {
  if (typeof d?.price === "number" && typeof d?.originalPrice === "number" && d.originalPrice > d.price && d.price > 0) {
    return Math.round(100 - (d.price / d.originalPrice) * 100);
  }
  return 0;
}

const faqs = [
  { q: "Is CoursesWyn free to use?", a: "Yes — browsing CoursesWyn costs nothing and needs no account. You only need a free Udemy account when you decide to enroll, where discounts of up to " + maxDiscountPct + "% are applied at checkout." },
  { q: "How fresh are the coupons?", a: "Our pipeline re-checks listings around the clock and pulls dead codes fast. The freshest batch is labelled by recency, and the count from the last 24 hours is shown right on this page." },
  { q: "What if a code stops working?", a: "Instructor codes carry redemption caps and expiry dates. If one runs out, the price snaps back to list — pick a sibling deal in the same category or come back tomorrow for the next batch." },
  { q: "Do I still get a certificate?", a: "Yes. A discounted enrollment carries the same certificate, lifetime access, updates and instructor Q&A as a full-price seat." },
  { q: "Can I learn on my phone?", a: "Yes. Every claimed course works in the Udemy iOS and Android apps, including offline downloads for commutes and flights." },
  { q: "How do you choose what to list?", a: "We rank candidates by rating depth, instructor track record and curriculum freshness before they go live — a steep cut alone is never enough to get listed." }
];

const pageTitle = `CoursesWyn — Verified Udemy Coupons Up to ${maxDiscountPct}% Off (Checked Daily)`;
const pageDescription = `Start at CoursesWyn: hand-picked verified Udemy coupons up to ${maxDiscountPct}% off for ${currentMonth} ${currentYear}. Browse today's top markdowns, top-rated picks and categories — every code re-checked daily.`;

const jsonLd = {
  "@context": "https://schema.org", "@graph": [
    { "@type": "WebSite", "@id": "https://courseswyn.com/#website", "url": "https://courseswyn.com", "name": "CoursesWyn", "description": pageDescription, "inLanguage": "en-US", "publisher": { "@id": "https://courseswyn.com/#organization" }, "potentialAction": { "@type": "SearchAction", "target": { "@type": "EntryPoint", "urlTemplate": "https://courseswyn.com/search?q={search_term_string}" }, "query-input": "required name=search_term_string" } },
    { "@type": "Organization", "@id": "https://courseswyn.com/#organization", "name": "CoursesWyn", "url": "https://courseswyn.com", "logo": { "@type": "ImageObject", "url": "https://courseswyn.com/logo.svg", "width": 200, "height": 60 }, "sameAs": ["https://x.com/CoursesWyn", "https://www.facebook.com/CoursesWyn", "https://medium.com/@coursewyn"], "description": `Verified Udemy coupons with savings up to ${maxDiscountPct}%, refreshed daily.` },
    { "@type": "WebPage", "@id": "https://courseswyn.com/#webpage", "url": "https://courseswyn.com", "name": pageTitle, "description": pageDescription, "author": { "@type": "Person", "name": "Andrew Derek", "url": "https://courseswyn.com/about" }, "isPartOf": { "@id": "https://courseswyn.com/#website" }, "dateModified": toISODateWIB(new Date()), "datePublished": toISODateWIB(new Date()), "inLanguage": "en-US" },
    { "@type": "BreadcrumbList", "itemListElement": [{ "@type": "ListItem", "position": 1, "name": "Home", "item": "https://courseswyn.com" }] },
    { "@type": "ItemList", "numberOfItems": latestSix.length, "itemListElement": latestSix.slice(0, 10).map((d, i) => ({ "@type": "ListItem", "position": i + 1, "item": { "@type": "Course", "name": clean(d.title), "url": "https://courseswyn.com/coupon/" + d.slug, "description": clean(d.description), "provider": { "@type": "Organization", "name": d.provider || "Udemy", "sameAs": "https://www.udemy.com" }, "offers": { "@type": "Offer", "price": d.price && d.price > 0 ? d.price : (d.originalPrice || 0), "priceCurrency": "USD", "availability": "https://schema.org/InStock", "url": "https://courseswyn.com/coupon/" + d.slug } } })) },
    { "@type": "ItemList", "name": "Deepest verified Udemy discounts", "numberOfItems": deepCuts.length, "itemListElement": deepCuts.map((d, i) => ({ "@type": "ListItem", "position": i + 1, "item": { "@type": "Course", "name": clean(d.title), "url": "https://courseswyn.com/coupon/" + d.slug, "provider": { "@type": "Organization", "name": "Udemy", "sameAs": "https://www.udemy.com" }, "offers": { "@type": "Offer", "price": d.price && d.price > 0 ? d.price : (d.originalPrice || 0), "priceCurrency": "USD", "availability": "https://schema.org/InStock", "url": "https://courseswyn.com/coupon/" + d.slug } } })) },
    { "@type": "FAQPage", "mainEntity": faqs.map(f => ({ "@type": "Question", "name": f.q, "acceptedAnswer": { "@type": "Answer", "text": f.a } })) }
  ]
};

const heroChips = [
  { name: "Python", q: "Python" },
  { name: "Web Dev", q: "Web development" },
  { name: "AWS", q: "AWS" },
  { name: "Excel", q: "Excel" },
  { name: "Design", q: "Design" },
  { name: "Business", q: "Business" },
];
const typewriterWords = [
  "Search discounted Udemy courses...",
  `${last24h} fresh finds in the last 24 hours`,
  'Try "Python", "Excel" or "AWS"',
  `Over ${totalCount.toLocaleString()} coupons tracked`,
];
const typewriterJson = JSON.stringify(typewriterWords);

const whyItems = [
  { t: "Keep more of your budget", d: "List prices on Udemy climb past $100 fast. Our tracked codes routinely cut 80–95%, so a month of serious upskilling can cost less than lunch." },
  { t: "Browse first, sign up never", d: "No CoursesWyn account, no newsletter hostage-taking. Open the directory, filter by what you want to learn, and only sign in on Udemy when you enroll." },
  { t: "Skills employers pay for", d: "We overweight IT, development, data, business and design — the categories hiring managers actually screen for — instead of filler catalog padding." },
];
const trustItems = [
  { t: "Re-checked at checkout", d: "Listings pass an automated pass plus a final check when you click through, so the price you see is the price Udemy shows." },
  { t: "Quality gate before price", d: "Ratings, review depth, instructor history and syllabus freshness decide what gets published — not how big the percentage looks." },
  { t: "Dead codes get buried", d: "Expired or capped-out coupons are pruned on a loop instead of lingering for months to waste your evening." },
  { t: "Same seat, smaller bill", d: "Discounted seats include the identical certificate, lifetime access, downloads and Q&A as full-price enrollments." },
  { t: "No CoursesWyn paywall", d: "Every filter, every category, every code is open. There is no premium tier holding the good stuff back." },
  { t: "Instructor-sourced supply", d: "A large share of codes comes straight from creators promoting launches and seasonal pushes — legitimate supply, not scraped guesses." },
];
---

<Layout title={pageTitle} description={pageDescription} canonical="https://courseswyn.com">
  <meta property="og:type" content="website" slot="head" />
  <meta property="og:url" content="https://courseswyn.com" slot="head" />
  <meta property="og:title" content={pageTitle} slot="head" />
  <meta property="og:description" content={pageDescription} slot="head" />
  <meta property="og:image" content="/assets/images/og-default.svg" slot="head" />
  <meta property="og:site_name" content="CoursesWyn" slot="head" />
  <meta property="og:locale" content="en_US" slot="head" />
  <meta name="twitter:card" content="summary_large_image" slot="head" />
  <meta name="twitter:title" content={pageTitle} slot="head" />
  <meta name="twitter:description" content={pageDescription} slot="head" />
  <meta name="twitter:site" content="@CoursesWyn" slot="head" />
  <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />

<style>
  .section-wrapper { width: 100%; padding: 3rem 1rem; }
  .container { max-width: 1200px; margin: 0 auto; width: 100%; }
  .prose-section h2 { font-size: clamp(1.4rem, 2.5vw, 1.8rem); font-weight: 700; color: var(--text); margin-bottom: 1rem; }
  .prose-section h3 { font-size: 1.15rem; font-weight: 600; color: var(--text); margin: 1.5rem 0 0.5rem; }
  .prose-section p { color: var(--muted); line-height: 1.75; font-size: 1rem; margin-bottom: 1rem; }
  .prose-section a { color: var(--brand); text-decoration: underline; text-underline-offset: 3px; }
  .community-btn:hover { border-color: var(--brand) !important; background: var(--brand) !important; color: #FFFFFF !important; transform: translateY(-2px); }
  @media (max-width: 768px) { h1 { font-size: 2.2rem !important; } .section-wrapper { padding: 2.25rem 1rem; } .stat-grid { grid-template-columns: repeat(2, 1fr) !important; } .lg-feature-grid { grid-template-columns: 1fr !important; } .lg-guide-grid { grid-template-columns: 1fr !important; gap: 1.5rem !important; } }
  html { scroll-behavior: smooth; }

  /* ===== VERIFIED LEDGER — ciri khas CoursesWyn ===== */
  .lg-eyebrow { font-size: 0.72rem; font-weight: 800; text-transform: uppercase; letter-spacing: 0.14em; color: var(--brand); display: flex; align-items: center; gap: 10px; margin-bottom: 0.6rem; }
  .lg-eyebrow::after { content: ''; flex: 1; border-top: 1px solid var(--ledger); }
  .lg-h2 { font-size: clamp(1.5rem, 3vw, 2.1rem); font-weight: 700; color: var(--text); margin: 0 0 0.5rem; letter-spacing: -0.01em; }
  .lg-sub { color: var(--muted); font-size: 0.92rem; line-height: 1.6; margin: 0 0 1.5rem; max-width: 620px; }
  .lg-sheet { background: var(--card); border: 1px solid var(--border); border-radius: 18px; padding: clamp(1.75rem, 4vw, 3rem); position: relative; box-shadow: 0 20px 44px -28px rgba(20,32,27,0.25); }
  .lg-folio { display: flex; align-items: center; justify-content: space-between; gap: 12px; flex-wrap: wrap; font-size: 0.7rem; font-weight: 800; text-transform: uppercase; letter-spacing: 0.12em; color: var(--muted); padding-bottom: 1rem; margin-bottom: 1.5rem; border-bottom: 3px double var(--ledger); }
  .lg-folio-live { display: inline-flex; align-items: center; gap: 7px; color: var(--brand); }
  .lg-blip { width: 7px; height: 7px; border-radius: 50%; background: var(--brand); animation: lgBlip 1.6s infinite; }
  @keyframes lgBlip { 0%, 100% { opacity: 1; } 50% { opacity: 0.3; } }
  .lg-search { display: flex; align-items: center; gap: 10px; background: var(--bg); border: 1px solid var(--border); border-left: 4px solid var(--brand); border-radius: 12px; padding: 0 8px 0 18px; height: 58px; max-width: 640px; margin: 0 auto 10px; transition: border-color 0.2s, box-shadow 0.2s; }
  .lg-search:focus-within { border-color: var(--brand); box-shadow: 0 0 0 3px var(--brand-soft); }
  .lg-search svg { width: 20px; height: 20px; color: var(--muted); flex-shrink: 0; }
  .lg-search input { flex: 1; background: transparent; border: none; outline: none; color: var(--text); font-size: 0.95rem; font-family: inherit; min-width: 0; }
  .lg-search kbd { font-family: var(--font-mono); color: var(--muted); font-size: 0.7rem; border: 1px solid var(--border); border-radius: 6px; padding: 3px 8px; white-space: nowrap; background: var(--card); }
  .lg-stats { display: grid; grid-template-columns: repeat(4, 1fr); margin-top: 26px; background: var(--bg); border: 1px solid var(--border); border-radius: 14px; overflow: hidden; }
  .lg-stat { padding: 16px 12px; text-align: center; border-left: 1px dashed var(--ledger); }
  .lg-stat:first-child { border-left: none; }
  .lg-stat-num { font-family: var(--font-mono); font-weight: 700; font-size: 1.4rem; color: var(--text); font-variant-numeric: tabular-nums; }
  .lg-stat-lab { font-size: 0.68rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.08em; color: var(--muted); margin-top: 4px; }
  @media (max-width: 640px) { .lg-stat { flex: 1 1 40%; border-left: none; border-top: 1px dashed var(--ledger); } .lg-stats { display: flex; flex-wrap: wrap; } .lg-stat:nth-child(-n+2) { border-top: none; } .lg-stat:nth-child(even) { border-left: 1px dashed var(--ledger); } }
  .lg-chip { display: inline-flex; align-items: center; gap: 7px; padding: 8px 14px; border-radius: 9px; background: var(--card); border: 1px solid var(--border); color: var(--text); text-decoration: none; font-size: 0.82rem; font-weight: 600; transition: all 0.18s; }
  .lg-chip:hover { border-color: var(--brand); color: var(--brand); transform: translateY(-2px); }
  .lg-chip b { font-family: var(--font-mono); font-weight: 600; font-size: 0.72rem; color: var(--muted); }
  .lg-chip:hover b { color: var(--brand); }
  .lg-link { display: inline-flex; align-items: center; gap: 7px; font-family: var(--font-mono); font-size: 0.8rem; font-weight: 600; color: var(--brand); text-decoration: none; border-bottom: 2px solid var(--brand-soft); padding-bottom: 2px; transition: border-color 0.2s; }
  .lg-link:hover { border-color: var(--brand); }
  .lg-index { counter-reset: lg; }
  .lg-row { display: flex; align-items: center; gap: 16px; padding: 15px 6px; border-top: 1px solid var(--ledger); text-decoration: none; color: inherit; transition: background 0.15s, padding-left 0.2s; }
  .lg-row:first-child { border-top: none; }
  a.lg-row:hover { background: var(--brand-soft); padding-left: 14px; }
  .lg-num { font-family: var(--font-mono); font-weight: 600; font-size: 0.8rem; color: var(--muted); min-width: 34px; }
  a.lg-row:hover .lg-num { color: var(--brand); }
  .lg-row-main { flex: 1; min-width: 0; }
  .lg-row-title { font-family: var(--font-display); font-weight: 600; font-size: 1rem; color: var(--text); line-height: 1.35; }
  .lg-row-sub { font-size: 0.82rem; color: var(--muted); margin-top: 3px; line-height: 1.5; }
  .lg-row-meta { font-family: var(--font-mono); font-size: 0.75rem; font-weight: 600; color: var(--brand); white-space: nowrap; text-align: right; }
  .lg-row-meta small { display: block; font-weight: 500; color: var(--muted); font-size: 0.65rem; margin-top: 2px; }
  .lg-row-arrow { color: var(--muted); font-weight: 700; transition: transform 0.2s, color 0.2s; }
  a.lg-row:hover .lg-row-arrow { transform: translateX(4px); color: var(--brand); }
  .lg-avatar { width: 46px; height: 46px; border-radius: 50%; object-fit: cover; flex-shrink: 0; border: 2px solid var(--ledger); }
  .lg-avatar-fallback { width: 46px; height: 46px; border-radius: 50%; background: var(--brand-soft); border: 2px solid var(--ledger); display: flex; align-items: center; justify-content: center; color: var(--brand); flex-shrink: 0; font-weight: 800; font-size: 1.05rem; font-family: var(--font-display); }
  .lg-feature-grid { display: grid; grid-template-columns: 1fr 380px; gap: 2.5rem; align-items: center; }
  .lg-feature-img { border-radius: 14px; overflow: hidden; border: 1px solid var(--border); }
  .lg-feature-img img { width: 100%; height: 290px; object-fit: cover; display: block; }
  .lg-price-row { display: flex; align-items: baseline; gap: 12px; flex-wrap: wrap; margin: 1.25rem 0; }
  .lg-price { font-family: var(--font-mono); font-weight: 700; font-size: 2rem; color: var(--text); }
  .lg-was { font-family: var(--font-mono); font-size: 1.05rem; color: var(--muted); text-decoration: line-through; }
  .lg-save { font-family: var(--font-mono); font-size: 0.75rem; font-weight: 700; color: #fff; background: #CF6F59; padding: 3px 10px; border-radius: 6px; }
  .lg-perf { position: relative; border-top: 2px dashed var(--ledger); margin: 0 8px; }
  .lg-perf::before, .lg-perf::after { content: ''; position: absolute; top: -11px; width: 20px; height: 20px; background: var(--bg); border: 1px solid var(--border); border-radius: 50%; }
  .lg-perf::before { left: -19px; }
  .lg-perf::after { right: -19px; }
  .lg-check { display: grid; grid-template-columns: 1fr 1fr; gap: 0 2.5rem; }
  @media (max-width: 760px) { .lg-check { grid-template-columns: 1fr; } }
  .lg-check-item { display: flex; gap: 12px; padding: 13px 4px; border-top: 1px solid var(--ledger); font-size: 0.88rem; line-height: 1.6; color: var(--muted); }
  .lg-check-item:first-child, .lg-check-item:nth-child(2) { border-top: none; }
  @media (max-width: 760px) { .lg-check-item:nth-child(2) { border-top: 1px solid var(--ledger); } }
  .lg-check-item b { color: var(--text); }
  .lg-check-mark { font-family: var(--font-mono); font-weight: 700; color: var(--brand); flex-shrink: 0; }
  .lg-faq { border-top: 1px solid var(--ledger); }
  .lg-faq-item { display: grid; grid-template-columns: 52px 1fr; gap: 14px; padding: 18px 4px; border-bottom: 1px solid var(--ledger); }
  .lg-faq-no { font-family: var(--font-mono); font-weight: 600; font-size: 0.78rem; color: var(--brand); padding-top: 3px; }
  .lg-faq-q { font-weight: 700; font-size: 0.98rem; color: var(--text); margin: 0 0 6px; line-height: 1.45; }
  .lg-faq-a { font-size: 0.87rem; color: var(--muted); line-height: 1.65; margin: 0; }
  .lg-guide-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 3.5rem; align-items: start; }
  .lg-cta-row { display: flex; gap: 12px; justify-content: center; align-items: center; flex-wrap: wrap; margin-top: 22px; }
  @media (prefers-reduced-motion: reduce) { .lg-blip { animation: none; } }
</style>

  <!-- 1. HERO — LEDGER SHEET -->
  <section class="section-wrapper" aria-label="Verified Udemy coupon ledger" style="padding-top:2rem;">
    <div class="container">
      <div class="lg-sheet">
        <div class="lg-folio"><span>Coupon ledger — {currentMonth} {currentYear}</span><span class="lg-folio-live"><span class="lg-blip"></span>№ {totalCount.toLocaleString()} tracked</span></div>
        <div style={{ textAlign: "center", maxWidth: "780px", margin: "0 auto" }}>
          <h1 style={{ margin: "0 0 12px" }}>
            <span style="display:block;font-size:clamp(1.9rem,3.6vw,2.7rem);">Verified Udemy Coupons</span>
            <span style="display:block;font-size:clamp(2.6rem,6vw,4.4rem);color:var(--brand);">{currentMonth} {currentYear}</span>
            <span style="display:block;font-size:clamp(1.9rem,3.6vw,2.7rem);">Hand-Picked Daily by CoursesWyn</span>
          </h1>
          <p style={{ color: "var(--muted)", fontSize: "1.02rem", lineHeight: 1.7, margin: "0 auto 22px", maxWidth: "640px" }}>Today's top verified Udemy coupons and discount codes — up to {maxDiscountPct}% off development, IT, business, design and data. Open the <a href="/udemy-coupon-code" style={{ color: "var(--brand)", fontWeight: 600 }}>full coupon catalog</a> or claim a pick below.</p>
          <form action="/search" method="GET" role="search" aria-label="Search discounted Udemy courses">
            <div class="lg-search">
              <svg fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" viewBox="0 0 24 24"><circle cx="11" cy="11" r="8" /><path d="m21 21-4.35-4.35" /></svg>
              <input type="search" name="q" id="hpSearchInput" data-typewriter={typewriterJson} placeholder="Try Python, Excel, AWS..." autocomplete="off" aria-label="Search discounted courses" />
              <button type="submit" class="btn btn-primary" style={{ borderRadius: "9px", padding: "0.65rem 1.4rem", fontSize: "0.9rem" }}>Find deals</button>
            </div>
          </form>
          <p style={{ fontSize: "0.78rem", color: "var(--muted)", margin: "10px 0 0", letterSpacing: "0.02em", fontWeight: 600 }}>{totalCount.toLocaleString()} coupons · up to {maxDiscountPct}% off · {uniqueInstructors.toLocaleString()} instructors tracked</p>
          <div class="lg-cta-row">
            <a href="/udemy-coupon-code" class="btn btn-primary btn-lg" style={{ borderRadius: "12px", padding: "0.95rem 2.2rem", fontSize: "1rem" }}>Explore today's drops</a>
            <a href="/how-to-redeem-coupon" class="btn btn-ghost btn-lg" style={{ borderRadius: "12px", padding: "0.95rem 2.2rem", color: "var(--brand)", borderColor: "var(--brand)" }}>How claiming works</a>
          </div>
          <div style={{ display: "flex", flexWrap: "wrap", gap: "9px", justifyContent: "center", marginTop: "20px" }}>
            {heroChips.map((c) => (<a href={"/search?q=" + encodeURIComponent(c.q)} class="lg-chip">{c.name}</a>))}
          </div>
        </div>
        <div class="lg-stats">
          <div class="lg-stat"><div class="lg-stat-num">{totalCount.toLocaleString()}</div><div class="lg-stat-lab">Total coupons</div></div>
          <div class="lg-stat"><div class="lg-stat-num">{last24h.toLocaleString()}</div><div class="lg-stat-lab">Fresh in 24h</div></div>
          <div class="lg-stat"><div class="lg-stat-num">{uniqueInstructors.toLocaleString()}</div><div class="lg-stat-lab">Instructors covered</div></div>
          <div class="lg-stat"><div class="lg-stat-num">−{maxDiscountPct}%</div><div class="lg-stat-lab">Biggest cut</div></div>
        </div>
      </div>
    </div>
  </section>

  <!-- TRENDING UDEMY TOPICS -->
  <section class="section-wrapper" aria-label="Trending Udemy course topics" style="padding-top:0.5rem;">
    <div class="container">
      <div class="lg-eyebrow">Trending now — by live count</div>
      <h2 class="lg-h2">Trending <span style={{ color: "var(--brand)" }}>Udemy course topics</span></h2>
      <p class="lg-sub">The most stocked topics in our verified Udemy coupon catalog — ranked by live coupon count.</p>
      <div style={{ display: "flex", flexWrap: "wrap", gap: "9px" }}>
        {trendingSkills.map((t) => (<a href={"/search?q=" + encodeURIComponent(t.name)} class="lg-chip" title={"Udemy coupons for " + t.name}>{t.name} <b>×{t.count}</b></a>))}
      </div>
    </div>
  </section>

  <!-- 2. LATEST DEALS -->
  <section class="section-wrapper" aria-label="Latest Udemy coupons and discounts" style="padding-top:0.5rem;">
    <div class="container">
      <div class="lg-eyebrow">Fresh drops — {currentMonth}</div>
      <div style={{ display: "flex", alignItems: "flex-end", justifyContent: "space-between", gap: "12px", flexWrap: "wrap", marginBottom: "1.25rem" }}>
        <div>
          <h2 class="lg-h2" style={{ marginBottom: "0.25rem" }}>Latest <span style={{ color: "var(--brand)" }}>Udemy coupons</span></h2>
          <p class="lg-sub" style={{ margin: 0 }}>Newly verified Udemy coupon codes. Open any ticket to claim the discount on Udemy.</p>
        </div>
        <a href="/udemy-coupon-code" class="lg-link">All {totalCount.toLocaleString()} coupons →</a>
      </div>
      <div class="grid">
        {latestSix.map((deal) => (<LedgerRow deal={deal} variant="grid" />))}
      </div>
    </div>
  </section>

  <!-- DEEPEST CUTS -->
  <section class="section-wrapper" aria-label="90 percent off top-rated Udemy courses" style="padding-top:0.5rem;">
    <div class="container">
      <div class="lg-eyebrow">Maximum savings — 4.5★ and up</div>
      <div style={{ display: "flex", alignItems: "flex-end", justifyContent: "space-between", gap: "12px", flexWrap: "wrap", marginBottom: "1.25rem" }}>
        <div>
          <h2 class="lg-h2" style={{ marginBottom: "0.25rem" }}>90% off <span style={{ color: "var(--brand)" }}>top-rated Udemy courses</span></h2>
          <p class="lg-sub" style={{ margin: 0 }}>{deepCutsCount.toLocaleString()} coupons cut 90% or more — below are the highest-rated ones.</p>
        </div>
        <a href="/udemy-coupon-code" class="lg-link">All {totalCount.toLocaleString()} Udemy coupons →</a>
      </div>
      <div class="grid">
        {deepCuts.map((deal) => (<LedgerRow deal={deal} variant="grid" />))}
      </div>
    </div>
  </section>

  <!-- 3. SPOTLIGHT -->
  {spotlight && (
    <section class="section-wrapper" aria-label="Featured Udemy discount of the day" style="padding-top:0.5rem;">
      <div class="container">
        <div class="lg-sheet">
          <div class="lg-folio"><span>Featured entry — best rating today</span><span class="lg-folio-live"><span class="lg-blip"></span>Checked {spotlight.updatedAt ? new Date(spotlight.updatedAt).toLocaleDateString("en-US", { month: "short", day: "numeric" }) : "recently"}</span></div>
          <div class="lg-feature-grid">
            <div>
              <h2 class="lg-h2" style={{ fontSize: "clamp(1.5rem, 2.8vw, 2rem)" }}><a href={"/coupon/" + spotlight.slug} style={{ color: "inherit", textDecoration: "none" }}>{clean(spotlight.title)}</a></h2>
              {spotlight.instructor && <p class="mono" style={{ fontSize: "0.75rem", color: "var(--muted)", margin: "6px 0 12px" }}>by {spotlight.instructor}{typeof spotlight.rating === "number" ? ` · ★ ${spotlight.rating.toFixed(1)}` : ""}</p>}
              <p style={{ color: "var(--muted)", fontSize: "0.98rem", lineHeight: 1.7, margin: "0 0 4px" }}>{clean(spotlight.description)?.slice(0, 220) || "A standout course from the current verification batch — strong ratings, active instructor, and a working discount."}</p>
              <div class="lg-price-row">
                <span class="lg-price">${Number(spotlight.price ?? 0).toFixed(2)}</span>
                {Number(spotlight.originalPrice ?? 0) > Number(spotlight.price ?? 0) && (<><span class="lg-was">${Number(spotlight.originalPrice).toFixed(2)}</span><span class="lg-save">−{pctOf(spotlight)}%</span></>)}
              </div>
              <a href={spotlight.url || ("/coupon/" + spotlight.slug)} target="_blank" rel="noopener noreferrer" class="btn btn-primary btn-lg" style={{ borderRadius: "12px", padding: "0.9rem 1.9rem" }}>Claim this code →</a>
            </div>
            <div class="lg-feature-img">
              {spotlight.image ? (<img src={spotlight.image} alt={clean(spotlight.title)} width={400} height={300} loading="lazy" decoding="async" />) : (<div style={{ width: "100%", height: "290px", display: "flex", alignItems: "center", justifyContent: "center", background: "var(--bg-secondary)", color: "var(--muted)" }}>CoursesWyn</div>)}
            </div>
          </div>
        </div>
      </div>
    </section>
  )}

  <!-- 4. WHY COURSESWYN -->
  <section class="section-wrapper" aria-label="Why use CoursesWyn for Udemy coupons" style="padding-top:0.5rem;">
    <div class="container">
      <div class="lg-eyebrow">Why CoursesWyn</div>
      <h2 class="lg-h2">Why use CoursesWyn for <span style={{ color: "var(--brand)" }}>Udemy coupons?</span></h2>
      <p class="lg-sub">Pay less for the same Udemy courses: verified coupon codes, no noise, certificate included.</p>
      <div class="lg-index">
        {whyItems.map((item, i) => (
          <div class="lg-row">
            <span class="lg-num">0{i + 1}</span>
            <div class="lg-row-main">
              <div class="lg-row-title">{item.t}</div>
              <div class="lg-row-sub">{item.d}</div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- 6. CATEGORIES -->
  <section class="section-wrapper" aria-label="Udemy coupons by category" style="padding-top:0.5rem;">
    <div class="container">
      <div class="lg-eyebrow">Udemy coupon categories</div>
      <h2 class="lg-h2">Udemy coupons <span style={{ color: "var(--brand)" }}>by category</span></h2>
      <p class="lg-sub">Eight stocked Udemy categories — pick your field and find the deepest verified coupon.</p>
      <div class="lg-index" role="list">
        {topCats.map((cat, i) => (
          <a href={"/categories/" + cat.slug} title={"Browse " + cat.count + " " + cat.name + " courses"} class="lg-row" role="listitem">
            <span class="lg-num">0{i + 1}</span>
            <div class="lg-row-main">
              <div class="lg-row-title">{cat.name}</div>
              <div class="lg-row-sub">{cat.blurb}</div>
            </div>
            <div class="lg-row-meta">{cat.count}<small>coupons</small></div>
            <span class="lg-row-arrow" aria-hidden="true">→</span>
          </a>
        ))}
      </div>
      <div style={{ marginTop: "1.5rem" }}><a href="/categories" class="lg-link">See every Udemy category →</a></div>
    </div>
  </section>

  <!-- TOP UDEMY INSTRUCTORS -->
  <section class="section-wrapper" aria-label="Top Udemy instructors with active coupons" style="padding-top:0.5rem;">
    <div class="container">
      <div class="lg-eyebrow">Udemy instructors — by active count</div>
      <h2 class="lg-h2">Top Udemy instructors <span style={{ color: "var(--brand)" }}>with active coupons</span></h2>
      <p class="lg-sub">Creators with the most verified Udemy discounts live right now.</p>
      <div class="lg-index" role="list">
        {topInstructors.map((ins) => (
          <a href={"/instructor/" + ins.slug} title={"Udemy coupons by " + ins.name} class="lg-row" role="listitem">
            {instructorImages.get(ins.slug) ? (
              <img src={instructorImages.get(ins.slug)} alt={ins.name} width={92} height={92} loading="lazy" class="lg-avatar" />
            ) : (
              <div class="lg-avatar-fallback" aria-hidden="true">{ins.name.charAt(0).toUpperCase()}</div>
            )}
            <div class="lg-row-main">
              <div class="lg-row-title">{ins.name}</div>
              <div class="lg-row-sub">Verified instructor catalog</div>
            </div>
            <div class="lg-row-meta">{ins.count}<small>coupons</small></div>
            <span class="lg-row-arrow" aria-hidden="true">→</span>
          </a>
        ))}
      </div>
      <div style={{ marginTop: "1.5rem" }}><a href="/instructors" class="lg-link">All Udemy instructors →</a></div>
    </div>
  </section>

  <!-- 7. LANGUAGES / TOPICS -->
  <section class="section-wrapper" aria-label="Udemy courses by language and topic" style="padding-top:0.5rem;">
    <div class="container">
      <div class="lg-eyebrow">Language & topic doors</div>
      <h2 class="lg-h2">Learn in <span style={{ color: "var(--brand)" }}>your language</span></h2>
      <p class="lg-sub">Most of the catalog is English-first with a growing Spanish shelf — or skip language entirely and browse by topic.</p>
      <div class="lg-index">
        <a href="/search?q=English" class="lg-row">
          <span class="lg-num">EN</span>
          <div class="lg-row-main"><div class="lg-row-title">English catalog</div><div class="lg-row-sub">The full {totalCount.toLocaleString()}-strong directory, fully searchable.</div></div>
          <span class="lg-row-arrow" aria-hidden="true">→</span>
        </a>
        <a href="/search?q=Spanish" class="lg-row">
          <span class="lg-num">ES</span>
          <div class="lg-row-main"><div class="lg-row-title">Catálogo en español</div><div class="lg-row-sub">Descuentos verificados en cursos seleccionados en español.</div></div>
          <span class="lg-row-arrow" aria-hidden="true">→</span>
        </a>
        <a href="/topics" class="lg-row">
          <span class="lg-num">✦</span>
          <div class="lg-row-main"><div class="lg-row-title">Browse by topic</div><div class="lg-row-sub">Python, AWS, Excel, design and 400+ narrower trails.</div></div>
          <span class="lg-row-arrow" aria-hidden="true">→</span>
        </a>
      </div>
    </div>
  </section>

  <!-- 8. GUIDE PROSE -->
  <section class="section-wrapper prose-section" aria-label="How Udemy coupon codes work" style="padding-top:0.5rem;">
    <div class="container">
      <div class="lg-eyebrow">Field notes</div>
      <h2 class="lg-h2">How Udemy <span style={{ color: "var(--brand)" }}>coupon codes work</span></h2>
      <p class="lg-sub">Short version: instructors trade margin for momentum. Longer version below — or jump to our <a href="/udemy-coupons-guide">coupon guide</a>.</p>
      <div class="lg-guide-grid" style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "3.5rem", alignItems: "start" }}>
        <div>
          <h3>Where do these codes come from?</h3>
          <p>Instructors generate coupon codes inside Udemy to push launches, seasonal sales and back-catalog titles. Each code carries its own expiry and redemption cap, which is why the same course can swing from $89 to $12 and back within days.</p>
          <p>Claiming one is deliberately boring: open a CoursesWyn ticket, hit the claim link, and Udemy applies the code at checkout. The course then lives in your library permanently.</p>
          <h3>Why would anyone discount 90%+?</h3>
          <p>Volume beats margin early on. A flood of enrolled learners means more ratings, better search placement inside Udemy, and social proof the instructor reuses everywhere else. You get the cheap seat; they get the flywheel.</p>
        </div>
        <div>
          <h3>How CoursesWyn keeps the list honest</h3>
          <p>Every candidate goes through rating, review-depth and instructor-history checks before it surfaces here, then gets re-polled on a schedule. Worked yesterday is no guarantee for today — so stale entries are retired instead of left to rot.</p>
          <ul style={{ color: "var(--muted)", lineHeight: 1.9, paddingLeft: "1.25rem", marginBottom: "1rem" }}>
            <li>Discount math is recomputed from live price pairs</li>
            <li>Thin, outdated or review-starved courses are filtered out</li>
            <li>Recency is shown so you can triage what to grab first</li>
          </ul>
          <p>Prefer a path over a single course? Start with <a href="/topics">topics</a> for skills or <a href="/categories">categories</a> for departments.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- 9. THREE DOORS -->
  <section class="section-wrapper" aria-label="Where to find Udemy coupons on CoursesWyn" style="padding-top:0.5rem;">
    <div class="container">
      <div class="lg-eyebrow">Three doors, one engine</div>
      <h2 class="lg-h2">Where to find <span style={{ color: "var(--brand)" }}>Udemy coupons</span> on CoursesWyn</h2>
      <p class="lg-sub">Pick your hunting style — the same verification engine sits behind all three doors.</p>
      <div class="lg-index">
        <a href="/udemy-coupon-code" class="lg-row">
          <span class="lg-num">01</span>
          <div class="lg-row-main"><div class="lg-row-title">Full coupon directory</div><div class="lg-row-sub">Every live code in one filterable grid. Best when you know roughly what you want.</div></div>
          <span class="lg-row-arrow" aria-hidden="true">→</span>
        </a>
        <a href="/udemy-coupons-today" class="lg-row">
          <span class="lg-num">02</span>
          <div class="lg-row-main"><div class="lg-row-title">Just landed today</div><div class="lg-row-sub">Only the freshest verifications from the last rotation.</div></div>
          <span class="lg-row-arrow" aria-hidden="true">→</span>
        </a>
        <a href="/udemy-coupons-guide" class="lg-row">
          <span class="lg-num">03</span>
          <div class="lg-row-main"><div class="lg-row-title">Learn the playbook</div><div class="lg-row-sub">How caps, expiries and seasonal pushes work.</div></div>
          <span class="lg-row-arrow" aria-hidden="true">→</span>
        </a>
      </div>
    </div>
  </section>

  <!-- 10. TRUST -->
  <section class="section-wrapper" aria-label="Why learners trust CoursesWyn" style="padding-top:0.5rem;">
    <div class="container">
      <div class="lg-eyebrow">House rules</div>
      <h2 class="lg-h2">Built to be <span style={{ color: "var(--brand)" }}>boringly reliable</span></h2>
      <p class="lg-sub">No mystery mechanics — just a directory that would rather show fewer working codes than many broken ones.</p>
      <div class="lg-check">
        {trustItems.map((item) => (
          <div class="lg-check-item"><span class="lg-check-mark">✓</span><span><b>{item.t}.</b> {item.d}</span></div>
        ))}
      </div>
      <div style={{ textAlign: "center", marginTop: "2rem" }}>
        <a href="/udemy-coupon-code" class="btn btn-primary btn-lg" style={{ borderRadius: "12px", padding: "0.9rem 2.2rem" }}>Grab a working code</a>
      </div>
    </div>
  </section>

  <!-- 11. FAQ -->
  <section class="section-wrapper" aria-label="Udemy coupon code FAQs" style="padding-top:0.5rem;">
    <div class="container">
      <div class="lg-eyebrow">Questions — answered</div>
      <h2 class="lg-h2">Udemy coupon code <span style={{ color: "var(--brand)" }}>FAQs</span></h2>
      <p class="lg-sub">The six things newcomers ask before their first discounted enrollment.</p>
      <div class="lg-faq">
        {faqs.map((faq, i) => (
          <div class="lg-faq-item" itemscope itemtype="https://schema.org/Question">
            <span class="lg-faq-no">Q{String(i + 1).padStart(2, "0")}</span>
            <div>
              <h3 class="lg-faq-q" itemprop="name">{faq.q}</h3>
              <div itemscope itemtype="https://schema.org/Answer" itemprop="acceptedAnswer"><p class="lg-faq-a" itemprop="text">{faq.a}</p></div>
            </div>
          </div>
        ))}
      </div>
      <div style={{ textAlign: "center", marginTop: "2rem" }}>
        <p style={{ color: "var(--muted)", marginBottom: "1rem", fontSize: "0.9rem" }}>Still curious? The full library is one click away.</p>
        <a href="/udemy-coupon-code" class="lg-link">Browse discounted courses →</a>
      </div>
    </div>
  </section>

  <!-- 12. COMMUNITY -->
  <section class="section-wrapper" aria-label="Get daily Udemy coupon alerts" style="padding-top:0.5rem;">
    <div class="container" style={{ textAlign: "center", maxWidth: "650px" }}>
      <div class="lg-eyebrow" style={{ justifyContent: "center" }}>Dispatches</div>
      <h2 class="lg-h2">Get daily <span style={{ color: "var(--brand)" }}>Udemy coupon alerts</span></h2>
      <p class="lg-sub" style={{ margin: "0 auto 2rem" }}>Follow CoursesWyn for the daily rotation of verified Udemy markdowns and study guides.</p>
      <div style={{ display: "flex", gap: "1rem", justifyContent: "center", flexWrap: "wrap" }}>
        <a href="https://facebook.com/CoursesWyn" target="_blank" rel="noopener noreferrer" class="community-btn" style={{ display: "inline-flex", alignItems: "center", gap: "0.6rem", padding: "0.9rem 1.75rem", borderRadius: "12px", background: "var(--card)", border: "1px solid var(--border)", color: "var(--text)", textDecoration: "none", fontWeight: 600, fontSize: "0.95rem", transition: "all 0.2s ease" }}>Facebook</a>
        <a href="https://x.com/CoursesWyn" target="_blank" rel="noopener noreferrer" class="community-btn" style={{ display: "inline-flex", alignItems: "center", gap: "0.6rem", padding: "0.9rem 1.75rem", borderRadius: "12px", background: "var(--card)", border: "1px solid var(--border)", color: "var(--text)", textDecoration: "none", fontWeight: 600, fontSize: "0.95rem", transition: "all 0.2s ease" }}>X / Twitter</a>
        <a href="https://medium.com/@coursewyn" target="_blank" rel="noopener noreferrer" class="community-btn" style={{ display: "inline-flex", alignItems: "center", gap: "0.6rem", padding: "0.9rem 1.75rem", borderRadius: "12px", background: "var(--card)", border: "1px solid var(--border)", color: "var(--text)", textDecoration: "none", fontWeight: 600, fontSize: "0.95rem", transition: "all 0.2s ease" }}>Medium</a>
      </div>
    </div>
  </section>

  <!-- PERSONAL PLAN TICKET -->
  <section class="section-wrapper" aria-label="Udemy Personal Plan" style="padding-top:0.5rem;">
    <div class="container">
      <div class="lg-sheet" style={{ textAlign: "center", overflow: "hidden", padding: "0 0 2.5rem" }}>
        <div class="lg-perf" aria-hidden="true" style={{ margin: "14px 8px 0" }}></div>
        <div style={{ padding: "2rem 1.5rem 0" }}>
          <div class="lg-eyebrow" style={{ justifyContent: "center" }}>Udemy Personal Plan — sponsored</div>
          <h2 class="lg-h2">One subscription, <span style={{ color: "var(--brand)" }}>26,000+ courses</span><br />zero coupon-hunting</h2>
          <p class="lg-sub" style={{ margin: "0 auto 1.5rem" }}>If single coupons feel like whack-a-mole, the flat-rate plan is the calm alternative — learn across topics without counting codes.</p>
          <a href="https://trk.udemy.com/c/6564357/3775958/39854" target="_blank" rel="noopener noreferrer" class="btn btn-primary btn-lg" style={{ borderRadius: "12px", padding: "0.95rem 2.4rem" }}>Start free trial</a>
          <p class="mono" style={{ fontSize: "0.7rem", color: "var(--muted)", marginTop: "14px", letterSpacing: "0.04em" }}>26,000+ courses · 7-day free trial · cancel anytime · from $20/mo</p>
        </div>
      </div>
    </div>
  </section>

<script>
  (function () {
    var input = document.querySelector('#hpSearchInput');
    if (!input) return;
    var raw = input.getAttribute('data-typewriter');
    if (!raw) return;
    var words = [];
    try { words = JSON.parse(raw); } catch (e) { return; }
    if (!words.length) return;
    var wi = 0, ci = 0, del = false;
    function tick() {
      var w = words[wi];
      if (!w) return;
      if (!del) { ci++; input.setAttribute('placeholder', w.substring(0, ci)); if (ci === w.length) { del = true; setTimeout(tick, 1500); return; } setTimeout(tick, 25); }
      else { ci--; input.setAttribute('placeholder', w.substring(0, ci)); if (ci === 0) { del = false; wi = (wi + 1) % words.length; } setTimeout(tick, 18); }
    }
    setTimeout(tick, 400);
  })();
</script>

</Layout>
