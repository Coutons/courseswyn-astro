---
import Layout from "../layouts/Layout.astro";
import { Image } from 'astro:assets';
import { getCollection } from "astro:content";
import { readDeals } from "../lib/store";
import { formatStringWIB, toISODateWIB, formatDateWIB } from "../lib/date";
import { normName, slugifyCategory } from "../lib/utils";
import { CATEGORY_ICONS } from "../lib/categories";

const allDeals = await readDeals();

const todayStr = toISODateWIB(new Date());
const todayDeals = allDeals.filter((d) => {
  const dDate = toISODateWIB(new Date(d.updatedAt ?? d.createdAt ?? d.expiresAt ?? 0));
  return dDate === todayStr;
});

const now = new Date();
const activeDeals = allDeals.filter((d) => !d.expiresAt || new Date(d.expiresAt) > now);
const featuredDeals = activeDeals.sort((a, b) => new Date(b.updatedAt ?? b.createdAt ?? 0).getTime() - new Date(a.updatedAt ?? a.createdAt ?? 0).getTime()).slice(0, 6);

const categoryMap = new Map<string, { name: string; count: number; subs: Map<string, number> }>();
allDeals.forEach(deal => {
  const cName = normName(deal.category) || "Uncategorized";
  const cKey = cName.toLowerCase();
  if (!categoryMap.has(cKey)) categoryMap.set(cKey, { name: cName, count: 0, subs: new Map() });
  const bucket = categoryMap.get(cKey)!;
  bucket.count++;
  const sName = normName(deal.subcategory);
  if (sName && sName.toLowerCase() !== cKey) {
    bucket.subs.set(sName, (bucket.subs.get(sName) || 0) + 1);
  }
});
const CATEGORY_DESCRIPTIONS: Record<string, string> = {
  "development": "Web, mobile, and software engineering courses.",
  "it-and-software": "IT fundamentals, cloud, and security certifications.",
  "business": "Entrepreneurship, management, and strategy courses.",
  "design": "Graphic design, UI/UX, and digital art courses.",
  "marketing": "SEO, social media, and growth marketing courses.",
  "data-science": "Machine learning, Python, and AI fundamentals.",
  "personal-development": "Productivity, leadership, and career skills.",
  "photography-and-video": "Photo, video, and post-production courses.",
  "music": "Music theory, production, and instrument courses.",
  "teaching-and-academics": "Academic subjects, test prep, languages.",
  "office-productivity": "Microsoft Office, Google Workspace, PM tools.",
  "finance-and-accounting": "Accounting, investing, and crypto courses.",
  "programming-languages": "Python, JavaScript, Java, Rust, and more.",
};
const topCategories = Array.from(categoryMap.values())
  .sort((a, b) => b.count - a.count).slice(0, 9)
  .map(c => ({ ...c, slug: slugifyCategory(c.name), description: CATEGORY_DESCRIPTIONS[slugifyCategory(c.name)] ?? `Browse ${c.name} courses with verified Udemy coupons.` }));
const totalStudents = allDeals.reduce((sum, d) => sum + (d.students ?? 0), 0);
const uniqueInstructors = [...new Set(allDeals.filter(d => d.instructor).map(d => d.instructor))].length;
const monthName = formatStringWIB(new Date(), { month: 'long', year: 'numeric' });
const pageTitle = `Free Udemy Courses 100% OFF Coupons & Promo Codes [${monthName}] | CoursesWyn`;
const pageDescription = `Get ${allDeals.length.toLocaleString()}+ verified Udemy coupon codes 100% OFF. Free access to top-rated courses in AI, Python, data science, cloud, and web development. Expert-reviewed & manually verified daily. Start learning for free at CoursesWyn.`;
const instructorUrl = "https://teach.udemy.com/update-for-instructors-improving-free-coupons-better-targeting-higher-quality-learning/";
const toISODate = (d?: string) => toISODateWIB(new Date(d ?? 0));

const allPosts = await getCollection("blog");
const recentPosts = allPosts.sort((a: any, b: any) => {
  const dateA = a.data.updatedAt || a.data.pubDate;
  const dateB = b.data.updatedAt || b.data.pubDate;
  return dateB.getTime() - dateA.getTime();
}).slice(0, 3);

const jsonLd = {
  "@context": "https://schema.org", "@graph": [
    { "@type": "WebSite", "@id": "https://courseswyn.com/#website", "url": "https://courseswyn.com", "name": "CoursesWyn", "description": pageDescription, "potentialAction": { "@type": "SearchAction", "target": { "@type": "EntryPoint", "urlTemplate": "https://courseswyn.com/search?q={search_term_string}" }, "query-input": "required name=search_term_string" } },
    { "@type": "Organization", "@id": "https://courseswyn.com/#organization", "name": "CoursesWyn", "url": "https://courseswyn.com", "logo": { "@type": "ImageObject", "url": "https://courseswyn.com/logo.svg", "width": 200, "height": 60 }, "sameAs": ["https://x.com/CoursesWyn", "https://www.facebook.com/CoursesWyn"] },
    { "@type": "BreadcrumbList", "@id": "https://courseswyn.com/#breadcrumb", "itemListElement": [{ "@type": "ListItem", "position": 1, "name": "Home", "item": "https://courseswyn.com" }, { "@type": "ListItem", "position": 2, "name": "Coupons", "item": "https://courseswyn.com/udemy-coupon-code" }] },
    { "@type": "FAQPage", "@id": "https://courseswyn.com/#faq", "mainEntity": [
      { "@type": "Question", "name": "Can free users use the coupons?", "acceptedAnswer": { "@type": "Answer", "text": "Free users can browse every deal. All coupons are unlocked — no premium paywall required. Click any deal and enroll directly on Udemy." } },
      { "@type": "Question", "name": "Where do the coupons come from?", "acceptedAnswer": { "@type": "Answer", "text": "From CoursesWyn proprietary discovery systems and instructor submissions. Every coupon is manually verified before listing. We do not accept auto-submitted or unverified coupons." } },
      { "@type": "Question", "name": "Why would instructors submit coupons here?", "acceptedAnswer": { "@type": "Answer", "text": "Public feeds burn coupons quickly. CoursesWyn brings them learners who are more likely to study, finish, and leave a review." } },
      { "@type": "Question", "name": "Do coupons always work?", "acceptedAnswer": { "@type": "Answer", "text": "CoursesWyn checks coupon validity close to real time. However, Udemy can still expire a coupon when its enrollment limit is reached. We remove expired deals within hours." } },
      { "@type": "Question", "name": "Do I need a Udemy account?", "acceptedAnswer": { "@type": "Answer", "text": "Yes. CoursesWyn helps you find and open the coupon. Enrollment and course access happen on Udemy. A free Udemy account is all you need." } },
      { "@type": "Question", "name": "How to redeem a coupon?", "acceptedAnswer": { "@type": "Answer", "text": "Follow our step-by-step redemption guide on CoursesWyn to claim any coupon and enroll on Udemy in under 60 seconds." } }
    ] },
    { "@type": "WebPage", "@id": "https://courseswyn.com/#webpage", "url": "https://courseswyn.com", "name": pageTitle, "description": pageDescription, "isPartOf": { "@id": "https://courseswyn.com/#website" }, "datePublished": "2024-06-01", "dateModified": toISODate() }
  ]
};
---

<Layout title={pageTitle} description={pageDescription} canonical="https://courseswyn.com" image="/assets/images/og-default.svg">
  <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} is:inline />

  <style>

    .home-card-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(340px, 1fr));
      gap: 1.5rem;
      margin-bottom: 2.5rem;
    }

    .home-card {
      display: flex;
      flex-direction: column;
      background: var(--card);
      border: 1px solid rgba(212,167,55,0.08);
      border-top: 1px solid transparent;
      border-radius: 16px;
      overflow: hidden;
      transition: all 0.4s ease;
      position: relative;
    }
    .home-card::before {
      content: '';
      position: absolute;
      top: -1px;
      left: 0;
      right: 0;
      height: 2px;
      background: linear-gradient(90deg, #B8860B, #D4AF37, #E8C547, #D4AF37, #B8860B);
      opacity: 0.15;
      transition: opacity 0.4s ease;
      z-index: 1;
    }
    .home-card:hover {
      border-color: rgba(212,167,55,0.3);
      transform: translateY(-4px);
      box-shadow: 0 16px 40px -8px rgba(212,167,55,0.15);
    }
    .home-card:hover::before {
      opacity: 1;
    }
    .home-card:hover .home-card-img img {
      transform: scale(1.06);
    }
    .home-card-img {
      position: relative;
      height: 180px;
      overflow: hidden;
      background: var(--border);
    }
    .home-card-img img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform 0.6s ease;
    }
    .home-badge-updated {
      position: absolute;
      top: 0.75rem;
      right: 0.75rem;
      background: rgba(0,0,0,0.75);
      backdrop-filter: blur(4px);
      color: white;
      padding: 0.2rem 0.5rem;
      border-radius: 4px;
      font-size: 0.7rem;
      font-weight: 600;
    }
    .home-badge-discount {
      position: absolute;
      top: 0.75rem;
      left: 0.75rem;
      background: #ef4444;
      color: white;
      padding: 0.25rem 0.5rem;
      border-radius: 4px;
      font-size: 0.75rem;
      font-weight: 700;
    }
    .home-badge-free {
      position: absolute;
      top: 0.75rem;
      left: 0.75rem;
      background: linear-gradient(135deg, #B8860B, #D4AF37, #E8C547, #D4AF37);
      color: #1a1814;
      padding: 0.25rem 0.5rem;
      border-radius: 4px;
      font-size: 0.75rem;
      font-weight: 700;
      box-shadow: 0 2px 8px rgba(212,167,55,0.3);
    }
    .home-card-body {
      padding: 1.25rem;
      flex: 1;
      display: flex;
      flex-direction: column;
      gap: 10px;
    }
    .home-card-title {
      font-size: 1.05rem;
      font-weight: 700;
      color: var(--text);
      margin: 0;
      line-height: 1.35;
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }
    .home-card-title a {
      color: inherit;
      text-decoration: none;
    }
    .home-card-title a:hover {
      color: var(--brand);
    }
    .home-card-instructor {
      display: flex;
      align-items: center;
      gap: 6px;
      font-size: 0.85rem;
      color: var(--text);
      font-weight: 500;
      margin: 0;
    }
    .home-card-instructor svg {
      width: 14px;
      height: 14px;
      fill: var(--muted);
      flex-shrink: 0;
    }
    .home-card-instructor a {
      color: var(--text);
      text-decoration: none;
    }
    .home-card-instructor a:hover {
      color: var(--brand);
    }
    .home-card-desc {
      font-size: 0.85rem;
      color: var(--muted);
      margin: 0;
      line-height: 1.5;
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }
    .home-card-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
    .home-card-rating {
      display: flex;
      align-items: center;
      gap: 4px;
      font-size: 0.85rem;
    }
    .home-card-rating svg {
      width: 14px;
      height: 14px;
      fill: var(--brand);
      flex-shrink: 0;
    }
    .home-card-rating-value {
      font-weight: 600;
      color: var(--muted);
    }
    .home-card-students {
      font-weight: 600;
      color: var(--muted);
    }
    .home-card-cat {
      display: inline-flex;
      align-items: center;
      gap: 4px;
      font-size: 0.8rem;
      font-weight: 600;
      color: var(--brand);
      text-decoration: none;
      padding: 0.2rem 0.5rem;
      border-radius: 4px;
      background: var(--card-hover, rgba(212,167,55,0.06));
      transition: all 0.3s ease;
    }
    .home-card-cat:hover {
      color: var(--brand);
      opacity: 0.85;
    }
    .home-card-cat svg {
      width: 12px;
      height: 12px;
      fill: currentColor;
    }
    .home-card-price-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
    }
    .home-card-price {
      font-size: 1.1rem;
      font-weight: 700;
      color: var(--brand);
    }
    .home-card-price del {
      font-size: 0.85rem;
      font-weight: 500;
      color: var(--muted);
      margin-left: 8px;
      text-decoration: line-through;
    }
    .home-card-discount-badge {
      background: #ef4444;
      color: white;
      padding: 0.2rem 0.5rem;
      border-radius: 4px;
      font-size: 0.75rem;
      font-weight: 700;
    }
    .home-card-verified-badge {
      background: linear-gradient(135deg, hsl(142,76%,36%), hsl(142,76%,45%));
      color: white;
      padding: 0.125rem 0.5rem;
      border-radius: 12px;
      font-size: 0.7rem;
      font-weight: 700;
      display: inline-flex;
      align-items: center;
      gap: 3px;
    }
    .home-card-verified-badge svg {
      width: 10px;
      height: 10px;
      fill: currentColor;
    }
    .home-card-duration {
      display: flex;
      align-items: center;
      gap: 6px;
      font-size: 0.8rem;
      color: var(--muted);
      font-weight: 500;
    }
    .home-card-duration svg {
      width: 14px;
      height: 14px;
      fill: var(--muted);
      flex-shrink: 0;
    }
    .home-card-btn {
      display: block;
      width: 100%;
      padding: 0.75rem 1.5rem;
      border-radius: 8px;
      background: linear-gradient(135deg, #B8860B 0%, #D4AF37 25%, #E8C547 50%, #D4AF37 75%, #B8860B 100%);
      color: #1a1814;
      font-size: 0.9rem;
      font-weight: 700;
      text-align: center;
      text-decoration: none;
      transition: all 0.3s;
    }
    .home-card-btn:hover {
      transform: translateY(-1px);
      box-shadow: 0 0 16px 2px rgba(212,167,55,0.3);
    }

    @media (max-width: 768px) {
      .home-card-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: 1rem;
      }
    }
    @media (max-width: 480px) {
      .home-card-grid {
        grid-template-columns: 1fr;
      }
    }

    .steps-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1.5rem;
      margin-top: 2rem;
    }
    .step-card {
      text-align: center;
      padding: 2rem 1.5rem;
      background: var(--card);
      border: 1px solid rgba(212,167,55,0.08);
      border-radius: 16px;
      transition: all 0.4s ease;
      position: relative;
      overflow: hidden;
    }
    .step-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 2px;
      background: linear-gradient(90deg, #B8860B, #D4AF37, #E8C547, #D4AF37, #B8860B);
      opacity: 0.15;
      transition: opacity 0.4s ease;
    }
    .step-card:hover {
      border-color: rgba(212,167,55,0.3);
      transform: translateY(-4px);
      box-shadow: 0 12px 32px -8px rgba(212,167,55,0.15);
    }
    .step-card:hover::before {
      opacity: 1;
    }
    .step-number {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 52px;
      height: 52px;
      border-radius: 50%;
      background: linear-gradient(135deg, #B8860B, #D4AF37, #E8C547);
      color: #1a1814;
      font-weight: 900;
      font-size: 1.3rem;
      margin: 0 auto 1rem;
      box-shadow: 0 0 20px rgba(212,167,55,0.2);
    }
    .step-card h3 {
      font-size: 1rem;
      font-weight: 700;
      color: var(--text);
      margin: 0 0 0.5rem;
    }
    .step-card p {
      font-size: 0.85rem;
      color: var(--text-secondary);
      line-height: 1.5;
      margin: 0;
    }
    @media (max-width: 768px) {
      .steps-grid {
        grid-template-columns: 1fr;
        gap: 1rem;
      }
    }

    .blog-mini-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1rem;
      margin-top: 1.5rem;
    }
    .blog-mini-card {
      display: flex;
      flex-direction: column;
      background: var(--card);
      border: 1px solid rgba(212,167,55,0.08);
      border-top: 1px solid transparent;
      border-radius: 12px;
      overflow: hidden;
      text-decoration: none;
      transition: all 0.4s ease;
      position: relative;
    }
    .blog-mini-card::before {
      content: '';
      position: absolute;
      top: -1px;
      left: 0;
      right: 0;
      height: 2px;
      background: linear-gradient(90deg, #B8860B, #D4AF37, #E8C547, #D4AF37, #B8860B);
      opacity: 0.15;
      transition: opacity 0.4s ease;
      z-index: 1;
    }
    .blog-mini-card:hover {
      border-color: rgba(212,167,55,0.3);
      transform: translateY(-4px);
      box-shadow: 0 12px 32px -8px rgba(212,167,55,0.15);
    }
    .blog-mini-card:hover::before {
      opacity: 1;
    }
    .blog-mini-body {
      padding: 1rem;
      flex: 1;
      display: flex;
      flex-direction: column;
    }
    .blog-mini-tag {
      font-size: 0.65rem;
      font-weight: 700;
      color: var(--brand);
      text-transform: uppercase;
      letter-spacing: 0.05em;
      margin-bottom: 0.4rem;
    }
    .blog-mini-body h3 {
      font-size: 0.85rem;
      font-weight: 600;
      color: var(--text);
      margin: 0 0 0.4rem;
      line-height: 1.4;
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }
    .blog-mini-card:hover .blog-mini-body h3 {
      color: var(--brand);
    }
    .blog-mini-desc {
      font-size: 0.72rem;
      color: var(--text-secondary);
      line-height: 1.4;
      margin: 0.25rem 0 0.4rem;
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }
    .blog-mini-date {
      font-size: 0.7rem;
      color: var(--muted);
      margin-top: auto;
    }
    .topic-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 1rem;
    }
    .topic-card {
      border-radius: var(--radius);
      border: 1px solid rgba(212,167,55,0.08);
      background: var(--card);
      padding: 1.5rem;
      display: flex;
      flex-direction: column;
      transition: all 0.4s ease;
      position: relative;
      overflow: hidden;
    }
    .topic-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 2px;
      background: linear-gradient(90deg, #B8860B, #D4AF37, #E8C547, #D4AF37, #B8860B);
      opacity: 0.15;
      transition: opacity 0.4s ease;
    }
    .topic-card:hover {
      border-color: rgba(212,167,55,0.3);
      transform: translateY(-4px);
      box-shadow: 0 12px 32px -8px rgba(212,167,55,0.15);
    }
    .topic-card:hover::before {
      opacity: 1;
    }
    .topic-card-head {
      display: flex;
      align-items: center;
      gap: 0.6rem;
      margin-bottom: 1rem;
    }
    .topic-icon {
      width: 40px;
      height: 40px;
      border-radius: 10px;
      display: flex;
      align-items: center;
      justify-content: center;
      border: 1px solid rgba(212,167,55,0.2);
      background: var(--brand-soft);
      font-size: 1.1rem;
      flex-shrink: 0;
      color: var(--brand);
    }
    .topic-card h3 {
      font-size: 0.95rem;
      font-weight: 800;
      margin: 0;
      color: var(--text);
    }
    .topic-links {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }
    .topic-link {
      display: flex;
      flex-direction: column;
      gap: 0.1rem;
      text-decoration: none;
    }
    .topic-link-title {
      font-size: 0.85rem;
      font-weight: 700;
      color: var(--brand);
      transition: all 0.2s;
      border-bottom: 1px solid transparent;
    }
    .topic-link:hover .topic-link-title {
      border-bottom-color: var(--brand);
    }
    .topic-link-desc {
      font-size: 0.78rem;
      color: var(--muted);
      line-height: 1.4;
    }

    @media (max-width: 768px) {
      .blog-mini-grid {
        grid-template-columns: 1fr;
      }
    }

    .stat-grid-home {
      grid-template-columns: repeat(4, 1fr);
    }
    @media (max-width: 768px) {
      .stat-grid-home {
        grid-template-columns: repeat(2, 1fr);
      }
    }
    @media (max-width: 768px) {
      .home-card-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: 0.75rem;
      }
    }
    @media (max-width: 480px) {
      .home-card-grid {
        grid-template-columns: 1fr;
      }
    }
  </style>

  <!-- HERO -->
  <section class="hero">
    <div class="container">
      <div class="hero-content anim-up">
        <h1>
          <span style="display:block">Free Udemy Courses</span>
          <span class="hero-gradient-text" style="display:block;font-size:clamp(2.5rem,6vw,4.5rem)">100% OFF</span>
          <span style="display:block">Coupons &amp; Promo Codes Updated Daily</span>
        </h1>
        <p>Access premium courses in AI, Python, web development and data science — all <strong>100% OFF</strong> with manually verified coupon codes updated daily.</p>
        <p style="font-size:0.82rem;color:var(--text-secondary);margin-top:0.5rem">Trusted by {Math.round(totalStudents / 1000)}K+ learners &middot; {uniqueInstructors} expert instructors &middot; {allDeals.length.toLocaleString()} active deals</p>
        <div class="hero-cta">
          <a href="/udemy-coupon-code" class="btn btn-primary btn-lg" style="border-radius:999px;padding:1rem 2.5rem">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/></svg>
            Browse Coupons
          </a>
          <a href="https://trk.udemy.com/c/6564357/3775958/39854" target="_blank" rel="noopener noreferrer" class="btn btn-ghost btn-lg" style="border-radius:999px;padding:1rem 2.5rem;color:#fbbf24;border-color:#fbbf24">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#fbbf24" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2 4l3 12h14l3-12-6 7-4-7-4 7-6-7z"/><path d="M3 20h18"/></svg>
            Unlock Coupon Access
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- STATS -->
  <section style="padding-bottom:2rem">
    <div class="container">
      <div class="stat-grid stat-grid-home">
        <div class="stat-box">
          <div class="stat-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="var(--brand)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M2 9a3 3 0 0 1 0 6v2a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2v-2a3 3 0 0 1 0-6V7a2 2 0 0 0-2-2H4a2 2 0 0 0-2 2Z"/><path d="M13 5v2"/><path d="M13 17v2"/><path d="M13 11v2"/></svg>
            <span class="stat-value">{allDeals.length.toLocaleString()}</span>
          </div>
          <div class="stat-label">Active Coupons</div>
        </div>
        <div class="stat-box">
          <div class="stat-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="var(--brand)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M16 7h6v6"/><path d="m22 7-8.5 8.5-5-5L2 17"/></svg>
            <span class="stat-value">{todayDeals.length.toLocaleString()}</span>
          </div>
          <div class="stat-label">Added Today</div>
        </div>
        <div class="stat-box">
          <div class="stat-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="var(--brand)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="m9 12 2 2 4-4"/></svg>
            <span class="stat-value">{uniqueInstructors}</span>
          </div>
          <div class="stat-label">Expert Instructors</div>
        </div>
        <div class="stat-box">
          <div class="stat-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="var(--brand)" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
            <span class="stat-value">{Math.round(totalStudents / 1000)}K+</span>
          </div>
          <div class="stat-label">Total Students</div>
        </div>
      </div>
    </div>
  </section>

  <!-- FEATURED DEALS -->
  <section class="section" style="padding-top:0;padding-bottom:0">
    <div class="container">
      <div style="display:flex;align-items:flex-end;justify-content:space-between;margin-bottom:0.5rem;flex-wrap:wrap;gap:0.5rem">
        <div>
          <span class="section-title">Fresh Deals</span>
          <h2 class="section-heading" style="margin-bottom:0.25rem">Latest coupon codes</h2>
          <p style="color:var(--text-secondary);font-size:0.85rem;line-height:1.5;margin:0;max-width:480px">Manually verified Udemy coupons updated {monthName}. Click any deal to enroll for free.</p>
        </div>
        <a href="/udemy-coupon-code" style="color:var(--brand);font-size:0.85rem;font-weight:600;text-decoration:none;white-space:nowrap">View all {allDeals.length.toLocaleString()} deals &rarr;</a>
      </div>
      <div class="home-card-grid">
        {featuredDeals.map((deal) => {
          const slug = deal.slug || deal.id;
          const instructor = deal.instructor?.split(',')[0]?.trim() || null;
          const instructorSlug = instructor ? instructor.toLowerCase().replace(/\s+/g, '-').replace(/[^a-z0-9-]/g, '') : null;
          const p = typeof deal.price === "number" && isFinite(deal.price) ? deal.price : 9.99;
          const op = typeof deal.originalPrice === "number" && isFinite(deal.originalPrice) && deal.originalPrice > 0 ? deal.originalPrice : null;
          const isFree = p === 0;
          const discountPct = op && op > p ? Math.round(100 - (p / op) * 100) : (isFree ? 100 : null);
          const hasDiscount = discountPct !== null;
          const duration = deal.duration ? deal.duration.replace(/^(\d+)\s*hours?.*$/i, " $1h") : "";
          const durationLabel = deal.duration?.replace(/^(\d+)\s*hours?(?:\s*(\d+)\s*minutes?)?.*$/i, (m: string, h: string, min: string) => min ? `${h}h ${min}m` : `${h}h`) || "";
          const categorySlug = deal.category ? deal.category.toLowerCase().replace(/\s+&\s+/g, '-and-').replace(/['\s]+/g, '-').replace(/-+/g, '-').replace(/^-|-$/g, '') : null;
          const description = deal.description ? deal.description.replace(/<[^>]*>/g, '').slice(0, 120) : null;
          const updatedAgo = deal.updatedAt ? (() => {
            const diff = Date.now() - new Date(deal.updatedAt).getTime();
            const hrs = Math.floor(diff / 3600000);
            if (hrs < 1) return 'Updated just now';
            if (hrs < 24) return `Updated ${hrs}h ago`;
            const days = Math.floor(hrs / 24);
            if (days < 30) return `Updated ${days}d ago`;
            return `Updated ${Math.floor(days / 30)}mo ago`;
          })() : null;
          return (
            <div class="home-card">
              <a href={`/coupon/${slug}`} class="home-card-img">
                <Image src={deal.image} alt={deal.title} width={400} height={225} loading="lazy" inferSize={false} />
                {updatedAgo && <span class="home-badge-updated">{updatedAgo}</span>}
                {isFree && <span class="home-badge-free">FREE</span>}
                {!isFree && hasDiscount && <span class="home-badge-discount">{discountPct}% OFF</span>}
              </a>
              <div class="home-card-body">
                <h3 class="home-card-title">
                  <a href={`/coupon/${slug}`}>{deal.title}</a>
                </h3>
                {instructor && (
                  <p class="home-card-instructor">
                    <svg viewBox="0 0 24 24"><path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/></svg>
                    {instructorSlug ? <a href={`/instructor/${instructorSlug}`}>By {instructor}</a> : `By ${instructor}`}
                  </p>
                )}
                {description && <p class="home-card-desc">{description}</p>}
                <div class="home-card-row">
                  <div class="home-card-rating">
                    {deal.rating && (
                      <>
                        <svg viewBox="0 0 24 24"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
                        <span class="home-card-rating-value">{deal.rating.toFixed(1)}</span>
                        {typeof deal.students === 'number' && deal.students > 0 && (
                          <span class="home-card-students">
                            ({(deal.students >= 1000 ? (deal.students/1000).toFixed(1).replace(/\.0$/, '') + 'K' : deal.students)}+ students)
                          </span>
                        )}
                      </>
                    )}
                  </div>
                  {deal.category && categorySlug && (
                    <a href={`/categories/${categorySlug}`} class="home-card-cat">
                      <svg viewBox="0 0 24 24"><path d="M10 4H4c-1.11 0-2 .89-2 2v3h2V6h4V4zM10 19v-2H4v-3H2v3c0 1.11.89 2 2 2h6zM20 4h-6v2h4v3h2V6c0-1.11-.89-2-2-2zM20 16h-2v3h-4v2h6c1.11 0 2-.89 2-2v-3z"/></svg>
                      {deal.category}
                    </a>
                  )}
                </div>
                <div class="home-card-price-row">
                  <div style="display:flex;align-items:center;gap:8px">
                    <span class="home-card-price">{isFree ? 'FREE' : `$${p.toFixed(2)}`}</span>
                    {hasDiscount && op && <del>${op.toFixed(2)}</del>}
                    {!isFree && hasDiscount && <span class="home-card-discount-badge">{discountPct}% OFF</span>}
                  </div>
                  <span class="home-card-verified-badge">
                    <svg viewBox="0 0 24 24"><path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z"/></svg>
                    VERIFIED
                  </span>
                </div>
                {duration && (
                  <div class="home-card-duration">
                    <svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><polyline points="12,6 12,12 16,14"/></svg>
                    Course Duration: {duration}
                  </div>
                )}
                <a href={deal.url || `/coupon/${slug}`}
                  target={deal.url ? "_blank" : "_self"}
                  rel={deal.url ? "noopener noreferrer" : undefined}
                  class="home-card-btn">
                  {isFree ? 'Get Free' : 'Get Coupon'}
                </a>
              </div>
            </div>
          );
        })}
      </div>
    </div>
  </section>

  <!-- HOW IT WORKS -->
  <section class="section" style="padding-top:0;padding-bottom:0">
    <div class="container">
      <div style="text-align:center;max-width:600px;margin:0 auto">
        <span class="section-title">Simple Process</span>
        <h2 class="section-heading">How to get free Udemy courses</h2>
        <p style="color:var(--text-secondary);font-size:0.92rem;line-height:1.6;margin:0.75rem auto 0">No credit card needed. Browse verified deals, click any coupon, and enroll at no cost. Takes less than a minute.</p>
      </div>
      <div class="steps-grid">
        <div class="step-card">
          <div class="step-number">1</div>
          <h3>Browse Courses</h3>
          <p>Explore {allDeals.length.toLocaleString()}+ verified deals sorted by category, rating, and instructor.</p>
        </div>
        <div class="step-card">
          <div class="step-number">2</div>
          <h3>Click Any Coupon</h3>
          <p>Each deal links directly to the Udemy enrollment page with the 100% off coupon pre-applied.</p>
        </div>
        <div class="step-card">
          <div class="step-number">3</div>
          <h3>Enroll for Free</h3>
          <p>Create a free Udemy account (or log in) and start learning — no payment required.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- TOPIC CARDS -->
  <section class="section">
    <div class="container">
      <div style="text-align:center;max-width:600px;margin:0 auto 2.5rem">
        <span class="section-title">Expert Guides by Topic</span>
        <h2 class="section-heading">Find the best courses in your field</h2>
        <p style="color:var(--text-secondary);font-size:0.92rem;line-height:1.6;margin:0.75rem auto 0">In-depth comparisons and rankings across AI, web development, data science and more — all tested and reviewed by our team.</p>
      </div>
      <div class="topic-grid">
        <div class="topic-card">
          <div class="topic-card-head">
            <span class="topic-icon">🧠</span>
            <h3>AI & Agentic AI</h3>
          </div>
          <div class="topic-links">
            <a href="/blog/best-udemy-agentic-ai-courses-2026" class="topic-link">
              <span class="topic-link-title">10 Best Agentic AI Courses 2026 →</span>
              <span class="topic-link-desc">Top-ranked courses tested by engineers for building autonomous AI agents</span>
            </a>
            <a href="/blog/top-10-best-mcp-courses-udemy-2026" class="topic-link">
              <span class="topic-link-title">Best MCP (Model Context Protocol) Courses →</span>
              <span class="topic-link-desc">Master MCP to link any LLM with tools, APIs and databases</span>
            </a>
            <a href="/blog/best-prompt-engineering-courses-udemy-2026" class="topic-link">
              <span class="topic-link-title">Best Prompt Engineering Courses 2026 →</span>
              <span class="topic-link-desc">From zero to advanced prompt engineering techniques that work</span>
            </a>
          </div>
        </div>
        <div class="topic-card">
          <div class="topic-card-head">
            <span class="topic-icon">🌐</span>
            <h3>Web & Software Dev</h3>
          </div>
          <div class="topic-links">
            <a href="/blog/best-ai-web-development-courses-udemy-2026" class="topic-link">
              <span class="topic-link-title">Best AI Web Development Courses 2026 →</span>
              <span class="topic-link-desc">Use Cursor, Windsurf and Copilot to build full-stack apps faster</span>
            </a>
            <a href="/blog/top-10-python-courses-on-udemy" class="topic-link">
              <span class="topic-link-title">Top 10 Python Courses on Udemy 2026 →</span>
              <span class="topic-link-desc">Top-rated Python courses ranked for beginners and pros in 2026</span>
            </a>
            <a href="/blog/best-react-nextjs-courses-2026" class="topic-link">
              <span class="topic-link-title">Best React & Next.js Courses 2026 →</span>
              <span class="topic-link-desc">Modern UI development with the latest frameworks hands-on</span>
            </a>
          </div>
        </div>
        <div class="topic-card">
          <div class="topic-card-head">
            <span class="topic-icon">☁️</span>
            <h3>Data & Cloud</h3>
          </div>
          <div class="topic-links">
            <a href="/blog/best-data-science-courses-udemy" class="topic-link">
              <span class="topic-link-title">Best Data Science Courses 2026 →</span>
              <span class="topic-link-desc">Learn Python, ML and data analysis through real-world projects</span>
            </a>
            <a href="/blog/best-aws-certification-courses-2026" class="topic-link">
              <span class="topic-link-title">Best AWS Certification Courses 2026 →</span>
              <span class="topic-link-desc">Complete certification prep with practice tests and study guides</span>
            </a>
            <a href="/blog/cloud-computing-courses-udemy-2026" class="topic-link">
              <span class="topic-link-title">Top 6 Cloud Computing Courses 2026 →</span>
              <span class="topic-link-desc">AWS, Azure and GCP fundamentals taught through hands-on labs</span>
            </a>
          </div>
        </div>
        <div class="topic-card">
          <div class="topic-card-head">
            <span class="topic-icon">🏆</span>
            <h3>Career & Platform</h3>
          </div>
          <div class="topic-links">
            <a href="/blog/best-udemy-courses-2026" class="topic-link">
              <span class="topic-link-title">70 Best Udemy Courses 2026 →</span>
              <span class="topic-link-desc">Data-driven rankings of the top courses across every category</span>
            </a>
            <a href="/blog/udemy-vs-coursera-vs-pluralsight" class="topic-link">
              <span class="topic-link-title">Udemy vs Coursera vs Pluralsight 2026 →</span>
              <span class="topic-link-desc">Which platform gives the best value for your learning goals?</span>
            </a>
            <a href="/blog/is-udemy-worth-it" class="topic-link">
              <span class="topic-link-title">Is Udemy Worth It in 2026? →</span>
              <span class="topic-link-desc">Honest review based on 10M+ student ratings and expert analysis</span>
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- POPULAR CATEGORIES -->
  <section class="section" style="padding-top:0">
    <div class="container">
      <div style="text-align:center;max-width:600px;margin:0 auto 2rem">
        <span class="section-title">Categories</span>
        <h2 class="section-heading" style="margin:0">Browse by category</h2>
        <p style="color:var(--text-secondary);font-size:0.92rem;line-height:1.6;margin:0.75rem auto 0">Find verified Udemy coupons in your field — from AI and coding to design and cloud computing.</p>
      </div>
      <div class="adv-cards">
        {topCategories.map(cat => (
          <a href={`/categories/${cat.slug}`} title={`Browse ${cat.count} ${cat.name} courses with Udemy coupons`} class="adv-card" style="text-decoration:none;cursor:pointer;">
            <div style="display:flex;align-items:center;gap:12px;margin-bottom:12px;">
              <div style="width:44px;height:44px;border-radius:10px;background:linear-gradient(135deg,var(--brand),#C5A028);display:flex;align-items:center;justify-content:center;color:#1a1814;flex-shrink:0;" set:html={CATEGORY_ICONS[cat.slug] || `<span style="font-weight:800;font-size:1rem;">${cat.name.charAt(0).toUpperCase()}</span>`}></div>
              <div>
                <h3 style="margin:0;font-size:1rem;font-weight:700;color:var(--text);line-height:1.2;">{cat.name}</h3>
                <p style="margin:2px 0 0;font-size:0.82rem;color:var(--brand);font-weight:600;">{cat.count} coupons</p>
              </div>
            </div>
            <p style="font-size:0.85rem;color:var(--text-secondary);line-height:1.55;margin:0 0 12px;">{cat.description}</p>
            {cat.subs.size > 0 && (
              <div style="display:flex;gap:4px;flex-wrap:wrap;">
                {Array.from(cat.subs.entries()).sort((a, b) => b[1] - a[1]).slice(0, 3).map(([sub]) => (
                  <span style="display:inline-block;padding:2px 8px;border-radius:999px;font-size:0.7rem;background:var(--bg-secondary);border:1px solid var(--border);color:var(--text-secondary);font-weight:500;">{sub}</span>
                ))}
                {cat.subs.size > 3 && <span style="font-size:0.7rem;color:var(--muted);padding:2px 4px;">+{cat.subs.size - 3}</span>}
              </div>
            )}
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- ADVANTAGE -->
  <section class="section" style="padding-top:0">
    <div class="container">
      <div class="adv-grid">
        <div>
          <span class="section-title">The CoursesWyn Advantage</span>
          <h2 class="section-heading" style="max-width:450px">Expert reviews make the difference.</h2>
        </div>
        <p style="color:var(--text-secondary);font-size:1rem;line-height:1.7;margin:0">
          Not just coupon hunters. CoursesWyn attracts learners who are more likely to study, finish, and review the course. <a href="/how-to-redeem-coupon" style="color:var(--brand);text-decoration:underline">How to redeem &rarr;</a>
        </p>
      </div>

      <div class="adv-cards">
        <div class="adv-card">
          <div class="adv-icon" style="color:var(--brand)">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/></svg>
          </div>
          <h3>Browse before paying</h3>
          <p>Browse all deals with expert ratings and detailed reviews. Pay only for the courses worth your time and money.</p>
        </div>
        <div class="adv-card">
          <div class="adv-icon" style="color:#059669">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 12l2 2 4-4"/><path d="M12 2a10 10 0 1 0 10 10"/></svg>
          </div>
          <h3>Expert-curated picks</h3>
          <p>Every course reviewed by industry professionals. Our "Expert Pick" badges highlight the best AI, LLM, and agentic AI courses.</p>
        </div>
        <div class="adv-card">
          <div class="adv-icon" style="color:var(--brand)">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
          </div>
          <h3>Manually verified daily</h3>
          <p>Every coupon checked by hand, not a bot. Expired deals removed within hours. You only see working coupons.</p>
        </div>
        <div class="adv-card">
          <div class="adv-icon" style="color:#7c3aed">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>
          </div>
          <h3>Live coupon checks</h3>
          <p>CoursesWyn checks coupon validity close to real time. What you see is what works — no dead links.</p>
        </div>
      </div>

      <div class="adv-banner">
        <div>
          <div style="display:flex;align-items:center;gap:0.5rem;margin-bottom:0.5rem">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
            <span style="font-size:0.75rem;font-weight:700;text-transform:uppercase;letter-spacing:0.05em;color:var(--brand)">Built for learners and instructors</span>
          </div>
          <p style="font-size:1rem;font-weight:600;color:var(--text);margin:0;line-height:1.5">Better learners attract better courses.<br>Better courses make CoursesWyn stronger.</p>
        </div>
        <a href="/udemy-coupon-code" class="btn btn-primary" style="padding:0.75rem 2rem;border-radius:999px;white-space:nowrap;margin-top:1rem;display:inline-flex;width:auto">Browse All Free Coupons 100% OFF &rarr;</a>
      </div>
    </div>
  </section>

  <!-- BLOG HIGHLIGHTS -->
  <section class="section">
    <div class="container">
      <div style="display:flex;align-items:flex-end;justify-content:space-between;margin-bottom:0.25rem;flex-wrap:wrap;gap:0.5rem">
        <div>
          <span class="section-title">Expert Guides</span>
          <h2 class="section-heading" style="margin-bottom:0.25rem">Latest from the blog</h2>
          <p style="color:var(--text-secondary);font-size:0.85rem;line-height:1.5;margin:0;max-width:480px">Detailed reviews and rankings to help you choose the right Udemy course for your career goals.</p>
        </div>
        <a href="/blog" style="color:var(--brand);font-size:0.85rem;font-weight:600;text-decoration:none;white-space:nowrap">Read all guides &rarr;</a>
      </div>
      <div class="blog-mini-grid">
        {recentPosts.map((post: any) => {
          const postDate = post.data.updatedAt || post.data.pubDate;
          const category = post.data.tags?.[0] || "Guide";
          const postSlug = post.id.replace(/\/index$/, '');
          return (
            <a href={`/blog/${postSlug}`} class="blog-mini-card">
              <div class="blog-mini-body">
                <span class="blog-mini-tag">{category}</span>
                <h3>{post.data.title}</h3>
                {post.data.description && <p class="blog-mini-desc">{post.data.description}</p>}
                <span class="blog-mini-date">{formatDateWIB(postDate)}</span>
              </div>
            </a>
          );
        })}
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="section">
    <div class="container">
      <div class="faq-header">
        <div>
          <span class="section-title">Straight Answers</span>
          <h2 class="section-heading">What learners ask before enrolling</h2>
        </div>
        <p style="color:var(--text-secondary);font-size:1rem;line-height:1.6;margin:0">Short version: browse free, find coupons, enroll on Udemy. <a href="/udemy-coupons-guide" style="color:var(--brand);text-decoration:underline">Read the full guide &rarr;</a></p>
      </div>
      <div class="faq-grid">
        {[
          { q: "Can free users use the coupons?", a: "Free users can browse every deal. All coupons are unlocked — no premium paywall required. Click any deal and enroll directly on Udemy." },
          { q: "Where do the coupons come from?", a: "From CoursesWyn proprietary discovery systems and instructor submissions. Every coupon is manually verified before listing. We do not accept auto-submitted or unverified coupons." },
          { q: "Why would instructors submit coupons here?", a: `Public feeds burn coupons quickly. CoursesWyn brings them learners who are more likely to study, finish, and leave a review. <a href="${instructorUrl}" target="_blank" rel="noopener noreferrer" style="color:var(--brand);text-decoration:underline">Learn more about Udemy coupon targeting.</a>` },
          { q: "Do coupons always work?", a: "CoursesWyn checks coupon validity close to real time. However, Udemy can still expire a coupon when its enrollment limit is reached. We remove expired deals within hours." },
          { q: "Do I need a Udemy account?", a: "Yes. CoursesWyn helps you find and open the coupon. Enrollment and course access happen on Udemy. A free Udemy account is all you need." },
          { q: "How to redeem a coupon?", a: `<a href="/how-to-redeem-coupon" style="color:var(--brand);text-decoration:underline">Follow our step-by-step redemption guide &rarr;</a>` },
          { q: "Are Udemy courses really free with these coupons?", a: "Yes. All coupons listed on CoursesWyn give you 100% OFF the original price — you pay nothing. No hidden fees, no trial required. Just click and enroll with a free Udemy account." },
          { q: "What are the best free Udemy courses in 2026?", a: `Top-rated categories include AI and machine learning, Python programming, web development with React and Next.js, data science, and cloud computing. <a href="/blog/best-udemy-courses-2026" style="color:var(--brand);text-decoration:underline">See our full rankings &rarr;</a>` }
        ].map((faq) => (
          <div class="faq-item">
            <div class="faq-question">
              <span>{faq.q}</span>
            </div>
            <div class="faq-answer"><p set:html={faq.a} /></div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="cta-section">
    <div class="container">
      <div class="cta-box">
        <h2>Go Unlimited with Udemy Personal Plan<br>26,000+ Premium Courses, One Subscription</h2>
        <div class="cta-actions">
          <a href="https://trk.udemy.com/c/6564357/3775958/39854" target="_blank" rel="noopener noreferrer" class="btn btn-primary btn-lg" style="border-radius:999px;padding:1rem 2.5rem">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M11.562 3.266a.5.5 0 0 1 .876 0L15.39 8.87a1 1 0 0 0 1.516.294L21.183 5.5a.5.5 0 0 1 .798.519l-2.834 10.246a1 1 0 0 1-.956.734H5.81a1 1 0 0 1-.957-.734L2.02 6.02a.5.5 0 0 1 .798-.519l4.276 3.664a1 1 0 0 0 1.516-.294z"/><path d="M5 21h14"/></svg>
            Start Free Trial
          </a>
        </div>
        <div class="cta-features">
          <span><span class="cta-dot"></span>26,000+ courses</span>
          <span><span class="cta-dot"></span>7-day free trial</span>
          <span><span class="cta-dot"></span>From $20/month</span>
        </div>
      </div>
    </div>
  </section>
</Layout>
