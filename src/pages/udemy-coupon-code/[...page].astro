---
export const prerender = true;
import Layout from "../../layouts/Layout.astro";
import LedgerRow from "../../components/LedgerRow.astro";
import AuthorBox from "../../components/AuthorBox.astro";
import { readDeals } from "../../lib/store";
import { toISODateWIB } from "../../lib/date";
import { slugifyCategory } from "../../lib/utils";

export async function getStaticPaths({ paginate }: { paginate: any }) {
  const allDeals = await readDeals();
  return paginate(allDeals, { pageSize: 36 });
}

const { page } = Astro.props as { page: any };
const allDeals = await readDeals();
const now = new Date();

const currentItems = page.data;
const currentPage = page.currentPage;
const totalPages = page.lastPage;
const startIndex = page.start;

const activeDeals = allDeals.filter((d: any) => !d.expiresAt || new Date(d.expiresAt) > now);
const totalActive = activeDeals.length;

const todayStr = toISODateWIB(now);
const todayAdded = allDeals.filter((d: any) => {
  const raw = d.updatedAt || d.createdAt || '';
  if (!raw) return false;
  return toISODateWIB(new Date(raw)) === todayStr;
}).length;

const decodeHtml = (str: string) =>
  str.replace(/&amp;/g, '&').replace(/&lt;/g, '<').replace(/&gt;/g, '>').replace(/&quot;/g, '"').replace(/&#39;/g, "'");

const topicCounts: Record<string, number> = {};
for (const d of allDeals) {
  const sub = d.subcategory ? decodeHtml(d.subcategory) : '';
  if (sub) {
    topicCounts[sub] = (topicCounts[sub] || 0) + 1;
  }
}
const totalTopics = Object.keys(topicCounts).length;

const selectedLanguage = Astro.url.searchParams.get('language');

const siteUrl = "https://courseswyn.com";
const baseDealsUrl = `${siteUrl}/udemy-coupon-code`;
const langQuery = selectedLanguage ? `?language=${encodeURIComponent(selectedLanguage)}` : "";
const currentUrl = currentPage === 1 ? `${baseDealsUrl}${langQuery}` : `${baseDealsUrl}/${currentPage}${langQuery}`;
const prevUrl = page.url.prev ? `${page.url.prev}${langQuery}` : null;
const nextUrl = page.url.next ? `${page.url.next}${langQuery}` : null;

const currentDate = new Date();
const monthNames = ["January","February","March","April","May","June",
  "July","August","September","October","November","December"];
const currentMonth = monthNames[currentDate.getMonth()];
const currentYear = currentDate.getFullYear();

const lastChecked = new Intl.DateTimeFormat('en-US', { timeZone: 'Asia/Jakarta', month: 'long', day: 'numeric', year: 'numeric' }).format(now);

// Totals use the full catalog (expired included) — listings show checkable deals
const totalCoupons = allDeals.length;
const catsSet = new Set<string>();
allDeals.forEach((d: any) => { if (d.category) catsSet.add((d.category as string).trim()); });
const categories = Array.from(catsSet).sort();
const latestUpdate = allDeals.reduce((m: number, d: any) => {
  const t = new Date(d.updatedAt || d.createdAt || 0).getTime();
  return t > m ? t : m;
}, 0);
const diffMin = Math.floor((now.getTime() - latestUpdate) / 60000);
const lastVerified = diffMin < 1 ? "Just now" : diffMin < 60 ? `${diffMin}m ago` : Math.floor(diffMin / 60) < 24 ? `${Math.floor(diffMin / 60)}h ago` : `${Math.floor(diffMin / 1440)}d ago`;

const discountPcts = allDeals
  .filter((d: any) => d.price && d.originalPrice && d.originalPrice > d.price)
  .map((d: any) => Math.round(100 - (d.price / d.originalPrice) * 100));
const maxDiscountPct = discountPcts.length ? Math.max(...discountPcts) : 95;

const pageTitle = currentPage === 1
  ? `All ${totalCoupons.toLocaleString()} Udemy Coupon Codes (${currentMonth} ${currentYear}) — Complete Catalog | CoursesWyn`
  : `Udemy Coupon Codes — Page ${currentPage} of ${totalPages} (Complete Catalog ${currentMonth} ${currentYear}) | CoursesWyn`;

const pageDescription = currentPage === 1
  ? `Browse the complete catalog of ${totalCoupons.toLocaleString()} verified Udemy coupon codes for ${currentMonth} ${currentYear}. Every discount in one paginated directory — save up to ${maxDiscountPct}%. Updated daily.`
  : `Page ${currentPage} of ${totalPages} in the complete Udemy coupon catalog — more verified discount codes, savings up to ${maxDiscountPct}%. Updated daily.`;

const pageKeywords = [
  "udemy coupon code", "udemy coupon codes", "free udemy coupon", "udemy discount",
  "udemy free course", "udemy promo code", "udemy deals", "udemy sale",
  "coupon code", "udemy courses", "verified udemy coupons"
].join(", ");

const currentDeals = currentItems.filter((d: any) => d.price > 0 || d.price == null);

const featuredDeals = activeDeals
  .filter((d: any) => d.rating && d.rating >= 4.5)
  .sort((a: any, b: any) => (b.rating || 0) - (a.rating || 0))
  .slice(0, 6);

const catCounts = allDeals.reduce<Record<string, number>>((acc, d: any) => {
  const c = (d.category || "Other").trim();
  if (c) acc[c] = (acc[c] || 0) + 1;
  return acc;
}, {});
const sortedCats = Object.entries(catCounts).sort((a, b) => b[1] - a[1]);
const subcatsByCat = allDeals.reduce<Record<string, Set<string>>>((acc, d: any) => {
  const c = (d.category || "Other").trim();
  const s = (d.subcategory || "").trim();
  if (c && s) { (acc[c] = acc[c] || new Set()).add(s); }
  return acc;
}, {});



const faqs = [
  { q: "Are these Udemy coupon codes legitimate and safe?", a: "Yes! All Udemy coupons listed on CoursesWyn are 100% legitimate. We source codes directly from Udemy's official affiliate program and verified course instructors. Each coupon is manually tested by our team before listing." },
  { q: "How do I use a Udemy coupon code?", a: "Browse our deals page and find a course you want, click the deal card, and you'll be redirected to Udemy with the coupon pre-applied. Verify the discounted price at checkout and click 'Enroll Now'." },
  { q: "How often are new Udemy coupons added?", a: "We update our database every single day with fresh Udemy coupon codes. Bookmark this page and check back regularly." },
  { q: "Do discounted Udemy courses include certificates?", a: "Yes! When you enroll in a Udemy course using our coupons, you receive the same certificate as students who paid full price — lifetime access included." },
  { q: "Why do some Udemy coupons say 'This coupon has expired'?", a: "Udemy coupons have two expiration triggers: time limit (usually 3-7 days) OR redemption limit (typically 1,000 students). Once either limit is hit, the coupon stops working. Return to CoursesWyn — we refresh our listings multiple times daily." },
  { q: "Is there a catch? Why do instructors offer coupons?", a: "No catch! Instructors offer coupons for strategic marketing: building student numbers, generating reviews for new courses, and boosting algorithm rankings. You get premium education at a discount — a win-win." },
  { q: "Can I use a Udemy coupon code on the Udemy mobile app?", a: "No. Udemy coupon codes can only be applied in a desktop or mobile browser at udemy.com checkout. The Udemy mobile app does not accept coupon codes — open the course page in your browser and log in there instead." },
  { q: "Can I get a refund on a Udemy course if I used a coupon?", a: "Yes. Udemy's 30-day money-back guarantee applies even when you enroll with a coupon code. Request the refund from your Purchase History within 30 days — it returns to your original payment method." },
  { q: "How much can I actually save with Udemy coupons?", a: "Most course coupons bring the price down to $9.99–$19.99 from a retail price of $49.99–$199.99 — savings of up to 95%. Many 100% off coupons are also available, though they sell out fast." },
];

const jsonLd: any = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "BreadcrumbList",
      "@id": `${currentUrl}#breadcrumb`,
      "itemListElement": [
        { "@type": "ListItem", "position": 1, "name": "Home", "item": siteUrl },
        { "@type": "ListItem", "position": 2, "name": "All Coupons", "item": baseDealsUrl },
        ...(currentPage > 1 ? [{ "@type": "ListItem", "position": 3, "name": `Page ${currentPage}`, "item": currentUrl }] : [])
      ]
    },
    {
      "@type": ["WebPage", "CollectionPage"],
      "@id": currentUrl,
      "url": currentUrl,
      "name": pageTitle,
      "description": pageDescription,
      "keywords": pageKeywords,
      "isPartOf": { "@id": `${siteUrl}#website` },
      "breadcrumb": { "@id": `${currentUrl}#breadcrumb` },
      "datePublished": currentDate.toISOString().split('T')[0],
      "dateModified": currentDate.toISOString().split('T')[0],
      "inLanguage": "en-US",
      "mainEntity": { "@id": `${currentUrl}#itemlist` },
      "author": { "@type": "Person", "name": "Andrew Derek", "url": siteUrl },
    },
    {
      "@type": "ItemList",
      "@id": `${currentUrl}#itemlist`,
      "name": `Verified Udemy Coupons — ${currentMonth} ${currentYear}`,
      "numberOfItems": totalActive,
      "itemListElement": currentItems.slice(0, 20).map((d: any, i: number) => ({
        "@type": "ListItem",
        "position": startIndex + i + 1,
        "url": `${siteUrl}/coupon/${d.slug}`,
        "item": {
          "@type": "Course",
          "name": d.title,
          "url": `${siteUrl}/coupon/${d.slug}`,
          "image": d.image,
          "provider": { "@type": "Organization", "name": "Udemy", "url": "https://www.udemy.com" },
          "offers": {
            "@type": "Offer",
            "price": String(d.price ?? "0"),
            "priceCurrency": "USD",
            "availability": "https://schema.org/InStock",
          },
        },
      })),
    },
    ...(currentPage === 1 ? [{
      "@type": "FAQPage",
      "@id": `${baseDealsUrl}#faq`,
      "mainEntity": faqs.map((f: any) => ({
        "@type": "Question",
        "name": f.q,
        "acceptedAnswer": { "@type": "Answer", "text": f.a },
      })),
    },
    {
      "@type": "HowTo",
      "@id": `${baseDealsUrl}#howto`,
      "name": "How to Redeem a Udemy Coupon Code",
      "description": "Four simple steps to redeem a Udemy coupon code and unlock your discount.",
      "totalTime": "PT5M",
      "step": [
        { "@type": "HowToStep", "position": 1, "name": "Choose your course", "text": "Browse the deals on this page and pick a course you want. Check the rating, review count, and curriculum before you enroll." },
        { "@type": "HowToStep", "position": 2, "name": "Click the deal card", "text": "Click the course card to open its deal page on CoursesWyn, then click the coupon or 'Get deal' button to jump to Udemy with the code pre-applied." },
        { "@type": "HowToStep", "position": 3, "name": "Verify the discounted price", "text": "On Udemy, check that the coupon discount is reflected in the final price before you pay. For codes entered by hand, paste the code into the coupon field and click Apply." },
        { "@type": "HowToStep", "position": 4, "name": "Enroll now", "text": "Click 'Enroll Now' to confirm your enrollment. Your course, certificate, and lifetime access are added immediately." },
      ],
    }] : []),
  ],
};

const pageItems: ({ type: 'page'; value: number } | { type: 'ellipsis'; id: string })[] = [];
const windowSize = 2;
let lastAdded = 0;
for (let p = 1; p <= totalPages; p++) {
  const inWindow = p >= currentPage - windowSize && p <= currentPage + windowSize;
  if (p === 1 || p === totalPages || inWindow) {
    if (lastAdded > 0 && p - lastAdded > 1) {
      pageItems.push({ type: 'ellipsis', id: `gap-${p}` });
    }
    pageItems.push({ type: 'page', value: p });
    lastAdded = p;
  }
}
---

<Layout
  title={pageTitle}
  description={pageDescription}
  canonical={currentUrl}
  hideBreadcrumbs={true}
>
  <Fragment slot="head">
    {prevUrl && <link rel="prev" href={prevUrl} />}
    {nextUrl && <link rel="next" href={nextUrl} />}
    {currentPage > 1 && <>
      <meta name="robots" content="noindex, follow" />
      <meta name="googlebot" content="noindex, follow" />
      <meta name="bingbot" content="noindex, follow" />
    </>}
    <meta name="keywords" content={pageKeywords} />
    <meta name="author" content="Andrew Derek" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;600;700&family=IBM+Plex+Mono:wght@400;500;600&display=swap" rel="stylesheet" />
  </Fragment>

  <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />

  <main>
    <div class="container">

      {currentPage === 1 ? (
        <section style={{ padding: "4rem 1rem 3rem", textAlign: "center", position: "relative", overflow: "hidden" }}>
          <div style={{ position: "absolute", top: "20%", left: "5%", width: "280px", height: "280px", background: "var(--brand)", filter: "blur(120px)", opacity: 0.07, borderRadius: "50%", pointerEvents: "none" }} />
          <div style={{ position: "absolute", bottom: "10%", right: "5%", width: "240px", height: "240px", background: "#CF6F59", filter: "blur(100px)", opacity: 0.07, borderRadius: "50%", pointerEvents: "none" }} />
          <div style={{ position: "relative", zIndex: 1, maxWidth: "900px", margin: "0 auto" }}>
            <div style={{ display: "inline-flex", alignItems: "center", gap: "0.5rem", background: "var(--brand-soft)", padding: "0.5rem 1rem", borderRadius: "9999px", marginBottom: "1.5rem", fontSize: "0.875rem", color: "var(--brand)", fontWeight: 600 }}>
              <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z" /></svg>
              Checked Daily — {currentMonth} {currentYear}
            </div>
            <h1 style={{ fontSize: "clamp(2rem, 5vw, 3.5rem)", fontWeight: 800, lineHeight: 1.1, maxWidth: "900px", margin: "0 auto 1.5rem" }}>
              <span style={{ background: "linear-gradient(135deg, var(--brand) 0%, #0B6B4A 100%)", WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent", backgroundClip: "text" }}>All {totalCoupons.toLocaleString()} Udemy Coupon Codes</span>
              <br />
              <span style={{ background: "linear-gradient(135deg, var(--text) 0%, var(--muted) 100%)", WebkitBackgroundClip: "text", WebkitTextFillColor: "transparent", backgroundClip: "text" }}>Every Discount, {currentMonth} {currentYear}</span>
            </h1>
            <p style={{ fontSize: "clamp(1rem, 2.5vw, 1.25rem)", color: "var(--muted)", maxWidth: "650px", margin: "0 auto 2.5rem", lineHeight: 1.7 }}>
              The complete CoursesWyn catalog: <strong style={{ color: "var(--text)" }}>{totalCoupons.toLocaleString()} verified Udemy coupon codes</strong> in one paginated directory. Save up to <strong style={{ color: "var(--brand)" }}>{maxDiscountPct}%</strong> — looking for today's highlights instead? Start at the <strong style={{ color: "var(--text)" }}>homepage picks</strong>.
            </p>
            <div class="hero-cta" style={{ display: "flex", gap: "0.75rem", justifyContent: "center", flexWrap: "wrap" }}>
              <a href="#latest" class="btn btn-primary btn-lg" style={{ borderRadius: "999px", padding: "1rem 2.5rem" }}>Browse All Coupons</a>
              <a href="/categories" class="btn btn-ghost btn-lg" style={{ borderRadius: "999px", padding: "1rem 2.5rem", color: "var(--brand)", borderColor: "var(--brand)" }}>Shop by Category</a>
            </div>
            <p style={{ fontSize: "0.82rem", color: "var(--muted)", marginTop: "1.25rem" }}>Compiled &amp; checked by <a href="/about" style={{ color: "var(--brand)", fontWeight: 600 }}>Andrew Derek</a> &middot; Offers last checked {lastChecked} &middot; <a href="/how-we-test-and-verify-udemy-coupons" style={{ color: "var(--brand)", fontWeight: 600 }}>How we verify</a></p>
          </div>
        </section>
      ) : (
        <div style={{ background: "var(--card)", borderBottom: "1px solid var(--border)", padding: "1.5rem 0", textAlign: "center" }}>
          <div class="container">
            <h1 style={{ fontSize: "1.5rem", fontWeight: 700, color: "var(--text)", margin: "0 0 0.25rem" }}>
              Udemy Coupon Codes & Discounts — Page {currentPage}
            </h1>
            <p style={{ color: "var(--muted)", margin: 0, fontSize: "0.9rem" }}>
              Showing {startIndex + 1}–{Math.min(startIndex + currentItems.length, totalCoupons)} of {totalCoupons.toLocaleString()} verified coupons
            </p>
          </div>
        </div>
      )}

      <section style={{ paddingBottom: "2rem", marginTop: currentPage === 1 ? "-1rem" : "1.5rem" }}>
        <div class="container">
          <div class="stat-grid" style={{ gridTemplateColumns: "repeat(4,1fr)", maxWidth: "850px" }}>
            <div class="stat-box"><div class="stat-icon"><span class="stat-value">{todayAdded}</span></div><div class="stat-label">Added Today</div></div>
            <div class="stat-box"><div class="stat-icon"><span class="stat-value">{totalCoupons.toLocaleString()}</span></div><div class="stat-label">Total Coupons</div></div>
            <div class="stat-box"><div class="stat-icon"><span class="stat-value">{categories.length}</span></div><div class="stat-label">Categories</div></div>
            <div class="stat-box"><div class="stat-icon"><span class="stat-value" style={{ fontSize: "1.1rem" }}>{lastVerified}</span></div><div class="stat-label">Verified</div></div>
          </div>
        </div>
      </section>

      <section style={{ background: "var(--card)", borderTop: "1px solid var(--border)", padding: "3rem 1.5rem 2rem", textAlign: "center" }}>
        <div class="container" style={{ maxWidth: "1280px" }}>
          <div style={{ display: "inline-flex", alignItems: "center", gap: "0.5rem", background: "rgba(11,107,74,0.1)", padding: "0.35rem 0.85rem", borderRadius: "20px", marginBottom: "0.75rem", fontSize: "0.75rem", fontWeight: 700, color: "var(--brand)", textTransform: "uppercase", letterSpacing: "0.06em" }}>
            Browse by Topic
          </div>
          <h2 style={{ fontSize: "1.6rem", fontWeight: 800, color: "var(--text)", margin: "0 0 0.35rem" }}>Udemy Coupon Categories</h2>
          <p style={{ fontSize: "0.85rem", color: "var(--muted)", margin: "0 auto 1rem", maxWidth: "550px", lineHeight: 1.6 }}>
            Jump straight to coupons in {categories.length} fields — from coding and AI to design, business and more.
          </p>
          <div style={{ display: "flex", flexWrap: "wrap", gap: "0.4rem", justifyContent: "center" }}>
            <a href="/udemy-coupon-code" style={{ fontSize: "0.78rem", fontWeight: 600, color: "#FFFFFF", textDecoration: "none", padding: "3px 10px", borderRadius: "100px", border: "1px solid var(--brand)", background: "var(--brand)" }}>All</a>
            {categories.slice(0, 15).map((cat: string) => (
              <a href={`/categories/${slugifyCategory(cat)}`} style={{ fontSize: "0.78rem", fontWeight: 600, color: "var(--muted)", textDecoration: "none", padding: "3px 10px", borderRadius: "100px", border: "1px solid var(--border)", background: "var(--bg)" }}>{cat}</a>
            ))}
          </div>
        </div>
      </section>

      {currentPage === 1 && (
      <section aria-label="Featured Udemy coupons" style={{ padding: "3rem 1.5rem 0", maxWidth: "1280px", margin: "0 auto" }}>
        <div style={{ textAlign: "center", marginBottom: "1.5rem" }}>
          <div style={{ display: "inline-flex", alignItems: "center", gap: "0.5rem", background: "rgba(11,107,74,0.1)", padding: "0.35rem 0.85rem", borderRadius: "20px", marginBottom: "0.75rem", fontSize: "0.75rem", fontWeight: 700, color: "var(--brand)", textTransform: "uppercase", letterSpacing: "0.06em" }}>Top Picks</div>
          <h2 style={{ fontSize: "1.6rem", fontWeight: 800, color: "var(--text)", margin: "0 0 0.3rem" }}>Highest-Rated Udemy Courses With Coupons</h2>
          <p style={{ fontSize: "0.9rem", color: "var(--muted)", maxWidth: "600px", margin: "0 auto", lineHeight: 1.6 }}>4.5★ and up, all with a working coupon attached — refreshed as new deals land.</p>
        </div>
        <div style={{ display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: "1.25rem" }} class="deals-grid">
          {featuredDeals.map((deal: any) => <LedgerRow deal={deal} variant="grid" />)}
        </div>
      </section>
      )}

      <section id="latest" style={{ padding: "3rem 1.5rem 4rem", maxWidth: "1280px", margin: "0 auto" }}>
        <div style={{ textAlign: "center", marginBottom: "1.5rem" }}>
          <div style={{ display: "inline-flex", alignItems: "center", gap: "0.5rem", background: "rgba(11,107,74,0.1)", padding: "0.35rem 0.85rem", borderRadius: "20px", marginBottom: "0.75rem", fontSize: "0.75rem", fontWeight: 700, color: "var(--brand)", textTransform: "uppercase", letterSpacing: "0.06em" }}>Fresh Coupons</div>
          <h2 style={{ fontSize: "1.6rem", fontWeight: 800, color: "var(--text)", margin: "0 0 0.3rem" }}>
            Every Coupon on This Page{currentPage > 1 ? ` — Page ${currentPage}` : ""}
          </h2>
          <p style={{ fontSize: "0.9rem", color: "var(--muted)", maxWidth: "650px", margin: "0 auto", lineHeight: 1.6 }}>
            {currentDeals.length} verified Udemy coupons below ({currentMonth} {currentYear} catalog) with savings up to {maxDiscountPct}% — programming, AI, business, design and more.
          </p>
        </div>
        {selectedLanguage && (
          <div style={{ marginBottom: "1rem", textAlign: "center" }}>
            <a href="/udemy-coupon-code" style={{ display: "inline-flex", alignItems: "center", gap: "0.4rem", padding: "0.3rem 0.75rem", background: "var(--card)", border: "1px solid var(--border)", borderRadius: "6px", color: "var(--muted)", textDecoration: "none", fontSize: "0.8rem", fontWeight: 600 }}>✕ Clear Filter</a>
          </div>
        )}
        <div style={{ display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: "1.25rem" }} class="deals-grid">
          {currentDeals.map((deal: any) => <LedgerRow deal={deal} variant="grid" />)}
        </div>
        <p style={{ textAlign: "center", fontSize: "0.8rem", color: "var(--muted)", marginTop: "1rem" }}>{currentDeals.length} coupons on this page — each one checked before listing.</p>

        {totalPages > 1 && (
          <nav aria-label="Pagination" style={{ display: "flex", alignItems: "center", justifyContent: "center", gap: "0.75rem", padding: "1.5rem 0", marginTop: "1.5rem", borderTop: "1px solid var(--border)" }}>
            {prevUrl ? (
              <a href={prevUrl} style={{ fontSize: "0.85rem", fontWeight: 700, color: "var(--brand)", textDecoration: "none", padding: "0.4rem 0.85rem", borderRadius: "6px", border: "1px solid var(--border)" }}>← Prev</a>
            ) : (
              <span style={{ fontSize: "0.85rem", fontWeight: 700, color: "var(--muted)", opacity: 0.4, padding: "0.4rem 0.85rem" }}>← Prev</span>
            )}
            <div style={{ display: "flex", alignItems: "center", gap: "4px" }}>
              {pageItems.map((item: any) => {
                if (item.type === 'ellipsis') {
                  return <span style={{ fontSize: "0.85rem", color: "var(--muted)", width: "34px", textAlign: "center" }}>...</span>;
                }
                const isActive = item.value === currentPage;
                const href = item.value === 1 ? `/udemy-coupon-code${langQuery}` : `/udemy-coupon-code/${item.value}${langQuery}`;
                return isActive
                  ? <span style={{ fontSize: "0.85rem", fontWeight: 700, color: "#FFFFFF", width: "34px", height: "34px", display: "flex", alignItems: "center", justifyContent: "center", borderRadius: "6px", border: "1px solid var(--brand)", background: "var(--brand)" }}>{item.value}</span>
                  : <a href={href} style={{ fontSize: "0.85rem", fontWeight: 700, color: "var(--text)", textDecoration: "none", width: "34px", height: "34px", display: "flex", alignItems: "center", justifyContent: "center", borderRadius: "6px", border: "1px solid var(--border)" }}>{item.value}</a>;
              })}
            </div>
            {nextUrl ? (
              <a href={nextUrl} style={{ fontSize: "0.85rem", fontWeight: 700, color: "var(--brand)", textDecoration: "none", padding: "0.4rem 0.85rem", borderRadius: "6px", border: "1px solid var(--border)" }}>Next →</a>
            ) : (
              <span style={{ fontSize: "0.85rem", fontWeight: 700, color: "var(--muted)", opacity: 0.4, padding: "0.4rem 0.85rem" }}>Next →</span>
            )}
          </nav>
        )}
      </section>

      <section style={{ padding: "4rem 1rem", background: "linear-gradient(135deg, rgba(11,107,74,0.04) 0%, transparent 100%)" }}>
        <div class="container" style={{ maxWidth: "1280px" }}>
          <div style={{ textAlign: "center", marginBottom: "2.5rem" }}>
            <div style={{ display: "inline-flex", alignItems: "center", gap: "0.5rem", background: "rgba(11,107,74,0.1)", padding: "0.35rem 0.85rem", borderRadius: "20px", marginBottom: "1rem", fontSize: "0.75rem", fontWeight: 700, color: "var(--brand)", textTransform: "uppercase", letterSpacing: "0.06em" }}>How We Verify</div>
            <h2 style={{ fontSize: "clamp(1.5rem, 4vw, 2rem)", fontWeight: 800, color: "var(--text)", marginBottom: "0.75rem" }}>
              How CoursesWyn keeps these Udemy coupons working
            </h2>
            <p style={{ color: "var(--muted)", fontSize: "1rem", maxWidth: "700px", margin: "0 auto", lineHeight: 1.6 }}>
              Every code below is re-checked against Udemy on a loop. Dead codes drop out on their own — fresh ones stream in all day.
            </p>
          </div>
          <div style={{ background: "var(--card)", border: "1px solid var(--border)", borderRadius: "12px", padding: "2rem" }}>
            <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(280px, 1fr))", gap: "1.5rem" }}>
              <div>
                <div style={{ display: "flex", alignItems: "center", gap: "0.5rem", marginBottom: "0.5rem" }}>
                  <span style={{ fontWeight: 700, fontSize: "0.95rem", color: "var(--text)" }}>⏱ Live re-checking</span>
                </div>
                <p style={{ color: "var(--muted)", fontSize: "0.9rem", lineHeight: 1.6, margin: 0 }}>
                  Since <a href="https://teach.udemy.com/update-for-instructors-improving-free-coupons-better-targeting-higher-quality-learning/" rel="noopener" target="_blank" style={{ color: "var(--brand)", fontWeight: 600 }}>Udemy tightened coupon rules</a>, most codes cap at a handful of redemptions or die within days. Our checker retires the dead ones automatically, so this list only ever shows codes with a pulse.
                </p>
              </div>
              <div>
                <div style={{ display: "flex", alignItems: "center", gap: "0.5rem", marginBottom: "0.5rem" }}>
                  <span style={{ fontWeight: 700, fontSize: "0.95rem", color: "var(--text)" }}>✓ Open method</span>
                </div>
                <p style={{ color: "var(--muted)", fontSize: "0.9rem", lineHeight: 1.6, margin: "0 0 0.75rem" }}>
                  No black box: each listing shows its discount math, rating and last-checked time. Prefer browsing by field? Try <a href="/categories" style={{ color: "var(--brand)", fontWeight: 600 }}>categories</a>, <a href="/topics" style={{ color: "var(--brand)", fontWeight: 600 }}>topics</a> or <a href="/udemy-coupons-today" style={{ color: "var(--brand)", fontWeight: 600 }}>today's fresh batch</a>.
                </p>
              </div>
            </div>
            <div style={{ marginTop: "1.5rem", paddingTop: "1.25rem", borderTop: "1px solid var(--border)" }}>
              <p style={{ color: "var(--muted)", fontSize: "0.85rem", lineHeight: 1.7, margin: 0 }}>
                <strong style={{ color: "var(--text)" }}>Affiliate note:</strong> CoursesWyn earns a small commission when you enroll through our links — it funds the checking pipeline and costs you nothing. Commissions never decide ranking. <a href="/affiliate-disclosure" style={{ color: "var(--brand)", textDecoration: "underline" }}>Full disclosure</a>
              </p>
            </div>
          </div>
        </div>
      </section>

      {currentPage === 1 && (
      <section style={{ padding: "4rem 1rem", background: "linear-gradient(180deg, rgba(11,107,74,0.05) 0%, rgba(11,107,74,0.015) 100%)" }}>
        <div class="container" style={{ maxWidth: "1400px" }}>
          <div style={{ textAlign: "center", marginBottom: "3rem" }}>
            <div style={{ display: "inline-flex", alignItems: "center", gap: "0.5rem", background: "linear-gradient(135deg, rgba(11,107,74,0.12) 0%, rgba(76,154,118,0.08) 100%)", padding: "0.5rem 1rem", borderRadius: "20px", marginBottom: "1rem", fontSize: "0.75rem", fontWeight: 700, color: "var(--muted)", textTransform: "uppercase", letterSpacing: "0.08em", border: "1px solid rgba(11,107,74,0.25)" }}>Learn More</div>
            <h2 style={{ fontSize: "clamp(1.6rem, 4vw, 2.2rem)", fontWeight: 800, color: "var(--text)", marginBottom: "0.75rem" }}>
              About <span style={{ color: "var(--brand)" }}>Udemy Coupon Codes</span> & Course Discounts
            </h2>
            <p style={{ color: "var(--muted)", fontSize: "1rem", maxWidth: "700px", margin: "0 auto", lineHeight: 1.6 }}>
              The short course on scoring verified Udemy discounts without the trial and error
            </p>
            <div style={{ display: "flex", gap: "1rem", justifyContent: "center", flexWrap: "wrap", marginTop: "1.5rem" }}>
              {[["✓", "Checked daily"], ["◉", `Up to ${maxDiscountPct}% off`], ["✦", "No CoursesWyn account"]].map(([icon, label]) => (
                <span style={{ display: "inline-flex", alignItems: "center", gap: "0.4rem", fontSize: "0.8rem", fontWeight: 600, color: "var(--text)", background: "var(--card)", border: "1px solid rgba(11,107,74,0.3)", borderRadius: "999px", padding: "0.45rem 1rem" }}><span style={{ color: "var(--brand)" }}>{icon}</span>{label}</span>
              ))}
            </div>
          </div>
          <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(300px, 1fr))", gap: "1.5rem" }}>
            {[
              ["01", "Where Do These Udemy Coupons Come From?", "Instructors mint coupon codes inside Udemy to fill launches, seasonal pushes and slow back-catalog titles. Each carries an expiry date and a redemption cap — which is why one course can swing from $89 to $12 and back within a week. CoursesWyn tracks those swings so you arrive while the code is alive."],
              ["02", "How Do I Claim One?", "Open any card on this page to reach its coupon page, then follow the claim link — Udemy opens with the code pre-applied and the cut visible at checkout. Hit Enroll and the course, certificate and lifetime access land in your library. The whole trip takes about two minutes."],
              ["03", "Why Trust This Directory?", "Because every entry carries its receipts: live discount math, learner rating, instructor history and a last-checked stamp. Thin, stale or review-starved courses never make the grid, and dead codes are pruned instead of left to rot."],
              ["04", "Do These Coupons Work Everywhere?", "Udemy is global, and so is this catalog — English-first with a growing Spanish shelf. Prices render in your local currency at checkout; the percentage cut travels with the code. If a code balks, it is almost always a spent cap, not your region."],
              ["05", "What Do I Actually Get?", "The identical seat full-price learners get: same video hours, same downloads, same instructor Q&A, same completion certificate for LinkedIn. Discount changes the receipt, nothing else."],
              ["06", "Which Udemy Fields Are Covered?", "All of them, weighted toward employable skills: development, IT and certifications, AI and data, business, design, marketing, office productivity and more. Start from the category pills above if you know your field."],
            ].map(([num, title, text]) => (
              <div class="about-card">
                <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: "1rem" }}>
                  <div style={{ width: "44px", height: "44px", borderRadius: "12px", background: "rgba(11,107,74,0.12)", display: "flex", alignItems: "center", justifyContent: "center", border: "1px solid rgba(11,107,74,0.25)", color: "var(--brand)", fontWeight: 800 }}>{num}</div>
                  <span style={{ fontSize: "0.8rem", fontWeight: 800, color: "rgba(11,107,74,0.4)", letterSpacing: "0.05em" }}>{num}</span>
                </div>
                <h3 style={{ fontSize: "1rem", fontWeight: 700, color: "var(--text)", marginBottom: "0.5rem" }}>{title}</h3>
                <p style={{ color: "var(--muted)", fontSize: "0.85rem", lineHeight: 1.7, margin: 0 }}>{text}</p>
              </div>
            ))}
          </div>
          <div style={{ marginTop: "3rem" }}>
            <div class="cta-box">
              <span class="cta-eyebrow">Udemy Personal Plan</span>
              <h2>One Subscription, <span style={{ color: "var(--brand)" }}>26,000+ Courses</span><br />Zero Coupon-Hunting</h2>
              <p class="cta-subtext">Prefer all-you-can-learn over single codes? One flat rate, certificates included, cancel anytime.</p>
              <div class="cta-actions">
                <a href="https://trk.udemy.com/c/6564357/3775958/39854" target="_blank" rel="noopener noreferrer" class="btn btn-primary btn-lg" style={{ borderRadius: "999px", padding: "1rem 2.5rem" }}>Start Free Trial</a>
              </div>
              <div class="cta-features">
                <span><span class="cta-dot"></span>26,000+ courses</span>
                <span><span class="cta-dot"></span>7-day free trial</span>
                <span><span class="cta-dot"></span>Cancel anytime</span>
                <span><span class="cta-dot"></span>From $20/month</span>
              </div>
            </div>
          </div>
        </div>
      </section>
      )}

      {currentPage === 1 && (
      <div class="cc-guide" aria-label="Udemy coupon guide">

        <section class="cc-guide-block" id="how-to-redeem">
          <h2 class="cc-section-title">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 10 4 15 9 20"/><path d="M20 4v7a4 4 0 0 1-4 4H4"/></svg>
            How to Redeem a Udemy Coupon Code
            <span class="cc-tag">Step-by-step</span>
          </h2>
          <p class="cc-guide-desc">Most coupons on this page apply automatically — click a deal and you're taken to Udemy with the discount pre-applied. For codes you enter by hand, follow these four steps:</p>
          <ol class="cc-steps">
            <li class="cc-step">
              <span class="cc-step-num">1</span>
              <div>
                <h3 class="cc-step-title">Choose your course</h3>
                <p class="cc-step-text">Browse the deals above and pick a course you want. Check the rating, review count, and curriculum before you enroll.</p>
              </div>
            </li>
            <li class="cc-step">
              <span class="cc-step-num">2</span>
              <div>
                <h3 class="cc-step-title">Open the deal page</h3>
                <p class="cc-step-text">Click the course card to open its deal page on CoursesWyn, then click the coupon or "Get deal" button. You'll be redirected to Udemy with the code already applied.</p>
              </div>
            </li>
            <li class="cc-step">
              <span class="cc-step-num">3</span>
              <div>
                <h3 class="cc-step-title">Verify the discounted price</h3>
                <p class="cc-step-text">On Udemy, confirm the coupon discount is reflected in the final price. For manual codes, paste the code into the coupon field at checkout and click Apply — Udemy shows the new total instantly.</p>
              </div>
            </li>
            <li class="cc-step">
              <span class="cc-step-num">4</span>
              <div>
                <h3 class="cc-step-title">Enroll now</h3>
                <p class="cc-step-text">Click "Enroll Now" to confirm. Your course, certificate of completion, and lifetime access are added immediately. Coupons can't be applied in the Udemy mobile app, so redeem in a browser.</p>
              </div>
            </li>
          </ol>
          <p class="cc-guide-note">Full walkthrough with screenshots: <a href="/how-to-redeem-coupon">How to Redeem a Udemy Coupon</a></p>
        </section>

        <section class="cc-guide-block" id="coupon-not-working">
          <h2 class="cc-section-title">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3Z"/><line x1="12" x2="12" y1="9" y2="13"/><line x1="12" x2="12.01" y1="17" y2="17"/></svg>
            Why Your Udemy Coupon Code Isn't Working
            <span class="cc-tag">Troubleshooting</span>
          </h2>
          <p class="cc-guide-desc">If a coupon shows the full price at checkout, one of these is usually the cause:</p>
          <ul class="cc-tips">
            <li class="cc-tip"><strong>The coupon expired.</strong> Most Udemy coupons last only 3-7 days or until 1,000 redemptions. Check the "Checked" column on this page — anything older may already be dead.</li>
            <li class="cc-tip"><strong>Course restrictions.</strong> Some codes only work on specific courses, categories, or first-time users. The deal page states any limitations.</li>
            <li class="cc-tip"><strong>Hidden spaces when pasting.</strong> Copy-paste often adds invisible spaces. Delete the code and type it manually.</li>
            <li class="cc-tip"><strong>Browser cache or cookies.</strong> Open the checkout page in incognito mode and try again.</li>
            <li class="cc-tip"><strong>The current sale price is lower.</strong> Udemy runs frequent sitewide sales (often 80-90% off). If the automatic sale price is already lower than the coupon, Udemy shows the lower price — that's not a bug.</li>
            <li class="cc-tip"><strong>Currency or region settings.</strong> Udemy prices by location. Changing your country or currency settings can change whether a code is accepted.</li>
          </ul>
          <p class="cc-guide-note">Still stuck? Our <a href="/how-to-redeem-coupon">redemption guide</a> covers every error you might see.</p>
        </section>

        <section class="cc-guide-block" id="find-more-coupons">
          <h2 class="cc-section-title">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14 4l6 6-11 11H3v-6L14 4z"/><path d="M14 4l6 6"/><path d="m9 9 6 6"/></svg>
            How to Find More Udemy Coupon Codes
            <span class="cc-tag">Save more</span>
          </h2>
          <ul class="cc-tips">
            <li class="cc-tip"><strong>Bookmark this page.</strong> We verify and refresh deals in near real time throughout the day — fresh codes drop constantly, not just in the morning.</li>
            <li class="cc-tip"><strong>Check during major sales.</strong> Black Friday, New Year, Back-to-School, and seasonal sales push discounts as high as 90-95% off.</li>
            <li class="cc-tip"><strong>Follow instructors directly.</strong> Many Udemy instructors share exclusive codes on their newsletters, YouTube, and personal sites.</li>
            <li class="cc-tip"><strong>Use the wishlist.</strong> Add courses you want to your Udemy wishlist — Udemy emails you the moment they go on sale.</li>
            <li class="cc-tip"><strong>Browse by category or topic.</strong> Use our <a href="/categories">categories</a> and <a href="/topics">topics</a> pages to find deals in the exact field you're learning.</li>
          </ul>
        </section>

        <section class="cc-guide-block" id="personal-plan">
          <h2 class="cc-section-title">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
            No Coupon Available? Try Udemy Personal Plan
            <span class="cc-tag">Unlimited access</span>
          </h2>
          <p class="cc-guide-text">Some of the courses we list no longer have a standalone coupon &mdash; Udemy's updated coupon policy means many codes auto-apply at checkout, cap at 10&ndash;100 redemptions, or expire within days. When a specific code is sold out, <strong>Udemy Personal Plan</strong> is often the better deal: one flat monthly price unlocks thousands of courses across programming, design, business and more &mdash; no per-course codes needed, cancel anytime.</p>
          <p class="cc-guide-text">It suits learners who take several courses per month. If you only want one course, keep an eye on this page instead &mdash; fresh codes drop every day and Udemy runs regular sitewide sales with discounts up to {maxDiscountPct}%.</p>
          <p class="cc-guide-note">Want to know if it's worth it? Read our <a href="/blog/udemy-personal-plan-review-2026/">Udemy Personal Plan review</a>.</p>
          <a class="cc-ppoffer" href="https://trk.udemy.com/c/6564357/3775958/39854" target="_blank" rel="noopener noreferrer sponsored">
            <span class="cc-ppoffer-label">Udemy Personal Plan</span>
            <span class="cc-ppoffer-cta">Get up to 80% off &rarr;</span>
          </a>
        </section>

        <section class="cc-guide-block" id="refund-policy">
          <h2 class="cc-section-title">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 12a9 9 0 1 0 9-9 9.75 9.75 0 0 0-6.74 2.74L3 8"/><path d="M3 3v5h5"/></svg>
            Udemy Refund Policy & Getting Help
            <span class="cc-tag">Good to know</span>
          </h2>
          <p class="cc-guide-text">Udemy offers a <strong>30-day money-back guarantee</strong> on most individual course purchases — even when you enrolled with a coupon. Request a refund from your Purchase History within 30 days; it returns to your original payment method (some payment types get Udemy credit instead, and Apple purchases are handled by Apple).</p>
          <p class="cc-guide-text">Note that <strong>Personal Plan subscriptions</strong> bought through Udemy.com carry no 30-day guarantee and are not refundable unless local law requires it. Refunds can't be requested from the mobile app.</p>
          <p class="cc-guide-text">For coupon, billing, or account issues, submit a ticket via the <a href="https://support.udemy.com" rel="noopener" target="_blank">official Udemy Help Center</a>. Udemy doesn't offer phone support in most regions — everything runs through their online ticket system.</p>
        </section>

      </div>
      )}

      {currentPage === 1 && (
        <section class="cc-category" aria-label="Browse Udemy coupon codes by category">
          <div class="cc-deals-header">
            <h2 class="cc-section-title">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/></svg>
              Browse Udemy Coupon Codes by Category
              <span class="cc-tag">{sortedCats.length} categories</span>
            </h2>
            <p class="cc-deals-desc">Find verified coupon codes in your field — from AI and coding to design and cloud computing.</p>
          </div>
          <nav class="cc-cat-grid" aria-label="Course categories">
            {sortedCats.slice(0, 12).map(([cat, count]) => {
              const slug = slugifyCategory(cat);
              const subCount = (subcatsByCat[cat] || new Set()).size;
              return (
                <a href={`/categories/${slug}`} class="cc-cat" title={`Browse ${cat} courses with Udemy coupons`}>
                  <div class="cc-cat-num">{count}</div>
                  <div class="cc-cat-label">{cat}</div>
                  <div class="cc-cat-count">{subCount} Subcategor{subCount === 1 ? "y" : "ies"}</div>
                </a>
              );
            })}
          </nav>
        </section>
      )}

      {currentPage === 1 && (
        <section aria-label="Udemy coupon code FAQs" style={{ background: "var(--bg)", padding: "4rem 1.5rem", maxWidth: "1280px", margin: "0 auto" }}>
          <div style={{ textAlign: "center", marginBottom: "2.5rem" }}>
            <div style={{ display: "inline-flex", alignItems: "center", gap: "0.5rem", background: "rgba(11,107,74,0.1)", padding: "0.35rem 0.85rem", borderRadius: "20px", marginBottom: "0.75rem", fontSize: "0.75rem", fontWeight: 700, color: "var(--brand)", textTransform: "uppercase", letterSpacing: "0.06em" }}>Got Questions?</div>
            <h2 style={{ fontSize: "clamp(1.3rem, 3vw, 1.8rem)", fontWeight: 800, color: "var(--text)", margin: "0 0 0.5rem" }}>
              Udemy Coupon Code <span style={{ color: "var(--brand)" }}>FAQs</span>
            </h2>
            <p style={{ color: "var(--muted)", fontSize: "1rem", maxWidth: "600px", margin: "0 auto", lineHeight: 1.6 }}>
              Straight answers on legitimacy, redemption, expiries, certificates and refunds.
            </p>
          </div>
          <div class="faq-grid" style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(420px, 1fr))", gap: "1.5rem" }}>
            {faqs.map((faq: any) => (
              <div itemscope itemtype="https://schema.org/Question" style={{ background: "var(--card)", padding: "1.75rem 2rem", borderRadius: "20px", border: "1px solid rgba(11,107,74,0.18)", position: "relative", overflow: "hidden" }}>
                <div style={{ display: "flex", alignItems: "center", gap: "1rem", marginBottom: "0.75rem" }}>
                  <div style={{ flexShrink: 0, width: "44px", height: "44px", background: "linear-gradient(135deg, rgba(11,107,74,0.15) 0%, rgba(76,154,118,0.08) 100%)", borderRadius: "10px", display: "flex", alignItems: "center", justifyContent: "center", border: "1px solid rgba(11,107,74,0.25)", color: "var(--brand)", fontWeight: 800, fontSize: "1.1rem" }}>?</div>
                  <h3 itemprop="name" style={{ color: "var(--text)", fontSize: "1.05rem", margin: 0, fontWeight: 700, lineHeight: 1.4 }}>{faq.q}</h3>
                </div>
                <div itemscope itemtype="https://schema.org/Answer" itemprop="acceptedAnswer">
                  <p itemprop="text" style={{ color: "var(--muted)", lineHeight: 1.75, fontSize: "0.95rem", margin: 0 }}>{faq.a}</p>
                </div>
              </div>
            ))}
          </div>
        </section>
      )}

      {currentPage === 1 && (
        <AuthorBox image="/images/author.jpg" />
      )}

      <section class="cc-cta">
        <p class="cc-cta-eyebrow">Catalog complete</p>
        <p class="cc-cta-text">Every coupon on this page was checked against Udemy before listing. Check back tomorrow — fresh verified codes are added daily.</p>
        <a class="cc-hero-btn" href="/udemy-coupons-today">See today's new coupons →</a>
      </section>

    </div>
  </main>
</Layout>

<style>
  .about-card {
    background: var(--card);
    padding: 1.5rem;
    border-radius: 16px;
    border: 1px solid var(--border);
    transition: transform 0.25s ease, box-shadow 0.25s ease, border-color 0.25s ease;
    position: relative;
    overflow: hidden;
  }
  .about-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, #0A5F40, #0B6B4A);
    opacity: 0;
    transition: opacity 0.25s ease;
  }
  .about-card:hover {
    transform: translateY(-4px);
    border-color: rgba(11,107,74,0.4);
    box-shadow: 0 14px 30px -12px rgba(0,0,0,0.5);
  }
  .about-card:hover::before {
    opacity: 1;
  }
  @media (max-width: 900px) {
    .deals-grid { grid-template-columns: repeat(2, 1fr) !important; }
  }
  @media (max-width: 640px) {
    .deals-grid { grid-template-columns: 1fr !important; }
  }
  .cc-root { background: var(--bg); padding: 2rem 1rem 4rem; }
  .cc-container { max-width: 1280px; margin: 0 auto; padding: 0 1.5rem; }

  /* ===== REGISTER / LEDGER HEADER ===== */
  .cc-register { position: relative; background: var(--card); border: 1px solid #E2E6EC; border-radius: 18px; padding: 30px 34px 28px; margin-top: 24px; box-shadow: 0 24px 48px -30px rgba(0, 0, 0, 0.35); }
  .cc-register-top { display: flex; align-items: center; justify-content: space-between; gap: 12px; flex-wrap: wrap; margin-bottom: 16px; }
  .cc-eyebrow { display: inline-flex; align-items: center; gap: 8px; font-family: 'IBM Plex Mono', Consolas, 'Courier New', monospace; font-size: 0.6875rem; font-weight: 600; color: var(--brand); background: rgba(11, 107, 74, 0.12); border: 1px solid rgba(0, 0, 0, 0.25); border-radius: 999px; padding: 6px 13px; }
  .cc-eyebrow a { color: inherit; text-decoration: none; }
  .cc-eyebrow a:hover { text-decoration: underline; }
  .cc-blip { width: 6px; height: 6px; border-radius: 50%; background: #0B6B4A; animation: ccBlip 1.6s infinite; }
  @keyframes ccBlip { 0%, 100% { opacity: 1; } 50% { opacity: .3; } }
  .cc-folio { font-family: 'IBM Plex Mono', Consolas, 'Courier New', monospace; font-size: 0.6875rem; color: var(--muted); border: 1px dashed #D5DCE2; border-radius: 6px; padding: 5px 10px; letter-spacing: .05em; white-space: nowrap; }
  .cc-title { font-size: clamp(2.1rem, 4.6vw, 3.4rem); font-weight: 700; color: var(--text); line-height: 1.08; letter-spacing: -0.02em; margin: 0 0 12px; }
  .cc-title .cc-accent { color: #CF6F59; }
  .cc-page-label { display: inline-flex; align-items: center; font-family: 'IBM Plex Mono', Consolas, 'Courier New', monospace; font-size: 0.6875rem; font-weight: 600; color: var(--brand); background: rgba(11, 107, 74, 0.12); border: 1px solid rgba(0, 0, 0, 0.25); border-radius: 999px; padding: 4px 10px; margin-left: 0.6rem; vertical-align: middle; }
  .cc-subtitle { color: var(--muted); font-size: 0.95rem; line-height: 1.7; margin: 0 0 20px; max-width: 640px; }
  .cc-subtitle strong { color: var(--text); font-weight: 700; }

  .cc-byline { display: flex; align-items: center; gap: 10px; flex-wrap: wrap; margin: 0 0 20px; }
  .cc-byline-badge { display: inline-flex; align-items: center; gap: 6px; font-family: 'IBM Plex Mono', Consolas, 'Courier New', monospace; font-size: 0.6875rem; font-weight: 600; color: var(--brand); background: rgba(11, 107, 74, 0.12); border: 1px solid rgba(0, 0, 0, 0.25); border-radius: 999px; padding: 5px 12px; text-decoration: none; transition: background 0.2s; }
  .cc-byline-badge:hover { background: rgba(11, 107, 74, 0.2); }
  .cc-byline-badge svg { flex-shrink: 0; }
  .cc-byline-date { font-family: 'IBM Plex Mono', Consolas, 'Courier New', monospace; font-size: 0.6875rem; color: var(--muted); }
  .cc-byline-verify { font-family: 'IBM Plex Mono', Consolas, 'Courier New', monospace; font-size: 0.6875rem; font-weight: 600; color: var(--text); text-decoration: none; border: 1px solid #D5DCE2; border-radius: 999px; padding: 5px 12px; transition: all 0.2s; }
  .cc-byline-verify:hover { border-color: var(--brand); color: var(--brand); }
  .cc-byline-note { font-family: 'IBM Plex Mono', Consolas, 'Courier New', monospace; font-size: 0.6875rem; color: var(--muted); margin: -6px 0 18px; line-height: 1.6; }
  .cc-byline-note a { color: var(--brand); text-decoration: none; font-weight: 600; }
  .cc-byline-note a:hover { text-decoration: underline; }

  .cc-toolbar { display: flex; align-items: center; gap: 12px; flex-wrap: wrap; }
  .cc-hero-search { display: flex; align-items: center; gap: 10px; flex: 1 1 300px; max-width: 460px; min-width: 240px; background: var(--card); border: 1px solid #E2E6EC; border-radius: 10px; padding: 0 14px; height: 48px; box-shadow: 0 8px 24px -18px rgba(0, 0, 0, 0.4); transition: border-color 0.2s, box-shadow 0.2s; }
  .cc-hero-search:focus-within { border-color: var(--brand); box-shadow: 0 8px 28px -14px rgba(0, 0, 0, 0.4); }
  .cc-hero-search svg { color: var(--muted); flex-shrink: 0; }
  .cc-hero-search input { flex: 1; border: none; outline: none; background: transparent; color: var(--text); font-size: 0.93rem; padding: 0; min-width: 0; }
  .cc-hero-search kbd { font-size: 0.7rem; color: var(--muted); background: transparent; border: 1px solid #E2E6EC; border-radius: 5px; padding: 2px 7px; white-space: nowrap; }
  .cc-hero-btn { font-family: 'Space Grotesk', Inter, sans-serif; font-size: 0.9rem; font-weight: 600; text-decoration: none; padding: 12px 20px; border-radius: 9px; background: #0B6B4A; color: #fff; box-shadow: 0 10px 24px rgba(0, 0, 0, 0.35); transition: background 0.2s, transform 0.2s; white-space: nowrap; }
  .cc-hero-btn:hover { background: #0B6B4A; transform: translateY(-1px); }
  .cc-hero-btn.ghost { background: transparent; color: var(--brand); border: 1px solid #0B6B4A; box-shadow: none; }
  .cc-hero-btn.ghost:hover { background: #0B6B4A; color: #fff; }

  .cc-ledger { display: flex; margin-top: 24px; border-top: 1px dashed #D5DCE2; padding-top: 20px; flex-wrap: wrap; }
  .cc-ledger-cell { flex: 1 1 150px; padding: 2px 18px; border-left: 1px solid #E2E6EC; }
  .cc-ledger-cell:first-child { border-left: none; padding-left: 2px; }
  .cc-ledger-label { font-family: 'IBM Plex Mono', Consolas, 'Courier New', monospace; font-size: 0.625rem; letter-spacing: .07em; text-transform: uppercase; color: var(--muted); margin-bottom: 4px; }
  .cc-ledger-value { font-family: 'Space Grotesk', Inter, sans-serif; font-weight: 700; font-size: 1.375rem; color: var(--text); }

  /* ===== SECTION HEADS ===== */
  .cc-sec-head { margin-bottom: 0.9rem; }
  .cc-sec-title { font-size: 1.2rem; font-weight: 700; color: var(--text); margin: 0 0 0.25rem; display: flex; align-items: center; gap: 0.6rem; flex-wrap: wrap; letter-spacing: -0.01em; }
  .cc-sec-title .cc-tag { font-family: 'IBM Plex Mono', Consolas, 'Courier New', monospace; font-size: 0.625rem; font-weight: 500; color: var(--muted); border: 1px solid #E2E6EC; padding: 3px 9px; border-radius: 999px; text-transform: uppercase; letter-spacing: .05em; margin-left: auto; white-space: nowrap; }
  .cc-sec-desc { font-size: 0.85rem; color: var(--muted); margin: 0; }

  /* ===== LEDGER TABLE (matches homepage deal table) ===== */
  .cc-featured { margin-top: 3.25rem; }
  .cc-deals-section { margin-top: 3.25rem; }
  .cc-ledger-wrap { background: var(--card); border: 1px solid #E2E6EC; border-radius: 16px; overflow: hidden; box-shadow: 0 10px 30px -18px rgba(0, 0, 0, 0.16); margin-top: 16px; }
  table.cc-ledger-table { width: 100%; border-collapse: collapse; }
  .cc-ledger-table thead th { font-family: 'IBM Plex Mono', Consolas, 'Courier New', monospace; font-size: 0.625rem; text-transform: uppercase; letter-spacing: .05em; color: var(--muted); text-align: left; padding: 12px 14px; border-bottom: 1px solid #E2E6EC; background: var(--bg-secondary); white-space: nowrap; }
  /* ===== GRID DESIGN (coursecard) — reuse existing LedgerRow grid ===== */
  .cc-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(310px, 1fr)); gap: 16px; margin-top: 16px; }
  @media (max-width: 640px) { .cc-grid { grid-template-columns: 1fr; } }
  .cc-deal-more { display: flex; justify-content: center; align-items: center; gap: 14px; flex-wrap: wrap; padding: 16px; background: var(--bg-secondary); border-top: 1px solid #E2E6EC; }
  .cc-deal-more a { font-family: 'IBM Plex Mono', Consolas, 'Courier New', monospace; font-size: 0.78125rem; font-weight: 600; color: var(--brand); text-decoration: none; }
  .cc-deal-more a:hover { text-decoration: underline; }
  .cc-deal-count { font-family: 'IBM Plex Mono', Consolas, 'Courier New', monospace; font-size: 0.71875rem; color: var(--muted); }

  /* ===== FOLIO PAGINATION ===== */
  .cc-pagination { display: flex; align-items: center; justify-content: space-between; gap: 0.75rem; padding: 1.1rem 0 0; margin-top: 1rem; border-top: 1px dashed #D5DCE2; }
  .cc-page-link { font-family: 'IBM Plex Mono', Consolas, 'Courier New', monospace; font-size: 0.8rem; font-weight: 600; color: var(--text); text-decoration: none; padding: 0.4rem 0.85rem; border: 1px solid #E2E6EC; border-radius: 6px; background: var(--card); transition: all 0.2s; white-space: nowrap; }
  .cc-page-link:hover { border-color: var(--brand); color: var(--brand); }
  .cc-page-disabled { color: #6E7686; opacity: 0.6; pointer-events: none; border-style: dashed; }
  .cc-page-numbers { display: flex; align-items: center; gap: 4px; }
  .cc-page-num { font-family: 'IBM Plex Mono', Consolas, 'Courier New', monospace; font-size: 0.8rem; font-weight: 600; color: var(--text); text-decoration: none; min-width: 32px; height: 32px; display: inline-flex; align-items: center; justify-content: center; border-radius: 6px; border: 1px solid #E2E6EC; background: var(--card); transition: all 0.2s; }
  .cc-page-num:hover { border-color: var(--brand); color: var(--brand); }
  .cc-page-active { background: #0B6B4A; color: #fff; border-color: var(--text); }
  .cc-page-active:hover { color: #fff; }
  .cc-page-ellipsis { font-family: 'IBM Plex Mono', Consolas, 'Courier New', monospace; font-size: 0.8rem; color: var(--muted); min-width: 28px; text-align: center; }

  /* ===== CATEGORY LEDGER BOXES ===== */
  .cc-category { margin: 3.25rem 0 0; }
  .cc-deals-header { margin-bottom: 0.9rem; }
  .cc-deals-desc { font-size: 0.85rem; color: var(--muted); margin: 0; }
  .cc-cat-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(170px, 1fr)); gap: 10px; }
  .cc-cat { background: var(--card); border: 1px solid #E2E6EC; border-radius: 12px; padding: 16px; text-align: left; text-decoration: none; display: block; transition: all 0.2s; }
  .cc-cat:hover { transform: translateY(-2px); border-color: #0B6B4A; box-shadow: 0 10px 28px -16px rgba(0, 0, 0, 0.35); }
  .cc-cat-num { font-family: 'Space Grotesk', Inter, sans-serif; font-weight: 700; font-size: 1.25rem; color: var(--text); }
  .cc-cat-label { font-size: 0.75rem; color: var(--muted); margin-top: 3px; font-weight: 600; }
  .cc-cat-count { font-family: 'IBM Plex Mono', Consolas, 'Courier New', monospace; font-size: 0.65625rem; color: var(--brand); margin-top: 6px; font-weight: 500; }

  /* ===== GENERIC SECTION HEADS (seo/faq/about) ===== */
  .cc-section { margin-top: 3.25rem; }
  .cc-section-title { font-size: 1.2rem; font-weight: 700; color: var(--text); margin: 0 0 0.3rem; display: flex; align-items: center; gap: 0.6rem; letter-spacing: -0.01em; flex-wrap: wrap; }
  .cc-section-title svg { color: var(--brand); flex-shrink: 0; }
  .cc-section-title .cc-tag { font-family: 'IBM Plex Mono', Consolas, 'Courier New', monospace; font-size: 0.625rem; font-weight: 500; color: var(--muted); border: 1px solid #E2E6EC; padding: 3px 9px; border-radius: 999px; text-transform: uppercase; letter-spacing: .05em; margin-left: auto; white-space: nowrap; }

  .cc-seo-section { background: var(--card); border: 1px solid #E2E6EC; border-radius: 14px; padding: 1.5rem; margin: 3.25rem 0 0; }
  .cc-seo-section .cc-section-title { font-size: 1.15rem; margin-bottom: 0.75rem; }
  .cc-seo-section p { font-size: 0.9rem; color: var(--muted); line-height: 1.6; margin: 0 0 0.75rem; }
  .cc-seo-section p:last-child { margin-bottom: 0; }
  .cc-seo-section a { color: var(--brand); text-decoration: none; font-weight: 600; }
  .cc-seo-section a:hover { text-decoration: underline; }

  /* ===== EVERGREEN GUIDE BLOCKS ===== */
  .cc-guide { margin: 3.25rem 0 0; display: grid; gap: 1.25rem; }
  .cc-guide-block { background: var(--card); border: 1px solid #E2E6EC; border-radius: 14px; padding: 1.5rem; }
  .cc-guide-block .cc-section-title { font-size: 1.15rem; margin-bottom: 0.75rem; }
  .cc-guide-desc { font-size: 0.9rem; color: var(--muted); line-height: 1.6; margin: 0 0 1rem; }
  .cc-guide-text { font-size: 0.9rem; color: var(--muted); line-height: 1.6; margin: 0 0 0.75rem; }
  .cc-guide-text:last-child { margin-bottom: 0; }
  .cc-guide-note { font-family: 'IBM Plex Mono', Consolas, 'Courier New', monospace; font-size: 0.75rem; color: var(--muted); margin: 1rem 0 0; }
  .cc-ppoffer { display: flex; align-items: center; justify-content: space-between; gap: 12px; flex-wrap: wrap; margin-top: 14px; background: rgba(11, 107, 74, 0.12); border: 1px solid rgba(0, 0, 0, 0.35); border-radius: 10px; padding: 12px 16px; text-decoration: none; transition: all 0.2s; }
  .cc-ppoffer:hover { background: rgba(11, 107, 74, 0.2); border-color: var(--brand); }
  .cc-ppoffer-label { font-family: 'Space Grotesk', Inter, sans-serif; font-size: 0.8125rem; font-weight: 700; color: var(--text); }
  .cc-ppoffer-cta { font-family: 'IBM Plex Mono', Consolas, 'Courier New', monospace; font-size: 0.71875rem; font-weight: 600; color: var(--brand); white-space: nowrap; }
  .cc-guide-block a { color: var(--brand); text-decoration: none; font-weight: 600; }
  .cc-guide-block a:hover { text-decoration: underline; }

  .cc-steps { list-style: none; margin: 0; padding: 0; display: grid; gap: 0.75rem; }
  .cc-step { display: flex; gap: 0.9rem; background: var(--card); border: 1px solid #E2E6EC; border-radius: 12px; padding: 1rem 1.1rem; }
  .cc-step-num { flex-shrink: 0; width: 28px; height: 28px; display: inline-flex; align-items: center; justify-content: center; border-radius: 999px; background: #0B6B4A; color: #fff; font-family: 'IBM Plex Mono', Consolas, 'Courier New', monospace; font-size: 0.75rem; font-weight: 600; }
  .cc-step-title { font-size: 0.95rem; font-weight: 700; color: var(--text); margin: 0 0 0.25rem; }
  .cc-step-text { font-size: 0.85rem; color: var(--muted); line-height: 1.55; margin: 0; }

  .cc-tips { list-style: none; margin: 0; padding: 0; display: grid; gap: 0.6rem; }
  .cc-tip { font-size: 0.87rem; color: var(--muted); line-height: 1.55; background: var(--card); border: 1px solid #E2E6EC; border-radius: 10px; padding: 0.85rem 1rem; }
  .cc-tip strong { color: var(--text); }
  .cc-tip a { font-weight: 600; }

  .cc-faq-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 0.75rem; }
  @media (max-width: 768px) { .cc-faq-grid { grid-template-columns: 1fr; } }
  .cc-faq-card { background: var(--card); border: 1px solid #E2E6EC; border-radius: 12px; padding: 1.1rem 1.15rem; }
  .cc-faq-card:hover { border-color: #6E7686; }
  .cc-faq-q { font-size: 0.95rem; font-weight: 700; color: var(--text); margin: 0 0 0.4rem; line-height: 1.4; }
  .cc-faq-a { font-size: 0.85rem; color: var(--muted); line-height: 1.55; margin: 0; }
  .cc-faq-a a { color: var(--brand); text-decoration: none; font-weight: 600; }
  .cc-faq-a a:hover { text-decoration: underline; }

  .cc-cta { background: var(--card); border: 1px solid #E2E6EC; color: var(--text); border-radius: 16px; padding: 30px 32px; text-align: center; margin: 3.25rem 0 0; }
  .cc-cta-eyebrow { font-family: 'IBM Plex Mono', Consolas, 'Courier New', monospace; font-size: 0.625rem; letter-spacing: .07em; text-transform: uppercase; color: var(--brand); margin: 0 0 8px; }
  .cc-cta-text { font-size: 0.95rem; line-height: 1.6; margin: 0 auto 18px; max-width: 480px; color: var(--muted); }
  .cc-cta .cc-hero-btn { background: #0B6B4A; color: #fff; box-shadow: 0 12px 28px -12px rgba(0, 0, 0, 0.5); }
  .cc-cta .cc-hero-btn:hover { background: #0B6B4A; transform: translateY(-1px); }

  @media (max-width: 768px) {
    .cc-title { font-size: 1.9rem; }
    .cc-register { padding: 26px 20px 22px; }
    .cc-ledger-cell { border-left: none; padding: 8px 0; }
    .cc-pagination { flex-wrap: wrap; justify-content: center; }
  }
  @media (max-width: 480px) {
    .cc-title { font-size: 1.7rem; }
    .cc-hero-btn { padding: 11px 16px; font-size: 0.85rem; }
  }
</style>
