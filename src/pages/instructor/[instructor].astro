---
import Layout from '../../layouts/Layout.astro';
import LedgerRow from '../../components/LedgerRow.astro';
import type { Deal } from '../../types/deal.d.ts';
import { readDeals } from '../../lib/store';
import { parseInstructors, createInstructorSlug } from '../../lib/instructors';
import { getInstructorImage } from '../../lib/instructorProfiles';
import { toISODateWIB } from '../../lib/date';
import { buildHeroFeed } from '../../lib/utils';

const siteUrl = 'https://courseswyn.com';

export async function getStaticPaths() {
  const deals: Deal[] = await readDeals();
  const slugToName = new Map<string, string>();

  for (const deal of deals) {
    if (!deal.instructor) continue;
    for (const name of parseInstructors(deal.instructor)) {
      const slug = createInstructorSlug(name);
      if (!slug) continue;
      if (!slugToName.has(slug)) {
        slugToName.set(slug, name);
      }
    }
  }

  return Array.from(slugToName.entries()).map(([instructor, instructorName]) => ({
    params: { instructor },
    props: { instructorName, allDeals: deals },
  }));
}

const { instructorName: instructorNameProp, allDeals: propAllDeals } = Astro.props as { instructorName?: string; allDeals?: Deal[] };
const slugParam = Astro.params.instructor ?? '';

const deals: Deal[] = propAllDeals || await readDeals();

function dealMatchesInstructorSlug(deal: Deal, targetSlug: string): boolean {
  if (!deal.instructor) return false;
  return parseInstructors(deal.instructor).some((n) => createInstructorSlug(n) === targetSlug);
}

let instructorName = instructorNameProp;
if (!instructorName && slugParam) {
  for (const deal of deals) {
    if (!deal.instructor) continue;
    for (const n of parseInstructors(deal.instructor)) {
      if (createInstructorSlug(n) === slugParam) {
        instructorName = n;
        break;
      }
    }
    if (instructorName) break;
  }
}

if (!instructorName || !slugParam) {
  return Astro.redirect('/instructors');
}

const instructorCourses = deals
  .filter((d) => dealMatchesInstructorSlug(d, slugParam))
  .sort((a, b) => (b.students ?? 0) - (a.students ?? 0));

if (instructorCourses.length === 0) {
  return Astro.redirect('/instructors');
}

const currentMonth = new Date().toLocaleDateString('en-US', { month: 'long', timeZone: 'Asia/Jakarta' });
const currentYear = Number(new Date().toLocaleString('en-US', { year: 'numeric', timeZone: 'Asia/Jakarta' }));
const currentDate = toISODateWIB();
const canonicalUrl = `${siteUrl}/instructor/${slugParam}`;

const instructorImage = await getInstructorImage(slugParam);

// og:image untuk share sosial: utamakan foto instructor, fallback ke gambar course pertama
// (jangan sampai jatuh ke og-default.svg yang tidak bisa dirender FB/X/WA).
const ogImage = instructorImage ?? instructorCourses[0]?.seoOgImage ?? instructorCourses[0]?.image;
const isInstructorAvatar = Boolean(instructorImage && ogImage === instructorImage);

function getInitials(name: string): string {
  return name.split(' ').filter(Boolean).map((n) => n[0]).join('').toUpperCase().slice(0, 2);
}

function getAvatarColor(name: string): string {
  const colors = ['#FFC94D', '#CF6F59', '#FFC94D', '#2E5AA8', '#7A4D1F', '#4D7A1F', '#8A2E5A'];
  let hash = 0;
  for (let i = 0; i < name.length; i++) hash = name.charCodeAt(i) + ((hash << 5) - hash);
  return colors[Math.abs(hash) % colors.length];
}

function getMaxDiscount(courses: Deal[]): number {
  return Math.max(
    0,
    ...courses.map((c) => {
      if (!c.originalPrice || !c.price || c.price >= c.originalPrice) return 0;
      return Math.round(100 - (c.price / c.originalPrice) * 100);
    })
  );
}

function hasFree(courses: Deal[]): boolean {
  return courses.some((c) => c.price === 0);
}

const totalStudents = instructorCourses.reduce((s, c) => s + (c.students ?? 0), 0);
const rated = instructorCourses.filter((c) => typeof c.rating === 'number' && isFinite(c.rating!));
const avgRating =
  rated.length > 0
    ? (rated.reduce((s, c) => s + (c.rating as number), 0) / rated.length).toFixed(1)
    : '—';
const provider = instructorCourses[0]?.provider || 'Udemy';
const maxDisc = getMaxDiscount(instructorCourses);
const freeCount = hasFree(instructorCourses) ? instructorCourses.filter((c) => c.price === 0).length : 0;
const now = new Date();
const heroFeed = buildHeroFeed(instructorCourses, now, 3);
const title = `${instructorName} — Udemy Coupons & Promo Codes ${currentMonth} ${currentYear} | CoursesWyn`;
const description = `Verified ${instructorCourses.length} Udemy coupon${instructorCourses.length === 1 ? '' : 's'} from ${instructorName}. ${provider} deals updated daily — up to ${maxDisc}% off.`;
const cleanName = instructorName.replace(/[^\x20-\x7E]/g, '').trim();

const pageKeywords = [
  `${instructorName.toLowerCase()} udemy`,
  `${instructorName.toLowerCase()} coupons`,
  `${instructorName.toLowerCase()} promo codes`,
  `${instructorName.toLowerCase()} courses free`,
  `best ${instructorName.toLowerCase()} courses`,
  "udemy coupon code"
].join(", ");

const faqItems = [
  {
    q: `How many courses does ${instructorName} have on CoursesWyn?`,
    a: `${instructorName} currently has ${instructorCourses.length} verified coupon${instructorCourses.length === 1 ? '' : 's'} on CoursesWyn across ${provider}.`,
  },
  {
    q: `Are ${instructorName}'s coupons verified?`,
    a: `Yes. Every coupon from ${instructorName} is manually verified before listing. Expired or invalid deals are removed within hours.`,
  },
  {
    q: `How do I get ${instructorName}'s courses for free?`,
    a: `Browse ${instructorName}'s deals on CoursesWyn and click any coupon that interests you. Many deals offer deep discounts — click through to see the current offer on Udemy.`,
  },
  {
    q: `What is the maximum discount for ${instructorName}'s courses?`,
    a: `The biggest discount available across ${instructorName}'s courses is up to ${maxDisc}% off.`,
  },
];

const benefitCards = [
  {
    icon: '<path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/>',
    color: '#FFC94D',
    title: 'Verified Coupons',
    text: `Every coupon from ${instructorName} is manually checked before listing. No expired or fake deals.`,
  },
  {
    icon: '<circle cx="12" cy="12" r="10"/><path d="m9 12 2 2 4-4"/>',
    color: '#FFC94D',
    title: 'Top-Rated Courses',
    text: `${avgRating !== '—' ? `${instructorName}'s courses average ${avgRating} stars` : `${instructorName}'s courses are available at great discounts`}.`,
  },
  {
    icon: '<path d="M12 2v4M12 18v4M4.93 4.93l2.83 2.83M16.24 16.24l2.83 2.83M2 12h4M18 12h4M4.93 19.07l2.83-2.83M16.24 7.76l2.83-2.83"/>',
    color: '#FFC94D',
    title: `Save up to ${maxDisc}%`,
    text: `Get the best deals on ${instructorName}'s ${provider} courses with discounts up to ${maxDisc}% off.`,
  },
  {
    icon: '<polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/>',
    color: '#FFC94D',
    title: 'Updated Daily',
    text: `New coupons added regularly. Deals refresh daily so you never miss a discount from ${instructorName}.`,
  },
];
---

<Layout title={title} description={description} canonical={canonicalUrl} image={ogImage} imageWidth={isInstructorAvatar ? 200 : 1200} imageHeight={isInstructorAvatar ? 200 : 630} twitterCard={isInstructorAvatar ? "summary" : "summary_large_image"} hideBreadcrumbs={false}>
  <Fragment slot="head">
    <meta name="keywords" content={pageKeywords} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;600;700&family=IBM+Plex+Mono:wght@400;500;600&display=swap" rel="stylesheet" />
  </Fragment>

  <script
    type="application/ld+json"
    set:html={JSON.stringify({
      '@context': 'https://schema.org',
      '@graph': [
        {
          '@type': 'BreadcrumbList',
          itemListElement: [
            { '@type': 'ListItem', position: 1, name: 'Home', item: siteUrl },
            { '@type': 'ListItem', position: 2, name: 'Instructors', item: `${siteUrl}/instructors` },
            { '@type': 'ListItem', position: 3, name: cleanName, item: canonicalUrl },
          ],
        },
        {
          '@type': ['WebPage', 'CollectionPage'],
          '@id': canonicalUrl,
          url: canonicalUrl,
          name: title,
          description,
          keywords: pageKeywords,
          inLanguage: 'en-US',
          dateModified: currentDate,
          isPartOf: { '@id': `${siteUrl}#website` },
          mainEntity: { '@id': `${canonicalUrl}#itemlist` },
        },
        {
          '@type': 'ItemList',
          '@id': `${canonicalUrl}#itemlist`,
          name: `${instructorName}'s Courses`,
          numberOfItems: instructorCourses.length,
          itemListElement: instructorCourses.slice(0, 20).map((c, i) => ({
            '@type': 'ListItem',
            position: i + 1,
            url: `${siteUrl}/coupon/${c.slug}`,
            item: {
              '@type': 'Course',
              name: c.title,
              url: `${siteUrl}/coupon/${c.slug}`,
              image: c.image,
              provider: { '@type': 'Organization', name: c.provider || 'Udemy' },
              offers: {
                '@type': 'Offer',
                price: String(c.price ?? '0'),
                priceCurrency: 'USD',
                availability: 'https://schema.org/InStock',
              },
            },
          })),
        },
        {
          '@type': 'FAQPage',
          mainEntity: faqItems.map((f) => ({
            '@type': 'Question',
            name: f.q,
            acceptedAnswer: { '@type': 'Answer', text: f.a },
          })),
        },
      ],
    })}
  />

  <main class="cc-root">
    <div class="cc-container">

      <header class="cc-header">
        <div class="dh-hero-grid">
          <div class="dh-hero-left">
            <div class="cc-instructor-head">
              {instructorImage ? (
                <div class="cc-avatar cc-avatar-img"><img src={instructorImage} alt={instructorName} loading="lazy" /></div>
              ) : (
                <div class="cc-avatar" style={`background:${getAvatarColor(instructorName)}`}>{getInitials(instructorName)}</div>
              )}
              <div class="dh-hero-badge" style="margin-bottom:0;"><span class="lg-blip"></span> {provider} &middot; {instructorCourses.length} Coupon{instructorCourses.length === 1 ? '' : 's'}</div>
            </div>
            <h1 class="dh-hero-title"><span class="cc-accent">{cleanName}</span> — Udemy Coupons &amp; Promo Codes</h1>
            <p class="dh-hero-sub">Verified <strong>{instructorCourses.length} coupon{instructorCourses.length === 1 ? '' : 's'} from {instructorName}</strong> on {provider} — checked against Udemy before listing, updated {currentMonth} {currentYear}.</p>
            <div class="dh-hero-stats">
              <div class="dh-stat"><b class="dh-stat-val"><span data-count={instructorCourses.length}>0</span></b><span class="dh-stat-label">courses</span></div>
              <div class="dh-stat"><b class="dh-stat-val"><span data-count={maxDisc}>0</span><span class="lg-plus">%</span></b><span class="dh-stat-label">max discount</span></div>
              <div class="dh-stat"><b class="dh-stat-val"><span data-count={totalStudents}>0</span><span class="lg-plus">+</span></b><span class="dh-stat-label">students</span></div>
            </div>
          </div>
          <div class="dh-terminal">
            <div class="dh-term-head">
              <span class="dh-term-dot" style="background:#ff5f56"></span>
              <span class="dh-term-dot" style="background:#ffbd2e"></span>
              <span class="dh-term-dot" style="background:#27c93f"></span>
              <span class="dh-term-title">agents@courseswyn: ~/instructor</span>
              <span class="dh-term-live"><span class="lg-blip"></span> Live</span>
            </div>
            <div class="dh-term-body">
              {heroFeed.map((d) => (
                <a class="dh-term-line" href={"/coupon/" + d.slug}>
                  <span class="dh-term-time">{d.timeLabel}</span>
                  <span class="dh-term-ok">✓</span>
                  <span class="dh-term-cat">{d.cat}</span>
                  <span class="dh-term-title2">{d.title}</span>
                  <span class="dh-term-save">−{d.pct}%</span>
                </a>
              ))}
              <div class="dh-term-line"><span class="dh-term-time">now</span><span class="dh-term-ok">✓</span><span class="dh-term-title2">codes checked by CoursesWyn<span class="caret"></span></span></div>
            </div>
            <div class="dh-term-foot">$ instructor codes verified <b>{instructorCourses.length}</b></div>
          </div>
        </div>
        <div class="cc-toolbar" style="margin-top:1.5rem;">
          <a class="cc-hero-btn" href="#deals">View all {instructorCourses.length} courses</a>
          <a class="cc-hero-btn ghost" href="/instructors">Browse all instructors</a>
          <a class="cc-hero-btn ghost" href="/udemy-coupons-today">Today's new deals</a>
        </div>
      </header>

      <section class="cc-section" aria-labelledby="why-title">
        <div class="cc-sec-head">
          <h2 id="why-title" class="cc-sec-title">Why {cleanName} <span class="cc-tag">{freeCount} free deals</span></h2>
          <p class="cc-sec-desc">{cleanName} offers <strong>{instructorCourses.length} verified coupon{instructorCourses.length === 1 ? '' : 's'}</strong> on CoursesWyn — browse top-rated courses with discounts up to <strong>{maxDisc}% off</strong>.</p>
        </div>
        <div class="cc-adv-grid">
          {benefitCards.map((b) => (
            <div class="cc-adv-card">
              <div class="cc-adv-icon" style={{ color: b.color }}>
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" set:html={b.icon} />
              </div>
              <h3>{b.title}</h3>
              <p>{b.text}</p>
            </div>
          ))}
        </div>
      </section>

      <section class="cc-deals-section" id="deals" aria-label={`${instructorName}'s courses`}>
        <div class="cc-sec-head">
          <h2 class="cc-sec-title">All {instructorName} Courses with Verified Coupons <span class="cc-tag">{instructorCourses.length} courses</span></h2>
          <p class="cc-sec-desc">Sorted by enrollment — every code checked against Udemy before listing.</p>
        </div>

        {instructorCourses.length > 0 ? (
          <div class="cc-ledger-wrap">
            <table class="cc-ledger-table">
              <thead><tr><th>Course</th><th>Price</th><th class="cc-col-hide">Status</th><th>Checked</th></tr></thead>
              <tbody>
                {instructorCourses.map((deal) => <LedgerRow deal={deal} />)}
              </tbody>
            </table>
            <div class="cc-deal-more"><span class="cc-deal-count">{instructorCourses.length} courses from {instructorName} — verified before listing</span><a href="/udemy-coupons-today">See today's new coupons →</a></div>
          </div>
        ) : (
          <div class="cc-empty">
            <h3>No courses from {instructorName}</h3>
            <p>We couldn't find any courses from this instructor right now. Try browsing our other instructors or check back later for fresh deals.</p>
            <a href="/instructors" class="cc-hero-btn">Browse All Instructors →</a>
          </div>
        )}
      </section>

      <section class="cc-section cc-faq" aria-labelledby="faq-title">
        <h2 id="faq-title" class="cc-section-title">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>
          Frequently asked questions
          <span class="cc-tag">{faqItems.length} questions</span>
        </h2>
        <div class="cc-faq-grid">
          {faqItems.map((faq) => (
            <div class="cc-faq-card">
              <h3 class="cc-faq-q">{faq.q}</h3>
              <p class="cc-faq-a">{faq.a}</p>
            </div>
          ))}
        </div>
      </section>

      <section class="cc-cta">
        <p class="cc-cta-eyebrow">More instructors to explore</p>
        <p class="cc-cta-text">Discover top Udemy instructors — all with verified coupon codes.</p>
        <a class="cc-hero-btn" href="/instructors">Browse All Instructors →</a>
      </section>

    </div>
  </main>
</Layout>

<style>
  .cc-root { background: var(--bg); padding: 2rem 1rem 4rem; }
  .cc-container { max-width: 1280px; margin: 0 auto; padding: 0 1.5rem; }

  /* ===== REGISTER / LEDGER HEADER ===== */
  .cc-register { position: relative; background: var(--card); border: 1px solid #232B26; border-radius: 18px; padding: 30px 34px 28px; margin-top: 24px; box-shadow: 0 24px 48px -30px rgba(0, 0, 0, 0.35); }
  .cc-instructor-head { display: flex; align-items: center; gap: 16px; margin-bottom: 16px; flex-wrap: wrap; }
  .cc-avatar { width: 56px; height: 56px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 1.1rem; font-weight: 800; color: #fff; flex-shrink: 0; overflow: hidden; box-shadow: 0 8px 18px -8px rgba(0, 0, 0, 0.4); }
  .cc-avatar-img img { width: 100%; height: 100%; object-fit: cover; display: block; }
  .cc-register-top { display: flex; align-items: center; justify-content: space-between; gap: 12px; flex-wrap: wrap; margin-bottom: 0; flex: 1; min-width: 0; }
  .cc-eyebrow { display: inline-flex; align-items: center; gap: 8px; font-family: var(--font-mono); font-size: 0.6875rem; font-weight: 600; color: var(--brand); background: rgba(76, 195, 138, 0.12); border: 1px solid rgba(0, 0, 0, 0.25); border-radius: 999px; padding: 6px 13px; }
  .cc-blip { width: 6px; height: 6px; border-radius: 50%; background: #FFC94D; animation: ccBlip 1.6s infinite; }
  @keyframes ccBlip { 0%, 100% { opacity: 1; } 50% { opacity: .3; } }
  .dh-hero-grid { display: grid; grid-template-columns: 1.05fr 1fr; gap: 2.5rem; align-items: center; }
  .dh-hero-badge { display: inline-flex; align-items: center; gap: 8px; font-family: var(--font-mono); font-size: 0.78rem; color: var(--text); background: var(--card); border: 1px solid var(--border); border-radius: 999px; padding: 6px 14px; margin-bottom: 1.25rem; }
  .dh-hero-title { font-size: clamp(2rem, 4.3vw, 3rem); font-weight: 800; letter-spacing: -0.02em; color: var(--text-bright, var(--text)); margin: 0 0 0.75rem; line-height: 1.12; }
  .dh-hero-title .cc-accent { color: var(--brand); }
  .dh-hero-sub { color: var(--muted); font-size: 1rem; line-height: 1.7; margin: 0 0 1.5rem; max-width: 560px; }
  .dh-hero-sub strong { color: var(--text); }
  .dh-hero-stats { display: flex; gap: 2rem; flex-wrap: wrap; }
  .dh-stat { display: flex; flex-direction: column; }
  .dh-stat-val { font-family: var(--font-display); font-size: 1.5rem; font-weight: 800; color: var(--brand); }
  .dh-stat-label { font-family: var(--font-mono); font-size: 0.7rem; color: var(--muted); }
  .lg-blip { width: 7px; height: 7px; border-radius: 50%; background: var(--brand); animation: lgBlip 1.6s infinite; }
  @keyframes lgBlip { 0%, 100% { opacity: 1; } 50% { opacity: 0.3; } }
  .caret { display: inline-block; width: 0.55ch; height: 1em; margin-left: 4px; background: var(--brand); vertical-align: text-bottom; animation: lgBlip 1.1s step-end infinite; }
  .dh-terminal { background: var(--bg); border: 1px solid var(--border); border-radius: 12px; overflow: hidden; text-align: left; }
  .dh-term-head { display: flex; align-items: center; gap: 6px; padding: 10px 14px; background: var(--bg-secondary); border-bottom: 1px solid var(--border); }
  .dh-term-dot { width: 11px; height: 11px; border-radius: 50%; }
  .dh-term-title { margin-left: 8px; font-family: var(--font-mono); font-size: 0.72rem; color: var(--muted); }
  .dh-term-live { margin-left: auto; display: inline-flex; align-items: center; gap: 6px; font-family: var(--font-mono); font-size: 0.7rem; font-weight: 700; color: var(--stamp); text-transform: uppercase; letter-spacing: 0.06em; }
  .dh-term-live .lg-blip { background: var(--stamp); }
  .dh-term-body { padding: 4px 0; }
  .dh-term-line { display: flex; align-items: center; gap: 10px; padding: 7px 16px; font-family: var(--font-mono); font-size: 0.8rem; color: var(--text); text-decoration: none; }
  a.dh-term-line:hover { background: var(--brand-soft); }
  .dh-term-time { color: var(--muted); flex-shrink: 0; font-size: 0.7rem; min-width: 42px; }
  .dh-term-ok { color: var(--stamp); font-weight: 700; flex-shrink: 0; }
  .dh-term-cat { color: var(--brand); flex-shrink: 0; font-size: 0.7rem; min-width: 64px; }
  .dh-term-title2 { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; flex: 1; }
  .dh-term-save { color: var(--brand); font-weight: 700; flex-shrink: 0; }
  .dh-term-foot { display: flex; align-items: center; gap: 8px; padding: 10px 16px; border-top: 1px solid var(--border); background: var(--bg-secondary); font-family: var(--font-mono); font-size: 0.78rem; color: var(--text); }
  .dh-term-foot b { color: var(--brand); }
  @media (max-width: 900px) { .dh-hero-grid { grid-template-columns: 1fr; gap: 1.75rem; } }
  .cc-folio { font-family: var(--font-mono); font-size: 0.6875rem; color: var(--muted); border: 1px dashed var(--border); border-radius: 6px; padding: 5px 10px; letter-spacing: .05em; white-space: nowrap; }
  .cc-title { font-size: clamp(2.1rem, 4.6vw, 3.4rem); font-weight: 700; color: var(--text); line-height: 1.08; letter-spacing: -0.02em; margin: 0 0 12px; }
  .cc-title .cc-accent { color: #CF6F59; }
  .cc-subtitle { color: var(--muted); font-size: 0.95rem; line-height: 1.7; margin: 0 0 20px; max-width: 640px; }
  .cc-subtitle strong { color: var(--text); font-weight: 700; }

  .cc-toolbar { display: flex; align-items: center; gap: 12px; flex-wrap: wrap; }
  .cc-hero-btn { font-family: var(--font-display); font-size: 0.9rem; font-weight: 600; text-decoration: none; padding: 12px 20px; border-radius: 9px; background: #FFC94D; color: #0b0e14; box-shadow: 0 10px 24px rgba(0, 0, 0, 0.35); transition: background 0.2s, transform 0.2s; white-space: nowrap; }
  .cc-hero-btn:hover { background: #FFC94D; transform: translateY(-1px); }
  .cc-hero-btn.ghost { background: transparent; color: var(--brand); border: 1px solid #FFC94D; box-shadow: none; }
  .cc-hero-btn.ghost:hover { background: #FFC94D; color: #0b0e14; }

  .cc-ledger { display: flex; margin-top: 24px; border-top: 1px dashed var(--border); padding-top: 20px; flex-wrap: wrap; }
  .cc-ledger-cell { flex: 1 1 150px; padding: 2px 18px; border-left: 1px solid #232B26; }
  .cc-ledger-cell:first-child { border-left: none; padding-left: 2px; }
  .cc-ledger-label { font-family: var(--font-mono); font-size: 0.625rem; letter-spacing: .07em; text-transform: uppercase; color: var(--muted); margin-bottom: 4px; }
  .cc-ledger-value { font-family: var(--font-display); font-weight: 700; font-size: 1.375rem; color: var(--text); }

  /* ===== SECTION HEADS ===== */
  .cc-section { margin-top: 3.25rem; }
  .cc-sec-head { margin-bottom: 0.9rem; }
  .cc-sec-title { font-size: 1.2rem; font-weight: 700; color: var(--text); margin: 0 0 0.25rem; display: flex; align-items: center; gap: 0.6rem; flex-wrap: wrap; letter-spacing: -0.01em; }
  .cc-sec-title .cc-tag { font-family: var(--font-mono); font-size: 0.625rem; font-weight: 500; color: var(--muted); border: 1px solid #232B26; padding: 3px 9px; border-radius: 999px; text-transform: uppercase; letter-spacing: .05em; margin-left: auto; white-space: nowrap; }
  .cc-sec-desc { font-size: 0.85rem; color: var(--muted); margin: 0; }
  .cc-sec-desc strong { color: var(--text); font-weight: 700; }

  /* ===== BENEFIT CARDS ===== */
  .cc-adv-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(220px, 1fr)); gap: 10px; margin-top: 16px; }
  .cc-adv-card { background: var(--card); border: 1px solid #232B26; border-radius: 12px; padding: 18px 16px; transition: all 0.2s; }
  .cc-adv-card:hover { border-color: #FFC94D; transform: translateY(-2px); box-shadow: 0 10px 28px -16px rgba(0, 0, 0, 0.35); }
  .cc-adv-icon { width: 36px; height: 36px; border-radius: 9px; background: var(--card); border: 1px solid #232B26; display: flex; align-items: center; justify-content: center; margin-bottom: 10px; }
  .cc-adv-card h3 { font-size: 0.95rem; font-weight: 700; color: var(--text); margin: 0 0 0.35rem; }
  .cc-adv-card p { font-size: 0.82rem; color: var(--muted); line-height: 1.55; margin: 0; }

  /* ===== LEDGER TABLE ===== */
  .cc-deals-section { margin-top: 3.25rem; scroll-margin-top: 1.5rem; }
  .cc-ledger-wrap { background: var(--card); border: 1px solid #232B26; border-radius: 16px; overflow: hidden; box-shadow: 0 10px 30px -18px rgba(0, 0, 0, 0.16); margin-top: 16px; }
  table.cc-ledger-table { width: 100%; border-collapse: collapse; }
  .cc-ledger-table thead th { font-family: var(--font-mono); font-size: 0.625rem; text-transform: uppercase; letter-spacing: .05em; color: var(--muted); text-align: left; padding: 12px 14px; border-bottom: 1px solid #232B26; background: var(--bg-secondary); white-space: nowrap; }
  .cc-deal-more { display: flex; justify-content: center; align-items: center; gap: 14px; flex-wrap: wrap; padding: 16px; background: var(--bg-secondary); border-top: 1px solid #232B26; }
  .cc-deal-more a { font-family: var(--font-mono); font-size: 0.78125rem; font-weight: 600; color: var(--brand); text-decoration: none; }
  .cc-deal-more a:hover { text-decoration: underline; }
  .cc-deal-count { font-family: var(--font-mono); font-size: 0.71875rem; color: var(--muted); }

  .cc-empty { text-align: center; padding: 3rem 2rem; background: var(--card); border: 1px solid #232B26; border-radius: 16px; margin-top: 16px; }
  .cc-empty h3 { color: var(--text); margin-bottom: 0.75rem; font-size: 1.4rem; }
  .cc-empty p { color: var(--muted); margin-bottom: 1.5rem; line-height: 1.6; max-width: 34rem; margin-left: auto; margin-right: auto; }

  /* ===== GENERIC SECTION HEADS (seo/faq) ===== */
  .cc-section-title { font-size: 1.2rem; font-weight: 700; color: var(--text); margin: 0 0 0.3rem; display: flex; align-items: center; gap: 0.6rem; letter-spacing: -0.01em; flex-wrap: wrap; }
  .cc-section-title svg { color: var(--brand); flex-shrink: 0; }
  .cc-section-title .cc-tag { font-family: var(--font-mono); font-size: 0.625rem; font-weight: 500; color: var(--muted); border: 1px solid #232B26; padding: 3px 9px; border-radius: 999px; text-transform: uppercase; letter-spacing: .05em; margin-left: auto; white-space: nowrap; }

  .cc-faq-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 0.75rem; }
  @media (max-width: 768px) { .cc-faq-grid { grid-template-columns: 1fr; } }
  .cc-faq-card { background: var(--card); border: 1px solid #232B26; border-radius: 12px; padding: 1.1rem 1.15rem; }
  .cc-faq-card:hover { border-color: #8A877C; }
  .cc-faq-q { font-size: 0.95rem; font-weight: 700; color: var(--text); margin: 0 0 0.4rem; line-height: 1.4; }
  .cc-faq-a { font-size: 0.85rem; color: var(--muted); line-height: 1.55; margin: 0; }

  .cc-cta { background: var(--card); border: 1px solid #232B26; color: var(--text); border-radius: 16px; padding: 30px 32px; text-align: center; margin: 3.25rem 0 0; }
  .cc-cta-eyebrow { font-family: var(--font-mono); font-size: 0.625rem; letter-spacing: .07em; text-transform: uppercase; color: var(--brand); margin: 0 0 8px; }
  .cc-cta-text { font-size: 0.95rem; line-height: 1.6; margin: 0 auto 18px; max-width: 480px; color: var(--muted); }
  .cc-cta .cc-hero-btn { background: #FFC94D; color: #0b0e14; box-shadow: 0 12px 28px -12px rgba(0, 0, 0, 0.5); }
  .cc-cta .cc-hero-btn:hover { background: #FFC94D; transform: translateY(-1px); }

  @media (max-width: 768px) {
    .cc-title { font-size: 1.9rem; }
    .cc-register { padding: 26px 20px 22px; }
    .cc-ledger-cell { border-left: none; padding: 8px 0; }
  }
  @media (max-width: 480px) {
    .cc-title { font-size: 1.7rem; }
    .cc-hero-btn { padding: 11px 16px; font-size: 0.85rem; }
  }
</style>

<script>
  (function () {
    function animateCount(el) {
      var target = parseInt(el.getAttribute('data-count') || '0', 10);
      if (isNaN(target)) return;
      var dur = 900, start = null;
      function step(ts) {
        if (!start) start = ts;
        var p = Math.min(1, (ts - start) / dur);
        el.textContent = Math.round(target * (1 - Math.pow(1 - p, 3)));
        if (p < 1) requestAnimationFrame(step);
      }
      requestAnimationFrame(step);
    }
    var counters = document.querySelectorAll('[data-count]');
    if (!('IntersectionObserver' in window)) {
      counters.forEach(animateCount);
    } else {
      var io = new IntersectionObserver(function (entries) {
        entries.forEach(function (e) {
          if (e.isIntersecting) { animateCount(e.target); io.unobserve(e.target); }
        });
      }, { threshold: 0.3 });
      counters.forEach(function (c) { io.observe(c); });
    }
  })();
</script>

</Layout>
